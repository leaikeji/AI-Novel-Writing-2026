# AI小说世界2026 项目偏好

## 1. 项目定位

- 本项目是运行在 QwenPaw 2.1.x 中的个人小说写作 PawApp，核心目标是让作者安全、可恢复、可审计地完成长篇小说创作。
- 它同时包含产品研究、正式规格、前后端代码、数据库迁移、小说 Skills、运行脚本和验收证据；处理任务前先判断当前工作属于哪一层。
- **仅面向桌面端 Web 使用，不涉及手机端或平板端。** 不默认设计、开发或验收移动端布局、触控交互、手机视口及移动端专属流程；只有用户明确改变产品范围后才纳入。
- 默认以中文沟通、写文档和说明结论；代码标识、协议字段及已有英文术语保持项目现状。
- 先交付用户要求的结果，再说明关键判断、验证结果和剩余风险。不要把计划、推断或演示效果表述成已经实现并验收的事实。

## 2. 开始工作前

1. 先运行 `git status --short`，识别并保留用户现有改动。不得重置、覆盖、暂存、提交或清理与当前任务无关的文件。
2. 先读 `README.md`、`docs/README.md`、`docs/开发文档/README.md`，再读与任务直接相关的最新计划、ADR、验收记录和代码。
3. 检查目标文档顶部的“状态”和阶段闸门。写着“已批准计划”“仅批准阶段 0”“尚未实施”的内容，不等于获准完成全部施工。
4. 若当前工作区、最新代码、旧阶段文档和新计划互相矛盾，区分“当前实现事实”与“目标决策”，在扩大改动前明确指出冲突。
5. 搜索文件优先使用 `rg`/`rg --files`，并排除 `.git`、`node_modules`、`build`、缓存和其他生成目录；不要从构建产物反推源代码修改。

## 3. 权威来源与判断方式

- 判断“现在实现了什么”：以当前源代码、Alembic 迁移、自动化测试和可复核运行证据共同为准。未提交或尚未通过相关测试的工作区代码只代表施工中候选，不能单独写成“已经实现并验收”。
- 判断“接下来允许做什么”：以用户当前指令、相关文档的最新状态、已批准计划和 ADR 为准。
- `docs/开发文档` 是本项目产品与架构决策区；行为、接口、数据或阶段状态发生变化时同步更新对应文档与索引。
- `docs/作家助手`、`docs/妙笔神书` 及其他竞品材料只是只读研究证据，不是默认需求，也不授权复制品牌、文案、素材、源码、私有接口或商业规则。
- `audit/` 和 `docs/开发文档/证据/` 用于保存可复核证据；`build/`、`frontend/dist/`、缓存、数据库 dump 和依赖目录不是手工编辑源。
- 调研文档继续使用“已核实事实 / 技术推断 / 项目建议 / 仍待验证”标签，并记录来源、版本或日期。时效性强的外部事实必须重新核验，优先使用官方一手资料。

## 4. 不可突破的安全边界

- 旧项目 `/Users/liujia/Documents/AI小说世界3` 仅可在任务确有需要时只读参考，不得在其中创建、修改或删除文件。
- **绝对禁止枚举、读取、搜索、复制、摘要或以任何方式处理 `/Users/liujia/Documents/AI小说世界3/Data`。** 涉及旧项目的命令必须显式排除该目录。
- 不得无故读取真实密钥，也不得打印、复制、提交或写入日志。优先参考 `.env.example`；只有任务确实需要时才最小化检查本地 `.env` 的必要字段，任何输出都必须隐藏其值。
- 不删除 PostgreSQL/QwenPaw 数据卷，不批量清理用户小说、正文、revision、媒体或审计备份。任何破坏性数据操作都需要用户明确授权、精确目标、事前备份和可执行恢复方案。
- 不静默覆盖权威正文、正式大纲或故事事实。模型输出先进入草稿、候选或提案；只有作者显式操作且基线/CAS 校验通过后，才能进入正式 revision 或故事账本。
- 不改写已经存在并可能执行过的迁移历史。新增 schema 变更使用新的 Alembic revision，并验证升级、兼容与恢复路径。
- 只处理用户自有、已授权或公开许可的小说文本和音频。竞品研究不得演变成正文抓取、登录绕过、反爬规避或私有数据复制。

## 5. 产品与架构偏好

### QwenPaw 上游隔离与升级兼容（硬约束）

- **不允许修改 QwenPaw 的任何上游核心代码。** 禁止 fork 或复制其核心源码后改造，禁止 monkey patch，禁止改写已安装包或容器中的 QwenPaw 源文件，禁止覆盖核心路由/组件，禁止读取或修改私有配置、私有数据库结构及未公开内部模块。
- 所有功能必须位于本仓库的 PawApp、公开的 `PawApp`/`PluginApi`/前端扩展点、官方 HTTP 接口或边界清晰的项目适配器中。使用公开 `route.wrap` 组织原生聊天属于允许的增量扩展；替换原生实现不允许。
- `docker/qwenpaw/Dockerfile` 只能形成可重建的薄运行层：安装公开依赖和本项目 PawApp，不得用 `COPY`、脚本替换或 bind mount 覆盖 QwenPaw 核心目录。QwenPaw 基础镜像及其 digest 必须能独立替换；兼容改动只能留在本项目代码和适配层，不能迁入上游核心。
- “不影响后续升级”在本项目中的可验收含义是：禁用或完整卸载 PawApp 后，不再残留插件路由包装、Skills 或工具注册，QwenPaw 原生功能恢复不受插件拦截的行为；需要保留的用户 workspace/专用 Agent 数据有明确策略；核心数据卷不因插件升级被删除；只依赖已验证的公开契约；每次升级直接在正式环境执行，但必须先完成可复核备份、固定回退点与中止条件；失败时可以退回旧基础镜像或降级公开功能，而不是紧急修改上游核心。
- 无法承诺未知的未来版本一定零适配即可运行。若上游公开契约变化，应在本项目适配层修复并补兼容测试；如果某项功能只能通过修改 QwenPaw 核心实现，立即停止施工，记录缺口、替代方案和降级路径，交由用户重新裁决，不得自行开例外。
- 任何 QwenPaw 版本升级计划必须写明：旧/新版本与 digest、公开契约差异、插件安装/升级/卸载验证、原生页面非回归、数据与配置备份、失败回退和正式切换门禁。

### PawApp 产品边界

- 保持命名空间：宿主入口 `/apps/ai-novel-world-2026`，后端 `/api/ai-novel-world-2026/...`；不得占用 QwenPaw 全局业务路由。
- QwenPaw 原生聊天、Agent、Provider、模型选择、Skills、MCP、工具、设置和插件管理是非回归基线。
- UI API、Agent 工具和后台处理共用同一领域服务与权限规则，不复制业务逻辑。模型传入的小说或文档标识不能替代服务端范围校验。
- 坚持 working copy、不可变 revision、候选/提案和派生数据分层。恢复历史应创建新 revision，不倒拨或改写旧记录。
- 模型、工具或网络失败不得改变正式正文；未同步稿必须保留可恢复路径。幂等键、内容哈希和 requested/actual 模型证据不能被便捷实现绕过。
- “AI 小说作家”Agent 是小说生成任务的模型权威来源。除非有新的明确决策，不新增第二套模型选择器、固定模型白名单、静默模型回退或逐模型 Skills。
- 不为未来设想提前引入第二套 Agent Runtime、通用聊天前端、业务 API、数据库、向量系统、任务队列或复杂权限体系。新增基础设施必须有已批准需求、依赖分析和独立验收。
- 前端继续复用 QwenPaw 宿主提供的 React、ReactDOM 和 Ant Design；不要把第二份宿主框架打进 PawApp bundle。
- 小说创作方法、提示策略和文风规则优先放在 `skills/` 或专用 Agent 文件中；根 `AGENTS.md` 只保存跨任务的工程与治理偏好，避免多处复制同一规则。

## 6. 实现偏好

- Python 遵循现有 FastAPI/Pydantic/SQLAlchemy/Alembic 分层；领域规则放在服务层，API、工具和脚本只做校验、编排与适配。
- 后端首先兼容真实 QwenPaw 宿主的 Python 3.11，并遵守 `pyproject.toml` 的 `>=3.11,<3.14` 边界；不得引入只有 Python 3.12/3.13 才支持的语法或运行假设。
- TypeScript 保持严格类型和现有模块边界；优先复用已有组件、样式和状态模型，不通过 `any`、重复 DTO 或前后端字段漂移绕过契约。
- 数据库事务应覆盖一次业务动作的全部权威写入；外部模型、TTS、向量或媒体调用不得占用长事务。可重试外部操作必须记录状态与幂等证据。
- 依赖优先复用现有固定版本和窄适配器。增加运行时依赖前说明必要性、传递成本、许可证、离线/容器影响及替代方案，并同步 `pyproject.toml`、`requirements.txt`、相关锁文件和依赖版本基线。
- 使用 `pnpm`，不要混用 npm 或 yarn。不要手工修改 `node_modules/`、`build/`、`frontend/dist/` 或 `__pycache__/`。
- 修改 Skill 时同步检查 manifest、安装/升级行为、专用 Agent 作用域和 `tests/test_skill_contract.py`；不要直接编辑打包副本。
- 修改 QwenPaw 公共契约、工具注册、Agent 配置或安装流程时，必须验证幂等安装、升级不覆盖用户选择、完整卸载清理和原生功能非回归。
- UI 仅按桌面 Web 设计；弹窗、三栏工作台、关系图、Diff 和编辑器必须考虑鼠标、键盘操作、焦点、滚动、空态、错误态及数据恢复。桌面浏览器窗口缩放、系统缩放和宿主侧栏挤压仍须按实际影响处理，不将这些桌面场景等同于手机端适配。
- 复核中确认的冗余代码和冗余测试必须安全移除，不能只追加新实现继续堆叠。删除前先证明没有调用者、没有独立风险或契约覆盖、不是已执行迁移／历史验收原始证据，且不会覆盖用户未提交改动；删除后运行受影响回归和 `git diff --check`。语义仍独立的负向测试、兼容测试、恢复测试和历史证据不得以“看起来重复”为由误删。

## 7. 验证与证据

- 验证强度与风险匹配；不得把“代码看起来正确”写成“已通过”。无法运行的项目要明确说明未验证项和原因。
- 本项目不使用隔离、测试或预发布环境做运行态验收。凡涉及 QwenPaw 运行态、浏览器、插件、Agent、Skill、模型、数据库业务链或朗读的验收，一律直接在正式环境执行；不得用隔离环境结果替代正式结论。
- 纯代码层的 lint、类型检查、单元测试、契约测试和构建仍在项目工作区内先行运行，用于阻止明显缺陷进入正式环境；它们只是代码校验，不构成产品验收，也不得用假对象、内存库或模拟返回声称真实写作链已通过。
- 纯计划、偏好、研究和审查报告修改只做文档检查，不安装插件；会被运行时加载的 Skill、Agent 提示或配置文件不按“纯文档”豁免。同一已批准范围内的候选通过代码校验后，集中打包并直接按公开更新路径安装到正式环境验收；不顺带夹带其他未批准候选。
- 每一次正式环境验收都必须当作一次真实创作，从真实的写作目标出发，完整操作当次功能所在的写作链；不做空点击、无意义占位输入、只查接口返回或为了留截图而走过场。
- 正式环境中所有面向作者可见的作品名、分卷名、章节名、人物名、大纲、正文、操作备注与媒体名称，禁止出现“测试”“验收”“演示”“Demo”“Smoke”“Sample”“临时”“XXX”“123”等技术性、占位性或敷衍性字样；标题必须像真实出版作品一样与题材、人物和情节相符，正文必须语义连贯且具备可继续创作价值。内部自动化用例名、代码文件名和历史证据中必要的“测试”术语不在此禁令内，不得为表面避字而损害技术可读性或改写历史事实。
- 真实写作验收应记录完整标题、写作意图、操作路径、模型与 Skill 实际证据、产出质量、恢复情况和作者可见结果。新建内容默认作为可继续创作的正式作品保留；不因它来自验收就自动删除、清空或隐藏。若确需删除，必须遵守精确目标、事前备份、作者明确授权和可恢复要求。
- 完整安装／升级／卸载生命周期测试由风险触发：涉及宿主公开契约、插件注册、安装器、Agent／Skill 配置保持、打包依赖、迁移兼容或已有明确生命周期缺陷时必须覆盖；普通业务或 UI 修改不默认重跑整套，但保留相关集成和真实桌面验证。同一候选、环境与测试范围未变化时可引用可复核的已有证据；变化影响结论才重跑，不用本条跳过计划69等尚未通过的必要门禁。
- 每次确需安装、替换或重启前，简要说明正式环境对象、触发原因、备份、中止条件及恢复安排；不得为方便验收反复替换唯一正式环境、无必要重启 Docker 或清空数据。优先通过公开更新路径替换插件，不把完整卸载后重装作为普通部署默认步骤；不以直接改写已安装副本代替部署。
- Python 或领域改动：使用项目解释器 `.venv/bin/python` 运行相关 `pytest`；跨领域、schema 或运行时改动运行 `.venv/bin/python -m pytest`。若 `.venv` 不存在，先显式解析一个满足 `>=3.11,<3.14` 的项目 Python，不得假定裸 `python` 命令可用。数据库代码校验必须使用工作区临时库且不得连接正式数据；涉及数据库业务行为的产品验收仍只在正式环境中通过正常写作操作执行，不直接篡改生产表。
- 前端改动：至少运行相关 Vitest，并按影响运行 `pnpm typecheck`、`pnpm test`、`pnpm build`。
- manifest、打包或安装改动：运行 `.venv/bin/python scripts/package_plugin.py` 和相关契约测试；需要真实运行态时再用同一项目解释器执行验证脚本，并报告容器、数据库和 Agent 的实际状态。
- Compose 或部署改动：先运行 `docker compose config`；涉及迁移、卷或升级时先备份，再验证健康检查与失败恢复。不得为测试方便删除持久卷。
- QwenPaw 上游升级：不设隔离或预发布环境，直接在正式环境验证插件安装、原位升级、完整卸载、原生聊天与设置非回归、Agent/Skill/工具作用域、数据卷保留和旧镜像回退。动手前必须完成正式数据与配置备份、记录镜像 digest、预演可执行的回退命令并设定中止条件；任一前置不满足就不得升级。
- UI 改动在自动化通过后做真实桌面浏览器检查；以实际桌面视口为准，按影响补充常用桌面尺寸、窗口缩放和宿主面板挤压路径，并把必要截图/报告放到约定的 `audit/` 或证据目录。不得默认加入 390×844 等手机视口、移动端截图或手机端通过门禁。
- 自2026-09-12作者明确桌面端范围起，旧计划中的手机／移动端验收要求不再作为新施工或结项阻断；历史截图和当时验收结论原样保留，仅在当前计划／台账注明范围调整，不伪称问题已修复。既有响应式代码与测试不得因此批量删除；与桌面缩放、容器宽度或通用可访问性有关的覆盖继续保留。
- 文档改动要检查相互链接、状态、日期、版本号和“已实现/已批准/待验证”措辞；不要为了统一表面文本而抹掉历史验收记录。
- 完成前复查 `git diff --check`、`git status --short` 和本次 diff，确认没有秘密、生成物、临时文件或无关改动。

## 8. 开发文档与子代理并行规划

- 新建或重写开发计划、施工计划、阶段实施文档、迁移计划或大型修复方案时，必须包含明确的“子代理并行施工设计”。简单到不值得并行的任务也要写“本任务不并行”及原因，不能省略判断。
- 子代理并行设计必须直接写在当前开发文档内部，不建立或依赖一个跨多个开发计划的总施工矩阵。当前文档要为可派发任务分配本地稳定工作包 ID，并使用 `PAR`、`PAR-C`、`SER`、`MUTEX`、`GATE`、`INT` 等标记；这些 ID 只属于本计划，并行标记只说明施工组织，不自动批准未立项功能。
- 每份计划至少写清：波次/ready set、工作包 ID、目标与非目标、前置依赖与阶段门禁、精确文件/目录所有权、只读范围、禁止触碰范围、共享资源锁、冻结接口、验收命令与证据、汇合顺序和唯一集成责任人。
- 优先并行：只读代码或文档审计、官方资料与许可证核验、相互独立的技术尖峰、契约冻结后的独立前后端模块、分层测试、不同页面的视觉/可访问性检查，以及互不共享故事状态的独立样书验收。
- 必须串行或单一所有者：范围/ADR 裁决、公共 schema 与 API 冻结、Alembic revision 和迁移顺序、同一共享文件或锁文件、数据库备份与破坏性操作、QwenPaw 安装/升级/卸载、共享 Agent/模型状态切换、最终集成、全量回归、暂存、提交和推送。
- 并行写代码前，主代理先冻结共享 DTO、状态机、数据库字段、迁移所有者和入口接线方式。多个代理不得同时修改同一文件；无法隔离写入范围时改为串行，不能靠事后解决大量冲突代替设计。
- 每个子代理任务必须写明：稳定工作包 ID、唯一目标、非目标、允许修改的精确文件、不可改变的接口、前置输入、必须运行的测试、要返回的证据、禁止触碰的共享文件/用户改动，以及给主代理的集成说明。
- 用户已为“当前开发文档中已批准进入施工的范围”授权按本文档自己的并行设计主动使用子代理并滚动并行；主代理应在当前运行环境允许且确实能缩短墙钟时间时执行，不必为该文档内每个工作包重复询问。该授权不表示一个聊天任务可以顺带派发其他开发文档的工作。子代理若还能拆成独立且不重叠的工作，可做有边界的下级委派；其上级仍对结果负责。
- 所有子代理共享当前权限与工作区，不能因并行而扩大范围。子代理默认不得暂存、提交、推送、删除数据、操作唯一共享运行环境或修改未分配文件；主代理负责等待结果、复核 diff、解决冲突、运行集成测试和作出阶段退出判断。

## 9. 协作与交付

- 默认自行完成可安全判断的工作；只有缺少会实质改变产品范围、数据处理或外部行为的决定时才询问用户。
- 执行已有开发计划时，只读取并执行当前开发文档内部的并行施工设计；满足前置门禁的 `PAR`/`PAR-C` 工作包按该文档的依赖波次派发，`SER`/`GATE`/`INT` 由主代理或指定唯一所有者完成。切换到另一份开发文档属于另一项施工任务，不能因为有空闲子代理而自动扩展范围。
- 不主动提交或推送 Git，除非用户明确要求。被要求提交时，只包含当前任务范围，并先展示或复核待提交 diff。
- 最终交付简明列出：完成内容、关键文件、实际验证、未验证或剩余风险。若修改影响数据、兼容性或用户创作内容，额外说明恢复方式。
