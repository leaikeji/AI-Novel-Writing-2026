# 角色关系网 Design QA

日期：2026-08-24

## 比较目标

- 源关系图：`/Users/liujia/.codex/visualizations/2026/08/24/01a0327a-66b0-7bd3-a795-54d21cbd2748/relationship-audit-110972/03-source-relationship-graph.png`，1265×712，DPR 1。
- 源编辑器：`/Users/liujia/.codex/visualizations/2026/08/24/01a0327a-66b0-7bd3-a795-54d21cbd2748/relationship-audit-110972/04-source-node-relationship-editor.png`，1265×712，DPR 1。
- 最终关系图：`implementation-graph-verified-1265x712.png`。
- 最终编辑器：`implementation-editor-verified-1265x712.png`。
- 同图对比：`compare-graph-verified.png`、`compare-editor-verified.png`，均为左侧妙笔神书、右侧本项目。
- 实现状态：本地真实作品《回到1988，坐在你身边》，6 个角色、2 条关系（1 条人工、1 条由章节“同步进展”自动生成）、1 个已保存布局视图。

## 全视图与重点区域

- 关系图与编辑器都使用与源图相同的 1265×712 CSS 视口、DPR 1 采集。
- 两组最终对比图已在同一比较输入中完成视觉复核。
- QwenPaw 宿主导航、作品封面与妙笔神书站点壳层不同，属于已明确排除的运行环境；验收聚焦于角色子视图、图谱画布与关系编辑器。

## 必查保真面

- 字体与排版：标题、辅助说明、节点姓名、边标签、弹窗字段层级清晰；修复了 `dot` 节点使白色姓名落在白底上不可见的问题。
- 间距与布局：标题后直接进入 540px 图谱画布；搜索、筛选和布局操作置于画布浮层，角色群组缩放与下移后不被遮挡。
- 颜色与令牌：主角橙、配角蓝、人工关系棕橙虚线、AI 关系实线、白底灰框和橙色主操作与项目现有视觉令牌一致。
- 图片与资产：本功能无新增位图资产；放大、缩小、适应、自动排布、保存、新增和删除使用现有 Ant Design 图标库。
- 文案与内容：保留“角色关系网 / 点击角色/连线编辑关系 / 滚轮缩放 / 拖动调整位置”核心语义；本地作品数据与源站示例不同属预期动态内容。
- 可访问性：Canvas 画布有语义说明和键盘支持；同步关系列表作为完整的 DOM 替代操作面，不依赖 Canvas 也能查看与打开关系。

## 对比与修复历史

1. 首轮：前端动态 chunk 在 QwenPaw blob 加载环境下无法解析，画布为空。生产构建改为单文件输出后修复。
2. 第二轮：提示和工具栏占用正常文档流，图谱首屏被推到过低位置。改为画布内浮层，并在小屏下回退为普通文档流。
3. 第三轮：节点姓名不可见，且顶部节点被浮层遮挡。节点改为固定最小直径的 `circle`，收紧力导向参数，自动缩放并向下避让工具层。
4. 编辑器轮：宿主默认按钮和 Radio 样式覆盖插件令牌，底部操作在 712px 高度下被裁切。增强弹窗作用域、显式定义单选状态，并收紧可滚动主体高度后修复。

## 交互与运行检查

- 章节“同步进展”→智能提案确认→关系网增量落库：通过；真实作品已自动生成“林知禾 — 陈岁安 / 调查同盟”。
- 同一章节重复同步：通过；关系按来源事实与端点幂等合并，不重复创建。
- 人工编辑 AI 关系：通过；保存后标记为 `manual_override`，后续章节同步不会覆盖或复活人工归档关系。
- 进入关系页：通过；不自动触发整书模型任务。整书扫描仅由“补全历史关系”显式触发。
- 自动关系审计字段：通过；保留来源章节修订、智能提案条目、生成任务、置信度与证据。
- 自动排布→“视图未保存”→保存布局→刷新恢复：通过。
- 从 DOM 关系列表打开现有关系编辑器：通过。
- 旧关系不确认方向时保存禁用；选择“无向关系”后警告消失、保存启用：通过，验收后关闭未保存草稿，未改写关系语义。
- 新增关系打开独立草稿，空名称时保存禁用：通过，验收后关闭未保存草稿。
- 控制台 `error` / `warning`：0。
- 前端：TypeScript 通过；3 个测试文件、8 项测试通过；Vite 生产构建通过。
- 后端：41 项默认测试通过，19 项需集成数据库的项目按现有规则跳过；关系网 PostgreSQL 定向集成测试 3 项通过；真实 QwenPaw 实例健康检查通过。
- 数据库迁移：`20260824_0008`；实际 PostgreSQL `alembic_version` 已在该版本。

## Findings

- 无可执行的 P0、P1 或 P2 差异。
- [P3] 源图只暴露缩放和上下平移，本项目显式增加搜索、分类/方向筛选、适应画布、自动排布、撤销布局和显式保存，工具层更高；这是已确认的功能增强，不影响图谱主任务。
- [P3] 对比两侧使用不同真实作品，关系数据不作为视觉差异；本项目最终状态同时覆盖人工关系与 AI 自动关系。

final result: passed
