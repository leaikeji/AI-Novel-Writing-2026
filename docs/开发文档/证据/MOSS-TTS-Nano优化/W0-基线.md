# MOSS-TTS-Nano 优化 W0 基线

状态：**2026-08-29 `MNX-G0=PASS`。只读基线已冻结，尚未修改业务代码、数据库、模型、运行配置、媒体或小说数据。**

## 1. 仓库与工作区

- 仓库：`/Users/liujia/Documents/AI小说世界2026`
- Git HEAD：`054c1d4a49f223d1eda8e6441caa6ef7cd2710d6`
- 当前 tracked source tree SHA-256：`2f46954d459a3931f1172501fd16c77525c359865a9effc3ea91d72b8fd30a92`
- 开工时 `git status --short` 只有本计划已经识别的两个文档改动：
  - `docs/开发文档/33-MOSS-TTS-Nano全预设直用人物专属音色与播放体验优化计划.md`
  - `docs/开发文档/README.md`
- 未发现计划 32 残留的业务代码、迁移、工作台或人物模型 dirty 文件；本计划可以取得相应文件所有权。

source tree hash 使用当前工作区的 `git ls-files` 路径与实际文件内容计算，因此包含上述已识别文档候选，而不是只代表 HEAD 提交内容。

## 2. 迁移基线

- 本地 `.venv/bin/alembic heads`：`20260829_0029 (head)`，单 head。
- 长期 PostgreSQL `alembic_version` 只读查询：`20260829_0029`。
- 开工迁移所有权从 `0029` 之后开始；任何 schema 变更只允许创建当时下一线性 revision，不改写 `0010–0029`。

## 3. 包、Bundle 与宿主

- `plugin.json` / `pyproject.toml`：均为 `0.4.0`。
- `build/ai-novel-world-2026` canonical package tree SHA-256：`672ec9c774db696d4bf156af78e0d25919dd47585dd987702b14d2e24f0bb0ee`。
- 已安装 `/app/working/plugins/ai-novel-world-2026` canonical package tree SHA-256：`672ec9c774db696d4bf156af78e0d25919dd47585dd987702b14d2e24f0bb0ee`。
- source、build 与已安装 `frontend/dist/index.js` SHA-256：均为 `06b6f54eb400fbb60a7cd9806cb9e3e82da6b8172e905103321a5ac31b4aa083`。
- QwenPaw 基础镜像：`ai-novel-2026-qwenpaw-runtime:2.1.0-mvp0`。
- 镜像 digest：`sha256:ea2c0858b94a6522821cceb73593bbea5f02c85fee2f08a8d7386ea90d9f15b1`。
- 长期 QwenPaw、PostgreSQL、Nano Sidecar 均为 `running/healthy`；本轮只读检查没有重启、重建或安装。

package tree 使用相对路径与逐文件 SHA-256 计算；build 与已安装结果相同。该结果证明 W0 时安装态与现有 build 候选一致，不代表后续新代码已经打包或安装。

## 4. 运行能力基线

`GET /api/ai-novel-world-2026/health` 的关键只读结果：

- PawApp `version=0.4.0`、`status=ready`；
- PostgreSQL `18.6`，connected；
- `generation_agent_id=ai-novel-writer`；
- `generation_model_policy=follow-agent-effective`；
- Nano `technical_enabled=true`、`lifecycle_status=ready`、`product_visible=true`；
- Nano protocol `moss-tts-sidecar/1.1`，模型 fingerprint `3c76f3e9e1381699c5555287cf66eeb023632d0c3ee94adc6d8ae1b1d455fd7d`；
- `model_loaded=false`、`idle_unload_seconds=300`；
- `reference_clone_ready=false`，VoiceGenerator 仍未产品化。

## 5. W0 退出结论

- 源码、build 与安装 bundle 身份已冻结，旧截图不再作为当前候选证据。
- 迁移 head、人物权威、Agent 模型政策和运行能力没有出现跨计划冲突。
- `MNX-G0=PASS`；后续代码施工仍必须先通过同波 `MNX-PRUNE-AUDIT`，再进入 `MNX-P0-CONTRACT`。
