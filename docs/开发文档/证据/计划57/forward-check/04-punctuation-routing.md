不触发 `suspense-writing` 或 `golden-finger-writing`。都市日常不是悬疑证据，标点校对也不是特殊能力设计；不因为此前对话在做分类 Skill，就把分类规则继续带入。

这次只做普通标点校对，不重写剧情、不新增设定、不输出内部检查表。没有生成或重写正文的要求，因此不强行调用 `prose-writing`；也不需要为完成窄机械任务加载其他小说方法。若当前消息确实只有“帮我校对这段都市日常文的标点”而没有原文，应答：

“请把原文发来，我只修改标点，保留措辞和内容。”
