# 项目滚动条视觉规范增量 QA

## 对比目标

- 源视觉真相：`reference.png`，1242×1328 像素，用户提供的妙笔神书“编辑章纲”截图。
- 实现截图：`implementation-final.png`，1920×1080 像素；CSS 视口 1920×1080，DPR 1。
- 聚焦同图对比：`comparison-focused-scrollbar.png`，1180×380 像素。
- 状态：正文编辑页打开“编辑章纲”弹窗，章纲文本足够长并出现纵向滚动条。

## Findings

- 无 P0/P1/P2 问题。
- [P3] 实现的滚动条在当前 Chromium 的 `thin` 渲染下比参考图约窄 1–2px；灰度、圆角、轨道明度和轻量感已经一致，保留更窄尺寸可减少对正文宽度的侵占。

## 必查保真面

- 字体与排版：本次只改滚动条视觉令牌，未改变正文、标签、按钮字体与排版。
- 间距与布局：滚动条固定 8px，未挤压弹窗布局；文本框仍为 553×316，可见内容区未发生结构变化。
- 颜色与令牌：轨道 `#f1f2f3`、滑块 `#919499`、悬停 `#777b81`，与参考的浅轨道、中灰滑块一致，不再出现近黑色粗条。
- 图片与资产：该控件没有图片资产；未新增 CSS 图形或替代图标。
- 文案与内容：无文案改动。

## 作用范围与交互

- 统一覆盖 `.anw-app` 内的页面、列表、正文区域，以及 `.anw-modal` 内的弹窗正文、列表和 textarea。
- 明确不覆盖 QwenPaw 宿主侧栏和宿主页面滚动条；宿主侧栏实测仍为 `scrollbar-color: auto`。
- 章纲 textarea 实测：`overflow:auto`、`resize:vertical`、高度 316、内容高度 359；右下角上下缩放能力保留。
- 统一样式实测：`scrollbar-width: thin`、`scrollbar-color: rgb(145,148,153) rgb(241,242,243)`、`color-scheme: light`。
- 悬停态由共享令牌切换到 `#777b81`；横向滚动条与纵向滚动条共用同一套 8px 规范。
- 页面控制台 error/warn：0。

## 比较历史

1. 用户指出原滚动条近黑、粗重，与参考图的轻量灰色拖拽条差异明显。
2. 建立全项目共享滚动条令牌和 WebKit/Firefox 双实现，只作用于项目自有页面及弹窗。
3. 修正章节创建向导原有的局部滚动条覆盖，避免同一项目出现两套拖拽条。
4. 重新构建、部署，并在 1920×1080 下进行聚焦同图比较；未发现需继续修正的 P0/P1/P2 差异。

## Implementation Checklist

- [x] 统一轨道、滑块、悬停、圆角和横纵尺寸。
- [x] 覆盖项目自有页面、弹窗、列表和 textarea。
- [x] 保留 textarea 上下缩放能力。
- [x] 排除 QwenPaw 宿主壳层。
- [x] 通过 TypeScript、8 项前端测试、生产构建、容器健康和浏览器控制台检查。

final result: passed
