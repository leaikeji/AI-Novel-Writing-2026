# L51-W1 权威一致性与写命令验收

状态：**PASS；候选源码与隔离测试已通过，尚未安装到长期 QwenPaw，也未写入真实小说。**

日期：2026-09-02（Asia/Shanghai）

## 1. 交付事实

- `backend/story_state/effective_state.py` 成为唯一无 I/O 双轴 resolver：事实纳入／生命周期与 health 独立，按 fact 组合 lifecycle、来源 binding、owning batch、入向 supersedes、timeline 和 projection 证据。
- `backend/story_state/fact_authority.py` 成为各数据库消费者共同的证据装配器；缺失 owning batch 失败关闭，不再把其他 revision 的 binding 状态传播给当前 fact。
- Context V3/V4、story-state、embedding、人物 workspace 读模型均消费同一 resolver 结果；已删除各自的 revision-wide validity map 和 raw `StoryFact.status` 纳入捷径。
- 章节 intelligence 不再制造“人物在本章出现”的 fallback 假事实；`no_changes + items=[]` 保持合法。原专用实现测试已在独立 no-changes 回归迁移完成后删除。
- intelligence commit、checkpoint、candidate adopt、revision restore、事实修正、批次撤销、timeline／event link／instance／revision mapping、人物／关系／故事线／伏笔和来源显示相关根写入，均按冻结矩阵收敛为：先锁 Novel 聚合根，实际权威变化每个业务事务只推进一次 `story_ledger_version`，no-op、精确回放和失败回滚推进零次。
- correction／batch revert 的同 key 同 payload 回放优先于 stale CAS；同 key 异 payload冲突；已撤销批次不再通过直接返回绕过 key/hash 核对。批次撤销只改变 batch 轴，不改写历史 fact lifecycle。
- 人物 root/profile 组合保存、关系批量操作和 timeline merge 的复合事务由唯一外层决定一次版本推进；纯关系图布局、working-copy 内容和其他明确排除项不推进账本版本。

## 2. 冗余清理

- 删除 `_fallback_character_presence_candidates`、调用路径和只覆盖该实现的 `tests/test_intelligence_presence_fallback.py`。
- 删除消费者本地的 lifecycle／binding／batch／supersedes 优先级和跨 revision fallback；语义独立的冲突、恢复、兼容、no-op 与负向测试保留。
- 没有删除已执行迁移、历史验收原始证据、StoryFact 来源快照、`accepted_item_ids`、`inverse_operations` 或其他恢复字段。

## 3. 2026-09-02 复验

定向命令：

```bash
.venv/bin/python -m pytest \
  tests/story_state/test_effective_state.py \
  tests/story_state/test_fact_authority.py \
  tests/context_v4/test_loader_fact_authority.py \
  tests/embedding/test_api_fact_authority.py \
  tests/character_workspace \
  tests/story_state/test_batch_revert.py \
  tests/story_state/test_persistence_contract.py \
  tests/story_state/test_revision_mapping_services.py -q
```

结果：**122 tests collected，全部通过**。唯一输出为项目既有 Starlette/httpx 弃用警告。

W1 施工期间另以一次性 PostgreSQL 18 容器验证并发、锁顺序、完整 command snapshot 矩阵和领域组合路径；合并后的 W2 同一隔离 PostgreSQL 回归仍通过。长期环境、正式数据库和用户正文均未写入。

## 4. 尚不代表的结论

- 本证据不是 W5 全量 pytest、前端、打包、真宿主浏览器或插件安装／卸载证据。
- 候选仍处于 dirty 工作树；未暂存、提交、推送或长期发布。
- W1 没有创建 schema migration，也不授权后续兼容列收缩。
