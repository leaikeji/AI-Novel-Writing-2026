# L51-W5 候选整体验收与发布门禁

状态：**`L51-QA` PASS；本文件冻结 W5 候选验收时点。后续 `L51-RELEASE` 已获授权并完成候选／恢复点预检，但因长期四角色生产基线缺失而在写入前停止；见独立发布预检证据。**

日期：2026-09-02（Asia/Shanghai）

## 1. 最终候选身份

| 项目 | 当前事实 |
| --- | --- |
| 源码基准提交 | `ee0992dc8e8cd1ded40b99ead662e629ca8b2bbb`；工作树 dirty，未暂存、提交或推送 |
| 候选迁移 head | `20260902_0037 (head)`；只新增账本默认页所需的部分索引 |
| 打包目录 | `build/ai-novel-world-2026` |
| 候选树 SHA-256 | `7233b58013118f893a044ae4edc7d44cc9e287e4f8625f18d274b6054d94df37` |
| 前端 bundle SHA-256 | `dd5f913799efe35f2dc4512b894deee587b82d3f310f90751c4c68ea903246bf`；源码 dist、打包副本与隔离安装后 HTTP 资产一致 |

`scripts/tts/verify_qwenpaw_plugin_lifecycle.py --mode dry-run` 和最终候选的真实隔离生命周期均通过；真实报告中的 candidate tree、migration head 和前端资产摘要与上表一致。

## 2. 自动化结果

- 后端全量：`.venv/bin/python -m pytest` → **3605 passed、180 skipped、3 warnings**，37.28s。
- 迁移定向：唯一 head、模型／DDL 一致、迁移窄边界、角色保护和 live upgrade／downgrade 均通过；未提供隔离数据库变量时，live 用例按设计 skip。
- 前端全量：使用工作区固定 Node／pnpm 运行 `pnpm test` → **137 files、1169 tests passed**；`pnpm typecheck` 与 `pnpm build` 均退出码 0。
- 插件：`.venv/bin/python scripts/package_plugin.py` 退出码 0；`tests/test_skill_contract.py` → **12 passed**。

第一次直接运行 `pnpm test` 时 shell 的 `PATH` 没有 Node，命令在收集测试前退出；加载工作区固定工具链后，上述前端完整命令全部通过。这是工具链入口问题，不记为测试失败，也没有用它掩盖任何未运行用例。

## 3. PostgreSQL 迁移与最终基准

用户批准 `L51-MIG-QUERY` 后，新增 revision `20260902_0037`：

```sql
CREATE INDEX ix_story_facts_novel_created_v2
  ON story_facts (novel_id, created_at DESC, id DESC)
  WHERE schema_version = 'story-fact/2';
```

隔离 PostgreSQL 实际完成 `0036 → 0037 → 0036 → 0037`；4,000 条合成事实全部保持。迁移前 2,000 条默认页为 Seq Scan、访问 2,000 行；迁移后为正式索引上的 Index Only Scan、访问 21 行。downgrade 只删除索引，不删列或数据。

最终 benchmark 要求正式索引已经存在，不再创建／删除探针索引。每页返回 20 条：

| 事实数 | list SQL / ORM / 峰值 / p50 / p95 | summary SQL / ORM / 峰值 / p50 / p95 | detail SQL / ORM / 峰值 / p50 / p95 | 默认页计划 / 扫描 / 返回 |
| ---: | --- | --- | --- | --- |
| 500 | 7 / 20 / 0.595 MiB / 106.963 / 111.364 ms | 3 / 0 / 0.488 MiB / 106.300 / 114.168 ms | 6 / 1 / 0.493 MiB / 97.060 / 101.570 ms | Index Only Scan / 21 / 20 |
| 2,000 | 7 / 20 / 0.514 MiB / 113.188 / 121.489 ms | 3 / 0 / 0.487 MiB / 109.905 / 111.999 ms | 6 / 1 / 0.562 MiB / 114.024 / 121.149 ms | Index Only Scan / 21 / 20 |
| 10,000 | 7 / 20 / 0.512 MiB / 1030.611 / 1038.993 ms | 3 / 0 / 0.605 MiB / 1015.844 / 1070.268 ms | 6 / 1 / 0.501 MiB / 986.091 / 1014.919 ms | Index Only Scan / 21 / 20 |

2,000 条硬门禁要求默认页访问不超过 64 行，实际为 21 行，**PASS**。10,000 条仍是非阻断 soak；端到端毫秒数明显受测试容器、缓存和同库样本影响，本计划只以可执行的 SQL 数、ORM 装载、内存和页选扫描门禁作发布判断，不把这组时延写成产品承诺。迁移完整证据见 [L51-MIG-QUERY 窄分页索引迁移验收](./L51-MIG-QUERY-窄分页索引迁移验收.md)。

## 4. 真实隔离插件生命周期

最终候选执行：install → force reinstall → uninstall → reinstall，run ID 为 `l51w5c0902`，结果 **PASS**：

- candidate staging 使用 `docker cp`，无 host bind mount；每次安装前复核候选树摘要；
- 候选与已应用迁移 head 均为 `20260902_0037`；
- 安装、强制重装、公开 DELETE 卸载和重新安装均返回成功；
- 卸载后插件目录、PawApp／插件注册、静态资产和本项目 API 路由为零残留，QwenPaw 原生 shell、Agent、Skills、Tools 路由继续可用；
- 隔离数据库哨兵及 4 个数据／配置卷哨兵在重装后保持；
- TTS 在无模型／token 的隔离拓扑中按构造保持 disabled，没有静默联网或启动 sidecar；
- 结束时只删除 run ID 对应的 2 个容器、5 个卷和 1 个网络，`broad_cleanup_used=false`，cleanup PASS。

原始机器报告：[W5-隔离插件生命周期-0037.json](./W5-隔离插件生命周期-0037.json)。此前 `l51w5qa0902`、`l51w5qb0902` 两次在 Docker `start-postgres` 阶段失败的报告继续保留为历史环境故障证据；它们没有加载候选，不再构成当前阻塞，也没有被改写成成功。

## 5. 1080P／2K 真实前端复核

按照用户最新口径，今后的真实页面验收固定覆盖 **1080P（1920×1080）和 2K（2560×1440）**。本轮在真实 QwenPaw shell 与当前候选 bundle 上，以只读合成 ledger GET fixture 完成两档复核；临时代理拒绝 POST／PUT／PATCH／DELETE，结束后已关闭并删除脚本。

两档都验证了原生助手展开与折叠：账本入口、总览、筛选、列表、详情和选中态可读，无页面级横向溢出，console error 为 0。1080P 额外验证详情深链、来源证据和真实 `<mark>`；Escape 关闭后焦点返回“查看来源证据”。没有触发修正、撤销或其他写动作。

Browser 目标 viewport 设置为 1920×1080／2560×1440；Codex 内嵌浏览器截出的内容面分别为 1900×1069／2535×1426（扣除了内嵌容器保留像素），因此不伪称截图文件自身正好等于目标分辨率。支持的浏览器接口不能改变真实浏览器缩放，本轮也不伪称执行了真实 200% zoom；200% 等效窄屏继续由既有自动化／历史诊断覆盖，不替代 1080P／2K 的真实验收。

| 证据 | SHA-256 |
| --- | --- |
| `W5-ui/ledger-1920x1080-assistant-collapsed.png` | `0741fe7092bc4beafffb5c85bfd766a0654caca3a32ed92e3f07e9d84369c215` |
| `W5-ui/ledger-1920x1080-assistant-expanded.png` | `1741cca28e778e834699bfb01164a68d6aaf5e80b209154d37cc7d15b72372e2` |
| `W5-ui/ledger-1920x1080-detail-source-assistant-expanded.png` | `8e604553a5880858c8f708f47a367ce7e363b58944b398742717b8be25631f39` |
| `W5-ui/ledger-2560x1440-assistant-collapsed.png` | `f9ab3a8d54f11c6d821e9a961e63ab3b38f2125879502f1493bd8049d8f2b47a` |
| `W5-ui/ledger-2560x1440-assistant-expanded.png` | `58d746670f82675c8f67a3c26f40a7b3010c2b0150a1a78cfd76d736e1903ed4` |
| `W5-ui/ledger-2560x1440-detail-assistant-expanded.png` | `f6d6df5a532768cd399c3f7388decb468a7875c42f172a1ca68f501ad467a2e3` |

这证明候选前端在真实 QwenPaw shell 中的 loader、目标桌面布局和关键交互；完整后端安装／卸载由第 4 节独立生命周期证据覆盖，两类证据不互相冒充。

## 6. 发布裁决与恢复边界

在 W5 冻结时点，`L51-QA` 已整体 PASS，已知问题应先修复的工作完成；当时下一步不是继续增加账本功能，而是等待用户是否单独授权 `L51-RELEASE`。后续授权与预检结果不回写成 W5 已发布，统一见文件末尾的独立证据链接。

W5 本轮没有修改真实小说、长期数据库或长期 PawApp，没有删除 PostgreSQL／QwenPaw 持久卷，也没有暂存、提交或推送。公开旧 GET 和兼容字段仍在截至 2026-12-01 的窗口内，不以“冗余”为由提前删除。

后续发布状态：[L51-RELEASE 长期发布预检与恢复点](./L51-RELEASE-长期发布预检与恢复点.md)。该增量已完成候选冻结与备份，但没有迁移、写 ACL 或安装候选。
