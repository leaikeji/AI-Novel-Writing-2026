# L51-W4 冗余清理与兼容边界

状态：**内部冗余清理 PASS；公开兼容项按冻结窗口保留，未提前做破坏性删除或 schema 收缩。**

日期：2026-09-02（Asia/Shanghai）

## 1. 已安全删除

- 删除会把合法 `no_changes` 伪造成“人物在本章出现”的 `_fallback_character_presence_candidates`、调用路径和只覆盖该实现的专用测试；独立 no-changes 回归已迁入仍存活的 intelligence 契约测试。
- 删除无生产调用者的 `sync_relationships_from_intelligence_proposal` 旧包装器，并迁移／保留真实领域风险测试。
- 人物卡和全书账本切到共享模块后，删除只为旧事实墙存在的 presenter helper、测试壳和孤立 `StoryFactRecord`。
- 第一方前端不再发送恒为空的 `item_overrides: {}`；兼容字段只留在后端公开请求 schema 和兼容测试中。
- 第一方关系 UI 统一显示 `label`，不再继续扩散 `relation_type ?? label` 双轨展示逻辑。

调用者复查未发现已删包装器、`StoryFactRecord` 或旧 presenter helper 的存活引用。删除后定向、全量前后端回归与构建均通过。

## 2. 有意保留的兼容项

- `CommitIntelligenceRequest.item_overrides` 已标记 deprecated，sunset 不早于 **2026-12-01**。现有兼容／负向测试仍使用非空 override 验证 canonical payload 与幂等冲突，不能在窗口到期前误删。
- 旧公开 `GET /novels/{novel_id}/story-facts` 返回 `Deprecation: true`、sunset 和 successor Link；最早在 0.6.0 且不早于 2026-12-01 才能进入 `L51-CLEAN-COMPAT`。
- 数据库／API 的 `relation_type` 与 `DerivedSourceBinding.derived_entity_type` 仍属于旧包回退和已执行迁移边界；本轮只停止第一方新代码扩散，不创建列删除迁移。
- 已执行迁移、历史事实、来源快照、commit batch、inverse evidence、负向／恢复／兼容测试和历史验收证据均保留。

## 3. 未授权项

- 没有删除真实 fallback 历史候选、用户小说、revision 或批次。
- 没有创建 `L51-MIG-COMPAT`，没有改写旧 Alembic revision。
- 没有把“当前有替代路径”误写为“公开兼容窗口已经到期”。
