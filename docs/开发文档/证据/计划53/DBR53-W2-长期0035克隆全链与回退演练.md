# DBR53-W2 长期 0035 克隆全链与回退演练

状态：**PASS（2026-09-02 12:13 +08:00）；仅隔离克隆，长期环境尚未在本证据阶段修改。**

## 1. 冻结输入

- run ID：`dbr53r0902a`；全部 Docker 资源均带 `ai.novel.world.gate=dbr53-role-rehearsal` 与 `ai.novel.world.validation-run=dbr53r0902a`，无端口、内部网络。
- 输入 dump：`postgresql-0035-before.dump`，25,801,699 bytes，SHA-256 `cc469547264c48a72f7419730975bdbf00adba2ab85ab7060f86c44876f32fed`。
- 候选树：SHA-256 `7233b58013118f893a044ae4edc7d44cc9e287e4f8625f18d274b6054d94df37`，head `20260902_0037`。
- 候选前端 bundle：SHA-256 `dd5f913799efe35f2dc4512b894deee587b82d3f310f90751c4c68ea903246bf`。
- QwenPaw 不可变 image ID：`sha256:ea2c0858b94a6522821cceb73593bbea5f02c85fee2f08a8d7386ea90d9f15b1`。
- PostgreSQL image digest：`sha256:2ba9ca5f2e7daa0f0e7723cba1ee9167bab54efd3640516a44ac1a928dd67e7a`。
- maintenance-root 树 SHA-256：`43491339e3fc57651774baf464774cd7144384a9ffa9bad5c29b5f5334614ba5`；266 files、44 directories、9,546,943 bytes。剔除本轮额外 role/validator 文件后，候选子树仍精确等于 `7233…df37`。
- 封存脚本 SHA-256：
  - `bootstrap.sh`：`7ba10a198eecbd881fe1c8207d6b80af0dbb7afd4154bc76563209eb1fab56c3`；
  - `bootstrap.sql`：`c691743506c49316d6e4e644db94485e37ab2f271960bee3753f5e3cc6b7a671`；
  - `migrate-as-owner.sh`：`650268622c55410987dab9a1b148cb0bbd44143a0b962be769e6fb8bba9ee46d`；
  - `validate_database_roles.py`：`5a70010e5794e60240cd127420e370d754b8bb03959724f0e1f2c4d06723f19c`。

## 2. 数据不变基线

- dump 恢复后 head=`20260830_0035`，四个目标角色均不存在，public 表 113 张、合计 164,422 行。
- 逐表 `table_name|count(*)` 排序清单 SHA-256：`bd7b28e585d7fa841f44a8a211e3e09f6e2f0d072a2e655b35a59a99193c5688`。
- `0036` 两表恢复后不存在；升级后两表均为 0；回退后再次不存在。
- 上述清单摘要在候选启动后、完整回退后、旧包启动后和最终负测清理后均保持完全相等。
- 全过程只读取 schema、数量、状态和 hash；未把小说标题、正文、人物名、事实内容、来源摘录、媒体内容或真实 UUID 写入证据。

## 3. 角色、迁移与安装链

1. 错误 `validate-0035` step 调用 bootstrap：非零退出；四角色仍为 0、三个认证卷仍为空。
2. `bootstrap-20260830_0035`：PASS；重复运行后三个 pgpass 文件摘要不变。
3. `validate-20260830_0035`：PASS；65 张保护表、113 public relations、222 routines、3 个运行角色实际登录、owner/ACL/raw-DML/default-ACL 均通过，`production_role_switch=HOLD`。
4. `upgrade head`：被包装器在 DB 访问前拒绝，实际 head 保持 `0035`。
5. `upgrade 20260902_0037`：PASS；线性执行 `0035→0036→0037`，原 113 表摘要与总行数不变，新两表合计 0，账本索引存在且定义符合 revision。
6. `bootstrap-20260902_0037` 与 `validate-20260902_0037`：PASS；67 张保护表、115 public relations、222 routines，运行连接切换继续 HOLD。
7. 停止态候选安装：冻结容器 ID、不可变 image ID 和三个实际卷；`/plugin` copy-back 树校验、公开 QwenPaw CLI、已安装树 copy-back 校验全部 PASS，目标容器始终 stopped。
8. 隔离启动只读 GET：插件与 PawApp 均为 `0.4.0`、loaded/installed；health ready、数据库 connected；小说数量 10 且 ID 唯一。TTS 在本演练显式 disabled；未调用写方法。
9. 启动后原 113 表摘要不变；新两表合计 0；活动 background jobs、未完成 attempts、有效 resource locks 均为 0。

## 4. 完整回退与非零拒绝

- 停止隔离 QwenPaw 后，目标库其他会话为 0；新两表合计 0。
- `downgrade 20260830_0035`：PASS；两张 `0036` 表与 `0037` 索引不存在。
- 回退 bootstrap 与 65 表 validator：PASS；逐表计数摘要仍为 `bd7b…c5688`。
- 维护窗旧包归档：archive SHA-256 `d49b4a4fd7b1ef3ed0102c8948edd77358bf1e662be1900fccaf5efe2657af6d`；安全解包后树 SHA-256 `fe4d86a55a3e1990ad0b04657c85a5f9a93618d993de8c095dd9572a84faf8dd`、head `20260830_0035`、bundle SHA-256 `43b329ec9830166d598f50eb75a19cec7ef7cc52b86dbf648a903c3cc480bbcd`。
- 旧包停止态恢复、安装树校验、启动与只读 GET：PASS；数据库仍为 `0035`，bundle 与冻结值相等，最终计数与任务静默均不变。
- 额外非零负测：在隔离 `0037` 中插入一条固定合成 command 后，downgrade 被明确拒绝；事务回滚后 head=`0037`、合成 command=1、`0037` 索引=1，证明跨 revision 原子拒绝。删除该唯一合成记录后再 downgrade 成功，最终回到 `0035` 且计数摘要恢复为基线。

## 5. 无效观测与处置

- 首次逐表计数命令因 shell 引号错误没有执行查询，产生的“0 表”摘要已明确作废；随后以逐表标识校验命令重新取得 113 表基线，未发生数据库写入。
- 一次容器内 Python GET 探针遗漏 `docker exec -i`，实际未执行脚本且无输出；随后用 `-i` 重跑。
- 一次最终 JSON shell 匹配假定字段顺序错误而自行退出；validator 容器实际为 0，随后直接读取完整安全 JSON，确认 PASS。
- 这些失败均未被计入 PASS 证据，也未修改正式或克隆业务数据。

## 6. 清理

- 已按精确标签和名称删除 2 个测试容器、9 个测试卷和 1 个内部网络；未使用 `compose down`、`prune` 或模糊过滤删除。
- 合成管理员 pgpass 单文件已删除，父目录已 `rmdir`。
- 旧包临时解包副本移入 `/Users/liujia/.Trash/ai-novel-2026-dbr53r0902a-old-package`，可恢复；权威归档仍保留在长期备份目录。
- 正式 QwenPaw、正式 PostgreSQL、正式卷和长期备份均未在 W2 中停止、挂载或修改。

## 7. 裁决

W2 退出门禁满足，可以进入 W3 的长期停机前复核与权威恢复点。该裁决不等于运行时 API／worker 角色切换获批；两者继续保持 HOLD。
