# 计划 45 G0 基线

日期：2026-09-01（Asia/Shanghai）

- 施工并入已批准的计划 47，同一脏工作树中保留计划 44、46 与 TTS 候选，不做 reset、stash、清理或跨范围提交。
- Git 基线：`main@858e50f`；长期 PawApp、长期 PostgreSQL、长期媒体和真实小说均未写入。
- 产品修复范围只包含前端播放队列、播放器测试和独立 Chromium 频率探针；没有修改音频文件、Edition、媒体 API 或 TTS 模型参数。
- 唯一正式播放后端冻结为双 `HTMLAudioElement`；不增加 DSP 依赖，不保留 Web Audio 变速回退。

G0 裁决：`PASS_SOURCE_SCOPE`。长期部署、作者听检、Git 提交和推送不在本次授权内。
