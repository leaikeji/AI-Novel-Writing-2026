# 修改章纲弹窗设计 QA

日期：2026-08-24

## 对比目标

- 源视觉真相：`/Users/liujia/Documents/AI小说世界2026/docs/开发文档/证据/修改章纲弹窗对标-2026-08-24/reference.png`
- 源图像素：1220×1396，按 @2x 归一为 610×698 CSS 像素。
- 源图归一副本：`/Users/liujia/Documents/AI小说世界2026/docs/开发文档/证据/修改章纲弹窗对标-2026-08-24/reference-normalized.png`
- 实现全屏截图：`/Users/liujia/Documents/AI小说世界2026/docs/开发文档/证据/修改章纲弹窗对标-2026-08-24/02-final-full.png`
- 实现聚焦截图：`/Users/liujia/Documents/AI小说世界2026/docs/开发文档/证据/修改章纲弹窗对标-2026-08-24/02-final-dialog.png`
- 高度调节截图：`/Users/liujia/Documents/AI小说世界2026/docs/开发文档/证据/修改章纲弹窗对标-2026-08-24/03-resize-control-dialog.png`
- 实现截图像素：全屏 1799×1229；弹窗裁切 610×704。
- 浏览器 CSS 视口：1799×1229；页面 DPR 为 2，但截图接口返回 1× 像素，因此以 610px CSS 宽度裁切与 610px 归一源图比较。
- 状态：本地真实作品《潮声替我说晚安》第一章；“编辑章纲”弹窗打开；正文大纲真实载入；目标字数 2500。

## 全视图与聚焦对比

- 全视图证据：`02-final-full.png`，用于核对弹窗居中、遮罩、与正文编辑器的层级关系。
- 聚焦证据：`reference-normalized.png` 与 `02-final-dialog.png` 已在同一比较输入中打开，弹窗宽度、约 700px 高度、84px 橙色标题区、大文本区、目标字数区和底部双按钮的主要比例一致。

## 必查保真面

- 字体与排版：系统中文字体、20px 主标题、14px 章节副标题、16px 表单标签与正文行高均与参考层级一致；无异常换行或截断。
- 间距与布局：610px 弹窗、双栏等宽底部按钮、20px 主体内边距和 320px 大纲区均对齐；实现比源图高 6px，属于圆角和截图裁切的 P3 级差异。
- 颜色与令牌：橙色标题渐变、浅橙聚焦描边、灰色取消按钮和橙色保存按钮一致；遮罩与阴影层级正确。
- 图片与资产：本组件没有位图资产；编辑与关闭图标使用项目现有 Ant Design 图标库，没有占位符、手绘 SVG 或 CSS 图形替代。
- 文案与内容：固定文案“编辑章纲 / 章节大纲 / 目标字数 / 建议范围：500-10000字 / 取消 / 保存章纲”对齐；章节编号和目标字数来自真实作品数据，因此与参考示例的“第5章 / 2200”不同属于预期动态内容。

## 对比历史

1. 初始实现 `01-before-full.png`：
   - [P1] 标题区仅 61px，高度和信息层级不足，副标题错误使用章节名称而非“第几章”。
   - [P2] 弹窗宽 720px、大纲区仅 278px 高，目标字数输入只占 190px，底部按钮为后台表单式小按钮。
   - [P2] 字数说明混入验收流程文案，且范围为 2000–5000，与源视觉 500–10000 不一致。
2. 第一轮修复：改为 610px 宽、84px 橙色标题区、真实章节编号、320px 大纲区、全宽目标字数输入和等宽双按钮；保存接口保留未展示的角色约束等原有字段。
3. 中间对比发现：
   - [P2] 取消按钮仍被宿主默认按钮样式覆盖为白底；目标字数出现浏览器原生上下箭头。
4. 最终修复：取消按钮改为独立语义按钮并强制灰底；隐藏数字输入原生箭头；最终证据为 `02-final-dialog.png`。
5. 用户复查发现 [P2]：参考图右下角存在原生拖拽柄，而实现把 `resize` 设为 `none`，缺少章纲框上下高度调节。
6. 修复：恢复原生 `resize: vertical`，初始高度 320px、最小 180px、最大 520px；宽度保持 568px，不允许横向改变。浏览器计算样式为 `resize: vertical`、`overflow: auto`，右下角拖拽柄在 `03-resize-control-dialog.png` 中清晰可见。

## 交互验证

- 打开弹窗：通过。
- 修改后取消，再次打开仍为服务端原值：通过。
- 临时修改章纲及目标字数并保存，重新打开后持久化：通过。
- 恢复原章纲与原目标字数，再次打开确认：通过。
- 输入 400 时保存按钮禁用，恢复 2500 后重新启用：通过。
- 章纲框计算样式：`resize: vertical`；高度范围 180–520px；只允许上下调整：通过。
- 控制台 `error` / `warning`：0。
- 构建验证：TypeScript 通过；3 个测试文件、8 项测试通过；Vite 生产构建通过；容器健康状态为 `running healthy`。

## Findings

- 无可执行的 P0、P1 或 P2 差异；新增的高度调节交互已补齐。
- [P3] 源图与实现的总高相差约 6px；不影响信息密度、操作路径或视觉层级。

final result: passed
