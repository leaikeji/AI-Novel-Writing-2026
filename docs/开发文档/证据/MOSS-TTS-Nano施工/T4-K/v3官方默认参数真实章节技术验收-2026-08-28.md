# v3 官方默认参数真实章节技术验收

状态：`TECHNICAL_PASS / HUMAN_LISTENING_PASS / SAME_RUN_RESUME_PASS / TEARDOWN_PASS / PASS_CANDIDATE`

日期：2026-08-28（Asia/Shanghai）

## 当前结论

canonical run `bb03ccaf-4681-490a-b987-84bec9199b3b` 已完成 T4-K v3 的真实产品技术链。正式绑定与实际推理均为：

| 角色 | 官方 preset | 参数来源 |
| --- | --- | --- |
| 旁白 | `onnx.Zhiming` | 固定官方 ONNX manifest 的原始 prompt codes 与官方默认生成参数 |
| 沈川 | `onnx.Junhao` | 固定官方 ONNX manifest 的原始 prompt codes 与官方默认生成参数 |
| 林晚 | `onnx.Xiaoyu` | 固定官方 ONNX manifest 的原始 prompt codes 与官方默认生成参数 |

三项统一使用 `seed=1234`、`sample_mode=fixed`、`do_sample=true`、`max_new_frames=375`。没有项目自制音色，没有换 seed、greedy、音高、语速、情绪、克隆、跨 runtime 名称猜测或其他参数调教。

本记录最初只证明技术链通过；该历史中间态已经结束。作者随后对同一 run 的最终章节混合样本作出明确结论，固定 listening finalize、同 run resume 与 teardown 均已完成。公开 capability 是否可用仍以长期安装器的 product-mode verify 为准，不由本文单独推断。

作者随后再次确认“官方预设是完全没问题的，官方的预设音色就用官方预设的参数”，并在听完 canonical 完整章节后明确回复“完整章节通过”。固定 finalizer 已把六项听检全部记录为通过：三类声音可区分、音色稳定、无严重漏读／重复、普通话可理解、停顿／接缝自然、响度一致。listening record SHA-256 为 `70f005a209be1b75cde351e0352bb89654a29256b7c5004decc5fd7bfa2a3ec0`，receipt SHA-256 为 `1d8c69a23de5cdd41fed50f6c715836887b7992bbcfa938cea34cce6119a66e1`。同 run resume 返回 `PASS_CANDIDATE`，精确 teardown 返回 `TOOLS_CLEANED`；最终 result SHA-256 为 `afc70316e08a7ea8cc053a622c5733c27c48e18a0a605ce8eef557b65bdd49f5`。后续 tree `e40d6314…` 的失败句段重试、切章恢复和补充浏览器修复另见 [T4 补充浏览器与 0024 候选验证](./T4补充浏览器与0024候选验证-2026-08-28.md)，不得回写或重标本记录的历史技术字段。

## 同一 run 技术结果

- 最终 launcher：`PASS_CANDIDATE`；human state：`PASS`；`baseline_restored=true`；精确 teardown 已删除一次性 `tool`／`incoming` 与 recovery 文件，只保留 listening、result 和必要恢复证据。
- 自动链：`PASS`，0 blocker，2 个正式人物音色，3 个互异官方音色，1 个未命中缓存的真实 Nano job；Edition fingerprint `ce643aa5ebb56d7b966b5227dc50a222d72d610566e1ff4cf31376a6a0b51d8e`。
- 人工链：`PASS`，预期 3 blocker 已按固定审核动作解决，3 个互异官方音色，1 个未命中缓存的真实 Nano job；Edition fingerprint `5f3414a75ca7235db436d4d76b9439eab6b195d78167e3958d21dc4eea8d75ae`。
- 黑盒 RTF：`0.033856`；request-to-ready：`8.031 s`；TTFA：`8031 ms`；章节音频总时长：`237.2 s`。
- 稳定性：固定 `1800.0 s`；Sidecar peak memory `1,782,411,428 B`；首／尾窗口中位数均为 `1,769,526,526 B`，growth `0 B`，允许上限 `134,217,728 B`；restart `0`，health failure `0`，`qwenpaw_slowdown_observed=false`。
- 浏览器：`1920×1080`、`2560×1440` × 助手收起／展开四组合全部通过；console error `0`，overlap `0`。
- 交互：播放器、CodeMirror 跟随、段落／光标跳播、latest-wins、play/pause、倍速、进度、编辑恢复、pending-gap 均通过；编辑动作产生 TTS 写入 `0`。
- 媒体：Range/ETag 状态覆盖 `200`、`206`、`304`、`416`。
- whole-host telemetry 如实保留：pageout delta `3313`、swapout delta `0`；按冻结的 result/probe 2.2 规则不把全机 pageout 单独误判为 Sidecar 内存泄漏。
- 采集时间：`2026-08-28T05:12:19Z`。

## 追加式运行历史

以下运行均保持原结论，未重标为成功：

| run | 原始结果 | 恢复 |
| --- | --- | --- |
| `4c9d8334-452f-4ec4-9403-5e75abdd1601` | 启动阶段上下文请求出现暂态 404 | baseline 已恢复 |
| `5a6fc9f7-7395-4d6c-ad8f-8b854b3200e1` | 浏览器段落跳播 HOLD | baseline 已恢复 |
| `a47ee1fc-ec61-4082-bd06-057c4cc4f965` | 浏览器段落跳播 HOLD | baseline 已恢复 |
| `cf4cbc59-0403-4c51-9c42-4defb4e5973c` | 旧 Sidecar 累计 restart 计数触发稳定性 HOLD | baseline 已恢复 |
| `77c05106-9bbf-41e4-8a81-a6bf9b705f58` | 同一累计 restart 计数触发稳定性 HOLD | baseline 已恢复 |

Sidecar 后续仅重建现有服务实例以清零历史累计 restart 计数；没有新增长期容器、第二数据库或数据卷，也没有删除任何持久卷。最终拓扑仍只有 PostgreSQL、QwenPaw、MOSS-TTS Sidecar 三个长期容器。

## 人工听检材料与完成结论

最初提供的前 18 句／69.6 秒样本（WAV SHA-256 `b0c2fec8db716ce2344412f60315684bcbd33c2f20de0ef4c0b3ed91ee0f5f32`）只用于尽早发现明显问题，不足以代表整章。随后已生成覆盖同一 run 人工 Edition 全部 58/58 个 ordinal 的完整仓库外试听文件：44 个唯一播放资产按 Manifest 重复关系实际播放为 58 句，正式音频 `237200 ms`，另精确插入 Manifest 的 `21120 ms` 句间间隔，总时长 `258.32 s`；WAV SHA-256 为 `6f1b6ef5b34a169efd05b98f17f17efbc4d0c0259141994ffa9b61819dfc0149`，大小 `49,597,484 B`，目录 `0700`、文件 `0600`、单硬链接。每个媒体响应均经实际 SHA-256 校验后使用 Sidecar 固定 FFmpeg 从 stdin 解码为 48 kHz／双声道／16-bit PCM；未向 Sidecar 写文件，没有再次推理、换参或后处理调音。该文件是一次性人工验收衍生物，不是产品音频导出能力，也不进入 Git。

作者已听完整章并实际确认“完整章节通过”。六项检查均由作者明确确认：三类声音可区分且稳定、没有严重漏读／重复、全部样本均为可理解中文、没有异常停顿／爆音／吞字／长空白、相邻句段响度大致一致。固定 finalizer、同 run resume 和 teardown 已在原 sealed run 上完成；未延长或伪造 listening 窗口，也未重跑 Nano、替换音色或修改官方参数。

## 子代理并行施工设计

本记录是既有 T4-K 计划的串行集成与证据收口，不新增开发工作包。技术运行、最终人工听检、同 run resume、teardown 和 T4-GATE 共享同一 canonical run／recovery 状态，标记为 `SER/GATE/MUTEX`，不得并行改写。既有子代理只完成已冻结接口下互不重叠的 executor、fixture/resume 与稳定性审计工作；最终状态判定、文档汇合、Git 暂存／提交／推送仍由主代理唯一负责。
