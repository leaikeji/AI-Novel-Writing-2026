# V3 第二组预登记哨兵策略

状态：冻结候选；只处理 V2 已登记的 SP-02 第二次独立尝试，不重试或覆盖 X01/X14。

- V2 的 X01/A 已分别因共享 QwenPaw 重建和完整公开流达到 600 秒上限而失败；两次失败证据保持不可变，X14/B 均未启动。
- V3 只把原16样本矩阵中预登记的 SP-02 attempt 2 设为哨兵：先 X11/A，成功后才允许 X05/B。它们与 V2 使用同一题面、同一最终输出要求、同一基线/候选 Skill 哈希和同一600秒服务端上限。
- A 侧安装基线 `prose-writing`，SHA-256 为 `cbf113c0a2b71cda1f54ca029d98ee9263323c21db75cd76539bffb0867d72e2`；B 侧安装候选，SHA-256 为 `1139c7bea46a7781c17ba55fb8543ec2d5f6aa42a65f1f4f960354d1c76317a2`。
- A/B 不使用提示覆盖层。同一 case、同一 attempt 的 prompt 必须逐字节相同；Skill 包哈希错配必须在模型调用前拒绝。
- 流诊断升级为 `writing-eval-stream-diagnostics-v2`，只记录结构计数、文本值长度计数、末事件类型、assistant 消息数和公开 usage 封套数；不得记录、摘要或回传任何诊断正文。
- 每侧只调用一次，不自动重试，不延长超时，不保存未完成流。X11 任一失败即停止，不启动 X05；X05 失败则整个 V3 哨兵失败。
- X11/X05 都生成完成且通过确定性纯净度后，才允许人工复核锚点、知识边界、资源来源、状态变化和停止条件；通过前不得进入完整16样本。
- `scene-craft` 候选 SHA-256 为 `49832573e8316d918b99ec2cb4710a47ca2481b7f95ded4fbf2c83836e623a34`，但本哨兵只显式调用 `prose-writing`，不把 `scene-craft` 质量写成已验证。

V3 的目的只是以预先登记的第二次独立尝试恢复可比哨兵，不把失败后的临时改题、重复抽样、延长等待或单跑候选伪装成 A/B 证据。
