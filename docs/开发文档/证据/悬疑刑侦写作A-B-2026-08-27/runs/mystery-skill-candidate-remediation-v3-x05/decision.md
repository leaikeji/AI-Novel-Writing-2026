# X05 候选缺陷修复检查

状态：用户于2026-08-28批准减重执行；这不是 V3 A/B 哨兵续跑，也不修改 V3 冻结策略或历史证据。

- 已有对照：X11/A 原始输出 SHA-256 `6e52ac37faa9d89bda3aa468eda6de596c502b7bb9f40918e19c746aa56d6ce7`，完整生成但 `agent_status_capsule=true`。
- 本次目标：只调用预登记 SP-02 attempt 2 的 X05/B 一次，核对候选 `prose-writing` 是否消除状态胶囊并保持篇幅、四锚点和完整结束。
- 证据身份：继续使用已冻结 `mystery-skill-ab-20260828-v3` API/提示/Skill 哈希合同，但以独立 run id 保存，结果只解释为候选缺陷修复检查。
- 停止规则：不自动重试；超时、Skill 哈希错配、模型漂移、输出污染或恢复失败均停止。
- 数据边界：研究路由不连接小说数据库；不创建 PostgreSQL dump，不写小说正文、revision、候选或故事事实。
- 结论边界：X05 通过最多支持“状态胶囊缺陷在本样本得到改善”，不支持“总体写作能力已提升”；后续正文质量采用四场景各一对的小规模评测。

## 实际结论

X05/B 在314.754秒后完整结束，733个非空白字符和四个字符串锚点通过，但原始输出仍附加独立 `⟦…⟧` 状态胶囊。运行器以 `output_purity_failed` 保存失败，不生成盲评文件、不重试。候选缺陷没有得到修复，因此不启动后续四场景评测，也不声称候选 Skill 的写作能力已经提升。
