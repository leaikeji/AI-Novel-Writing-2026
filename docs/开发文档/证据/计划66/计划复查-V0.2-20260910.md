# 计划 66 V0.2 文档复查

状态：文档修订与静态复查完成；研究未启动。

日期：2026-09-10（Asia/Shanghai）

对象：[计划 66 V0.2](../../66-种田文十五部样本扫榜拆解与写作能力闭环计划.md)。本记录评审计划的可执行性与合同一致性，不证明十五部样本可得、正文可处理、模型调用已成功或写作能力已提高。

## 本次范围

用户要求“优化方案，然后再复查一遍”。本轮主代理串行修订计划、两处导航与本记录；未启动子代理研究任务、真实扫榜、正文读取／导入、模型实验或正式 Skill 修改。外部研究库的文件只作方法与合同核对。

## 复查结果

| 原问题／新增核查 | V0.2 处理 | 结论边界 |
| --- | --- | --- |
| opening 四项 yes 淘汰 | L1 可 unknown，L2 冻结阶段类型；经营质量指标不再作为入选必要条件 | 减少慢热误杀与预先筛除反例，仍需真实试点校准 |
| GF 强弱比较混杂 | 频道、状态、角色、发布时间／字数匹配；每格一书只作描述性对照 | 不声称金手指的独立效应 |
| 改类／换样没有闭合规则 | 三阶段 GF 判定、先重分配、再限额替换、保留排除台账 | 缺格可交付部分研究，不能冒充十五部完成 |
| 模型预算不明 | 五书试点、逐运行字符／分块预算、两次尝试、累计调用上限与耗时限制 | 具体运行值在有章节索引后填写，当前未授权或发生消耗 |
| 分块提示遗漏专项问题 | 核对到完整任务包包含 research_questions，批量提示却省略；计划选择完整 next/record/fail 路径 | 未修批量脚本，也不假称该脚本已符合专项取证要求 |
| 非法目标 Skill ID | 知识包与跨书候选限九 ID；golden-finger-writing 留在下游裁决说明 | 只有写进受限机器字段才会失败，纠正此前“必然失败”的过宽表述 |
| 深浅知识包不可比 | 十五部 L2 基线包与五部结构锚点增量包分开 | L3/L4 后见信息不反写 L2 |
| 全局等待与授权空档 | 逐书、逐簇 ready set；后续启动覆盖 W0—W2 | 在已明确授权范围内持续推进，不重复索要同一许可 |
| 评测顺序不明确 | 本题材作者金标准→独立评审校准→六题 A→开发 B→九题确认 | 旧悬疑八样本不是种田研究的前置欠账 |
| 评测无效与候选失败混淆 | rejected、revise、inconclusive 分开；不改旧知识包枚举 | 无候选或增益不明均可如实交付 |
| 并行所有权不完整 | 明确共享文件、spec、运行、初始化／指针、staging、版本化对照和实验目录 | 派发前将占位符解析为绝对路径；同文件唯一写者 |

## 证据依据与检查

- 现行 `novel-deconstruction/0.6.2` 的 `research-products.md`、`deconstruction-modes.md` 及研究库 `knowledge_package.py`：核对范围语义、九个目标 Skill、知识包 staging／发布路径。
- `run_deconstruction.py:build_packet`、`qwenpaw_batch.py:model_prompt/execute`、`qwenpaw_aggregate.py`：核对研究问题在不同提示阶段的传递，以及部分失败发生在 dispatch 保存之前的现有行为。
- 研究库 `08-写作评测/v3.2/悬疑分类能力评测修复结论-v3.2.0.md`：核对旧作者金标准和防地板规则的原实验边界；新题材门槛是本计划明确选择的新设计，未复用旧完成状态。
- 结构化静态检查：十五格无重复、九目标 ID 与校验器集合一致、四组运行配置的 profile／辅助维度合法、主计划与复核记录链接存在、Markdown 围栏与表格完整。
- 空白检查分别覆盖 tracked diff、未跟踪计划及新复核文件；复核索引只改计划 66 条目。现有用户工程改动保留。

文档复查未发现仍需阻止方案交付的内部冲突。后续真实执行仍须核实候选与处理范围、填入实际模型／字符／调用／耗时预算、完成取证试点和本题材评审校准；这些均已定义为对应阶段的输入和门禁，不是本轮文档已完成的验收。
