# E2E37-G0：写作基线与共享锁冻结

日期：2026-08-29（Asia/Shanghai）

状态：`PARTIAL_READY`。非 TTS 写作基线通过；其他任务仍占用共享文件与迁移所有权，因此计划 37 只进入审计、fixture 和独立测试目录施工。

## 已核实事实

- 主工作区分支：`main`。
- 基线提交：`94f6b6644df363234969f0e6882e1b8c3fb1229e`（计划 36）。
- 当前 Alembic 唯一 head：`20260829_0033`。
- TTS 工作位于独立 worktree `/Users/liujia/Documents/AI小说世界2026-tts35-core`，但仍在修改 `backend/models.py` 等共享文件，迁移和共享后端锁尚未释放。
- 主工作区现存 `docs/README.md`、`docs/开发文档/README.md`、`pyproject.toml` 及计划 35 文档／证据改动；计划 37 不覆盖、不暂存、不清理这些文件。
- 长期 PawApp `/api/ai-novel-world-2026/health` 返回 `status=ready`、数据库已连接、AI 候选生成与向量检索启用。
- health 中 TTS production 的 `TTS_DATABASE_SCHEMA_OUTDATED` 属于计划 35 范围，不进入计划 37 验收或裁决。

## 已执行基线

```text
.venv/bin/python -m pytest --ignore=tests/narration
763 passed, 35 skipped

vitest run --exclude frontend/src/narration/**
54 files passed, 398 tests passed

.venv/bin/python -m alembic heads
20260829_0033 (head)
```

专项写作测试 `tests/model_execution`、`tests/context_v4`、`tests/character_workspace`、`tests/embedding` 同样通过。

## 门禁裁决

- `E2E37-AUDIT`：允许。
- `E2E37-FIXTURE`：允许。
- `E2E37-MATRIX`：允许，但只能写 `tests/writing_e2e/**`。
- 修改 ORM、迁移、共享 API、共享工作台、长期数据库：暂不允许。
- TTS 功能、接口、浏览器、声音和朗读验收：明确不执行。
- 真实正文模型或百炼调用：未取得本次调用授权，保持关闭。

共享锁释放后必须重新记录 Git 提交、单一 migration head 和非 TTS 写作回归，才能进入 `E2E37-INT`。
