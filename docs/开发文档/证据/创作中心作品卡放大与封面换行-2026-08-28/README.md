# 创作中心作品卡放大与封面换行验收证据

日期：2026-08-28
页面：创作中心（`?novel_center=1`）

## 变更结果

- 当前作品卡从固定 600px 放大为最高 760px，并随可用宽度收缩。
- 主封面由 112×150 放大为 136×181，作品标题同步提升到 23px。
- 作品切换区改为自动换行布局；每个作品封面及“新建”入口最终固定为 88×117，不再参与 flex 压缩。
- 缩略图先组成固定列宽的响应式网格，列组作为整体居中；每一行仍从网格第一列开始排列。当前 9 本作品加 1 个“新建”入口在宽屏和 1366×900 窗口均排成两行。

## 证据

- `01-修复前-1947x1236.png`：同尺寸修复前运行态，作品卡宽 600px；10 个入口被压缩到约 49px，并全部塞在一行。
- `02-修复后-1947x1236.png`：同尺寸修复后运行态，作品卡宽 760px；入口保持 72×96，并自动换行。
- `03-修复后-1366x900.png`：较窄窗口复查，作品卡随可用空间收缩到约 661px，入口仍保持 72×96 并换行。
- `04-修复前后对照.png`：同视口全页前后对照。
- `05-作品区域聚焦对照.png`：作品卡与作品切换区聚焦对照。
- `06-受限宽度调整前-1267x713.png`：受限主工作区下，72×96 缩略图仍采用行内居中的调整前状态。
- `07-缩略封面放大并左对齐-1947x1236.png`：中间方案，缩略图为 88×117，但整组仍贴着容器左边缘。
- `08-缩略封面左对齐-1366x900.png`：中间方案的较窄窗口状态。
- `09-缩略封面放大左对齐前后对照.png`：最终调整的同视口全页对照。
- `10-缩略封面区域聚焦对照.png`：缩略图尺寸和逐行对齐方式的聚焦对照。
- `15-整组左贴边-1866x1236.png` 与 `16-居中网格最终-1866x1236.png`：同视口、同作品顺序的最终调整前后证据。
- `17-同状态左贴边与居中网格对照.png`：最终调整的同状态全页对照。
- `18-同状态居中网格聚焦对照.png`：最终列组居中、行内左对齐的聚焦对照。
- `19-居中网格最终-1366x900.png`：最终较窄窗口状态。

## 自动化与运行态验证

- `pnpm typecheck`：通过。
- `pnpm vitest run frontend/src/assistant-layout.test.ts frontend/src/creative-center-shell.test.ts`：2 个测试文件、8 个测试通过。
- `pnpm build`：通过。
- `.venv/bin/python scripts/package_plugin.py`：通过。
- 本地 QwenPaw 热安装：成功。
- 1866×1236 实测：7 列网格在 760px 容器内左右留白均约 24px；两行起点一致，入口统一 88×117，无页面横向溢出。
- 1366×900 实测：6 列网格在可用容器内左右留白均约 26px；两行起点一致，入口统一 88×117，无页面横向溢出。
- 页面控制台：无 error。
