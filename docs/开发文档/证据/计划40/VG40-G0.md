# VG40-G0 基线冻结记录

状态：**2026-08-30 已完成基线冻结、计划 35 集成回归和长期部署；尚未下载 VoiceGenerator。**

## Git 与迁移基线

- 当前主线基线：`940fdb17cf009745bb98ed374b20354d588ff53b`。
- 计划 35 候选提交：`8046b9958c14a05d62b292ae4fa4fd91763a7efe`，分支 `codex/tts35-core`。
- 两者共同基础包含计划 36 稳定提交 `94f6b6644df363234969f0e6882e1b8c3fb1229e`。
- 本次施工工作树：`/Users/liujia/Documents/AI小说世界2026-vg40`，分支 `codex/vg40-implementation`。
- 计划 35 代码与当前主线自动汇合成功；仅三个开发文档/索引发生内容冲突，无后端、迁移或前端源码冲突。
- 当前长期数据库 head：`20260829_0033`；计划 35 的下一迁移为线性 `20260829_0034`。

## 长期运行态基线

- QwenPaw、Nano Sidecar 与 PostgreSQL 容器当前健康。
- 技术朗读运行时可探测，Nano 采用按需加载。
- 生产朗读生命周期因长期数据库尚未应用 `0034` 而关闭，错误为 `TTS_DATABASE_SCHEMA_OUTDATED`。
- 在隔离回归、备份和迁移门禁通过前，不修改长期数据库、容器或用户小说。

## 主机资源基线

- 主机：Apple M4，物理内存 `17,179,869,184` bytes（16 GiB）。
- 当前数据盘可用空间约 45 GiB，真正下载前必须再次测量。
- Docker Desktop VM 约 7.75 GiB，不作为 VoiceGenerator 承载环境。
- VoiceGenerator 必须使用一次性 macOS 原生进程、重模型互斥和系统级 watchdog。

## 集成与长期部署结果

- Python 全量：`3304 passed, 145 skipped`。
- 前端全量：`112` 个文件、`969 passed`；typecheck 与 build 通过。
- manifest／Skill／QwenPaw 公开契约：`127 passed`。
- 一次性 PostgreSQL：fresh → `0034` → `0033` → `0034` 通过；Nano 实验 `13 passed, 1 skipped`，私人删除 `19 passed`。
- 长期备份：`/Users/liujia/Documents/AI小说世界2026-backups/plan40-20260830-core-deploy`，包含数据库、原安装插件、实际媒体副本、迁移头和 1037 条 SHA-256 清单。
- 长期数据库已线性升级到 `20260829_0034`。
- 合并候选已通过 QwenPaw 公开热安装路径安装；三容器均 healthy，PawApp health ready。
- 18 个官方 preset 的目录契约为 `moss-tts-official-preset-catalog/2.0`，全部可见。
- narration technical／production 均为 ready，worker 已运行，Nano 保持按需未加载。
- 首次恢复曾因仍使用旧 Sidecar 镜像而触发 `SIDECAR_LIFECYCLE_FAILED`；未写数据。重建当前候选 Sidecar 并按 Sidecar → PawApp 顺序恢复后通过完整 verifier。
- 当前 bundle SHA-256：`86b8ea39bdec6e1fa4ce1f0cd993540f64cd3a7661a8338ce51483d2bf2e8e63`。
- 当前插件目录清单摘要：`dc3a994dccd13bfc48d5899a32673a0a933f57e7eaa68bfa1b4ef8963ba8f156`。
- 当前 Sidecar image digest：`sha256:a3b63657b2c74a4862448cfc6e798c52d1ea74d92e5e2a8910dd6b545054ce4e`。

## 当前门禁裁决

- `PLAN35-INTEGRATION=PASS`
- `PLAN35-DEPLOY=PASS`
- `VG40-T0=PASS`
- `VG40-T1=PASS`
- `VG16-DOWNLOAD=NOT_STARTED`
- `VG16-FEASIBILITY=UNMEASURED`
- `VG16-PRODUCT=LOCKED`

T1 最初因 4 GiB 前置门禁正确拒绝；用户随后明确授权仅对无权重探针采用低内存例外。固定六项 MPS/BF16 操作已全部通过，5% MPS allocator、swap 零增长及 60 秒回收均满足窄门禁，`T1-MPS-COMPAT=PASS`。详见 `VG40-T1.md`。本记录仍不证明 VoiceGenerator 权重已能在 16 GiB 主机安全加载或生成。
