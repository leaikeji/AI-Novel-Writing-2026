# VG40 T3–T6 真实生成、稳定性与 Nano 二次验证证据

日期：2026-08-30

裁决：`VG40-SPIKE=PASS_LOCAL_RISK_ACCEPTED`

边界：只适用于当前 Apple M4／16 GiB 主机、固定模型 revision、MPS/BF16 串行拓扑和用户明确接受低余量风险的本地运行。

## 1. 固定身份

- VoiceGenerator revision：`97521ec2b6f3ec5026ac1f5751f8fc302d82c2d4`
- VoiceGenerator 权重 SHA-256：`dbe345257ff9f6cc84195bed830a268b39d5e0b728ff3ba90e715150a49b16d4`
- Audio Tokenizer revision：`3cd226ba2947efa357ef453bcad111b6eafba782`
- 输入 fixture digest：`155b477c14f6ddcbe54a91d8ff7354660a26e172feedcbfe087eede2689ac5f8`
- 生成适配器：`vg40-mps-generation-adapter/2`
- codec 适配器：`vg40-mps-codec-adapter/1`
- 文本控制 token：greedy；声音采样仍使用官方推荐的 `audio_temperature=1.5`、`audio_top_p=0.6`、`audio_top_k=50`、`audio_repetition_penalty=1.1`。

适配器 v2 只在官方 `audio_end` 出现、16 个 codec 通道全部冲刷完成后停止，不把达到 token 上限的截断音频当成功。聊天层 `im_end` 单独记录，不是声音资产完成条件。

## 2. T3／T4 严格机器有效样音

首条严格通过样音使用 seed `104729`：

- token shape：`[50, 16]`
- VoiceGenerator artifact SHA-256：`72765d0be3b7273aeadf833bbec8caa193526b83a61a27d07a47a2ad88309548`
- WAV：4.00 秒、48 kHz、双声道、PCM16
- WAV SHA-256：`87e7531901fbc239810357c3afbc3752f8aa179db15dedae8933a2debf5fd370`
- RMS：`-26.0905 dBFS`；clipped fraction：`0`；non-finite：`0`
- generator 与 codec 分属两个进程，前者退出并通过 60 秒回收后才启动后者；两阶段 MPS allocation 退出后均为 `0`。

先前 50-token 诊断样音为 2.96 秒，保持 `FAIL` 原始证据，没有补静音、覆盖或改写为 PASS。

## 3. T5 三次统一契约冷运行

| seed | token shape | 生成秒数 | WAV 秒数 | WAV SHA-256 | 最低 T3 余量 | 最低 T4 余量 | 结果 |
| --- | --- | ---: | ---: | --- | ---: | ---: | --- |
| `104729` | `[42,16]` | `9.176` | `3.36` | `cabf7026f5d3a644b93e549f7a88926a684e7982c20af7d5ee2312debfc708be` | `3,117,006,848` | `3,500,802,048` | PASS |
| `130363` | `[48,16]` | `10.171` | `3.84` | `d32772aca4e96bb71b3d887be4b14b0526356ec7707a614289c163054bb6f914` | `3,125,313,536` | `3,367,649,280` | PASS |
| `155921` | `[41,16]` | `9.365` | `3.28` | `982dfdc070766c513cb02623e0db83bc974d4f4d1336231e096d1f8197c7d68b` | `3,129,491,456` | `3,705,405,440` | PASS |

三条 WAV hash 互不相同，均为 48 kHz 双声道 PCM16，时长 3–5 秒，机器波形检查通过，无 non-finite，clipped fraction 不超过 `0.001`。三次每个阶段均无持续 critical pressure，退出后 child absent 且 60 秒恢复通过。

seed `155921` 在旧的随机文本控制通道中两次无法在 128／256 step 内形成完整协议结束，均正确失败且未发布 artifact。失败促成文本控制通道 greedy 化；三条最终 T5 结果均在同一最终参数和适配器 v2 下重新冷运行，旧失败目录保持不可变诊断证据。

## 4. T5 取消与崩溃恢复

- 加载期作者取消：watchdog `cancelled/user_cancelled`，子进程 `SIGTERM(-15)`；无 token、WAV、临时资产，60 秒恢复通过。
- 模型完整加载到 MPS 后注入 `SIGKILL(-9)`：watchdog `child_failed`，最低观测余量 `1,938,735,104` bytes、swap 增量 `509,670,851` bytes、持续 critical `0` 秒；无 token、WAV、临时资产，60 秒恢复通过。
- 故障运行后不存在 `t2_load_probe`、`t3_generate_probe`、`t4_decode_probe` 或 `t5_crash_probe` 残留进程。

## 5. T6 Nano 二次验证

来源是最终 T5 seed `104729` 的 3.36 秒 VoiceGenerator WAV：

- source SHA-256：`cabf7026f5d3a644b93e549f7a88926a684e7982c20af7d5ee2312debfc708be`
- Nano requested/actual fingerprint：`3c76f3e9e1381699c5555287cf66eeb023632d0c3ee94adc6d8ae1b1d455fd7d`
- Nano 输出：4.32 秒、48 kHz、双声道、PCM16
- Nano 输出 SHA-256：`7c65b0072c64be208d60b4d634292de011f2bfba074bb4fe2bb16c9f6dfd356e`
- validation text digest：`d36b347401fb0b9a7670f46247de1afeebb99abee7a1551b5d29de723994b3d4`
- 数据库写入：`0`；Nano 输出音频发布：`false`

验证前 VoiceGenerator 和 codec 已完全退出；PawApp 暂停并释放 Sidecar 唯一租约。T6 客户端通过正式 `moss-tts-sidecar/1.1` acquire／renew／warmup／multipart synthesize／release 协议运行，退出后 Sidecar 回到 unloaded，随后长期 PawApp 恢复 `ready`，lease generation 为 `5`。

## 6. 清理说明

Sidecar `/tmp` 参考 WAV 已删除，长期 PostgreSQL、Sidecar、PawApp 均已恢复。Docker Desktop 当时拒绝删除一个从未启动的临时容器 `vg40-t6-client`，因此其临时镜像 `vg40-qwenpaw-helper:temporary` 也暂时保留；两者不运行、不持有模型或网络租约，待 Docker Desktop 自身恢复容器层删除能力后再清理。没有为清理它们而重启 Docker Desktop，以免打断用户其他运行容器。

## 7. 阶段裁决

- `VG16-FEASIBILITY=PASS`
- `VG16-SAFE=PASS_LOCAL_RISK_ACCEPTED`
- `VG16-QUALITY=MACHINE_PASS_AUTHOR_LISTEN_HOLD`
- `VG16-PERF=PASS`
- `VG40-SPIKE=PASS_LOCAL_RISK_ACCEPTED`
- `VG16-PRODUCT=NOT_IMPLEMENTED`
- `VG16-FINAL=HOLD_MEASUREMENT`

该证据批准进入 `0035`、正式运行服务和人物卡产品闭环施工；尚不允许表述为“人物专属音色产品已全部可用”。

> 后续状态（2026-08-31）：本文件保留 T3–T6 阶段退出时的历史裁决，不覆盖原始尖峰事实。`0035`、T7、正式人物绑定、四章 Edition 和浏览器已在后续波次完成；当前总裁决以 `VG40-FINAL.md` 为准。
