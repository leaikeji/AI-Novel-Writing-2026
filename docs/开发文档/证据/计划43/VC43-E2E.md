# VC43-E2E — 真实 QwenPaw 浏览器验收

状态：**PASS**

日期：2026-08-31（Asia/Shanghai）

## 隔离环境

- 一次性 QwenPaw：`127.0.0.1:18089`；一次性 PostgreSQL：`127.0.0.1:15439`。
- 只使用合成小说和合成草稿，不与长期 QwenPaw 共享 data、secret、backup 或 media 卷。

## 用户主问题

- 零分卷时“新建章节”为 disabled，`title="请先创建分卷，再新建章节"`，空态同步提示；强制点击仍不打开对话框，草稿计数前后均为 0。
- 新增空名分卷时显示只读 `第1卷`，数据库名称为空。
- 章节向导显示只读 `第1章`；输入“第99章 · 潮声”立即规范化为“潮声”，确认与列表显示“第1章 潮声”，数据库只保存 `title=潮声`。
- 390×844 窄屏下 `clientWidth=scrollWidth=bodyScrollWidth=386`，无水平溢出；本路径未观察到 console warning/error。

## 失效草稿补充复验

- 合成小说：`9ecf2dc7-8b43-4067-8c61-c4d2ed4f7d8e`；草稿：`00ef2443-91c5-4eb9-82a8-1a36b31bf213`。
- 在 UI 中打开向导并填入“浏览器复验保留标记：潮声仍在”，然后在隔离 API 中删除原分卷并把既有章节移入新卷。
- 刷新并再开“新建章节”后，向导显示“原章节草稿已恢复，并已重新绑定到当前有效分卷；已填写内容均已保留”，且文本框仍显示上述标记。
- 数据库证明仍是同一草稿 ID，version `3→4`、step 保持 4、`volume_id` 更新为新卷；小说 Document 数仍为 1，未重复新建章节。

## 长期只读冒烟

- `18088` health 200，原生 `/chat` 200，插件仍 loaded/enabled。
- 只读打开现有《潮汐盲区》，可见卷章派生标题；未新建、修改或删除真实数据。
