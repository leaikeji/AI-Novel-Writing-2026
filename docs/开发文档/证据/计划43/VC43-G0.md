# VC43-G0 — 契约与施工基线

状态：**PASS，V0.3 施工已获用户明确授权。**

日期：2026-08-31（Asia/Shanghai）

## 授权与范围

- 用户明确指令：“修复问题”。该指令批准 43 号计划 V0.3 的卷章自动编号、无卷创建守卫及第三轮复查缺口施工，不要求固定批准句式。
- 不授权真实小说数据写入、QwenPaw 上游修改、Git 提交／推送或绕过隔离环境的浏览器写入。
- 当前工作区已有 42 号关系网投影、朗读、人物卡等未提交改动；本计划只在分配文件的必要行块中增量施工，不重置、不覆盖、不清理。

## 冻结基线

- `HEAD`: `60bb9d9e5a201e604e4d77ee19a596935aaa73ff`
- `backend/ + frontend/src/ + tests/` 已跟踪工作区 diff SHA-256：`0e9232070c7fca28027773010d7ca8c67a76dbd0d86623e65f8957a0a1f8c236`
- 既有未跟踪文件：
  - `backend/relationship_contracts.py`: `1d12d17cfbd90410a8021120746aa88e2fe2249e9899f89c2b056a4d2925514a`
  - `frontend/src/chapter-creation-policy.ts`: `f3a3820491a63d45d41006bd769c15f53c4144896bd21270dbc8326663424994`
  - `frontend/src/chapter-creation-policy.test.ts`: `9771cb02079cec80e93bd57b8429ba00856a529cfab569d9d74f28d3924e1194`
- 冻结审计初筛共 120 个 raw title／局部编号候选调用点；初筛输出哈希：`e710b67c4702c7e986a5eb4a78f003141cd29817bda40f6ceae3b993377dadad`。RO-AUDIT 负责分类，不直接修改代码。

## 冻结接口

- 规范树：真实分卷按 `Volume.position`，卷内章节按 `Document.position`，未分卷最后。
- 存储只保存语义名称；展示序号派生。
- 新章节必须显式绑定当前小说有效分卷；历史未分卷能力保留。
- 章节准备请求使用单调 `requestGeneration`；过期响应不得落状态。
- 卷章写动作按小说级固定锁序串行化；同一草稿 key 并发首次创建收敛。
- 纯 ordinal 变化不得远程重嵌入，但语义索引 narrative metadata 必须同步更新；名称刷新失败不得回滚作者名称。

## G0 退出判断

- V0.3 已冻结第三轮八项缺口及精确工作包。
- 源码写包须等待 RO-AUDIT 返回完整分类清单。
- 任一代理发现必须迁移 schema、修改 QwenPaw 核心或覆盖未分配脏改动时立即停止并交回主代理。
