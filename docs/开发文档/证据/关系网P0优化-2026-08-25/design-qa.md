# 角色关系网 P0 设计与交互 QA

日期：2026-08-25
验收小说：《回到1988，坐在你身边》
验收范围：关系图自动生成来源、ForceAtlas2 拖拽联动、布局保存/撤销/缩放，以及与妙笔神书关系图的视觉与行为基线对照。

## 源视觉真相

- 妙笔神书参考图：`../关系网交互审计-2026-08-24/04-miaobi-graph-before-drag.png`。
- 源图像素：1265×712；源截图未携带可复核的 CSS 视口和 DPR 元数据，因此本轮按完整关系图区域做归一化比较。
- 行为基线来自同目录 `audit.md`：妙笔神书使用 `vis-network 10.0.2`、`forceAtlas2Based`，拖动期间不隐藏节点/连线，并保留相邻边高亮和物理收敛。
- 本项目保留 QwenPaw 宿主、自动同步状态、筛选、布局操作和无障碍关系列表；这些属于产品功能差异，不冒充妙笔神书站点壳层。

## 最终实现证据

- 完整页面状态：`17-final-saved-accepted.png`。
- 聚焦后的完整关系图：`18-final-saved-full-graph.png`。
- 完整页面同图比较：`19-final-source-vs-implementation.png`。
- 关系图聚焦同图比较：`20-final-graph-focused-comparison.png`。
- 实现 CSS 视口：1280×720；浏览器运行时 DPR：2；浏览器截图输出：1280×720 CSS 像素。
- 最终状态：6 个有效角色、6 条有效关系（5 条 AI、1 条人工）、保存布局版本 2、缩放 1.12、6 个节点位置。

## 视觉与交互结果

- 图谱使用与参考产品同一家族的 `vis-network 10.1.2`；无需更换插件。
- 全局物理保持可再次启动；拖动开始时启动 ForceAtlas2，拖动结束后保留 650ms 邻接联动与自然收敛，再停止空闲模拟。
- `hideEdgesOnDrag=false`、`hideNodesOnDrag=false`、`hoverConnectedEdges=true`、`selectConnectedEdges=true` 均已启用。
- 只有用户双击固定的节点会持久化 `pinned`；保存过坐标不再等同于关闭全部节点物理。
- 关系标签沿边旋转并对长标题做 5 字压缩；完整标题和说明仍保留在悬浮提示与下方关系列表。
- 工具栏和警告改为占据正常文档流，不再覆盖 Canvas；图谱密度、留白、边宽和节点层级已在聚焦同图中对齐参考意图。
- 自动排布等待 1.4 秒后仍保持未保存状态，不再因 React 重建画布而瞬间复原。
- 缩放回归：112% → 132% → 112%；撤销后未保存状态清除。
- 自动排布已保存为最终布局；刷新后恢复 112% 和 6 个节点位置。

## 数据正确性与隔离

- 当前自动同步来源摘要：6 个角色、1 条关系情报、1 个匹配章节、3 个未匹配当前角色的章节被隔离。
- 62 条来自错置故事的派生事实已标记为 `source_superseded`；没有删除章节、提案、提交批次或人工关系。
- 数据库当前仍有 55 条有效事实；关系网保持 5 条 AI 关系和 1 条人工关系。
- 清理前完整 PostgreSQL 备份：`pre-relationship-cleanup.dump`，SHA-256：`9a0ccfecce521bf4e4b976cbd9f9c8ce5315d75a489c4dea3f2086100b0d8d3f`；`pg_restore --list` 已验证可读。
- 自动来源现在必须同时命中至少两个当前小说已登记角色；完整生成候选还必须达到 80% 置信度且证据文本同时提及关系两端角色。

## 回归与运行状态

- TypeScript：`tsc --noEmit` 通过。
- 前端单元测试：5 个文件、14 个测试全部通过。
- 后端默认测试：42 通过、19 个数据库用例按默认配置跳过。
- PostgreSQL 定向集成：关系增量同步/人工覆盖、图谱版本化/布局持久化，2 个测试全部通过。
- Vite 生产构建通过；QwenPaw 插件安装校验通过；健康接口为 `ready`，数据库连接正常。
- 浏览器文档状态为 `complete`；容器最近 10 分钟日志未发现 `ERROR`、`Traceback`、未处理异常。

## 对比历史

1. [P1] 初始版本在稳定后关闭全局 physics，并把所有保存节点设为 `physics:false`；拖动只有单节点位移，没有邻接弹簧联动。
2. 修复为可重启 ForceAtlas2、拖动开始启动模拟、拖动结束短时收敛、双击固定节点，并对相邻连线做选择高亮。
3. [P1] 第一轮浏览器回归发现工具栏状态更新会重新创建筛选关系数组，使 Canvas 被销毁重建；缩放和自动排布因此瞬间复原。
4. 用稳定 memo 化关系集合修复生命周期；浏览器复测缩放 84% → 99%，自动排布稳定后仍保留 dirty 状态。
5. [P2] 第一轮同图比较发现关系标签水平堆叠、图谱过于紧凑且浮层遮挡画布；已依次修正为沿边标签、长标题压缩、较长弹簧、正常文档流工具栏和保存后的新布局。
6. 最终 `20-final-graph-focused-comparison.png` 中，图谱覆盖范围、力导向拓扑、节点主次、边标签方向和整体留白已达到参考意图；无剩余 P0/P1/P2。

## 已知验证边界

- 两端关系图都是 Canvas；当前桌面浏览器自动化接口不能稳定保存真实鼠标按住期间的中间帧。因此拖拽动画通过运行代码、实际配置、拖动生命周期和拖后稳定状态交叉验证，最终手感仍适合由用户亲手拖动一次确认。
- 章节 4 正文中存在“陈守义/陈守正”的原文称谓不一致；本轮未擅自改写章节正文，因为这会使整章事实重新失效并触发同步。严格角色来源门已保证该称谓不会错误进入当前关系网。

final result: passed
