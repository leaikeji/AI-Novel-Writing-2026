# 章节标题编辑弹窗 Design QA

日期：2026-08-24

## 比较目标

- 源视觉真相：`reference.png`，1020×610 像素；按约 2x 密度归一为 `reference-normalized.png`（510×305）。
- 最终实现：`implementation-final-dialog.png`，弹窗 CSS 尺寸 520×303.59；归一为 `implementation-final-normalized.png`（510×305）。
- 完整页面：`implementation-final-full.png`。
- 浏览器 CSS 视口：1811×1249；浏览器报告 DPR：2。
- 状态：第4章《退回的旧木盒》的“编辑章节标题”弹窗打开，输入值 6 字，计数 6/20。

## 完整视图与重点区域

- 完整视图用于检查弹窗在正文编辑器中的居中、遮罩和层级。
- 重点区域使用弹窗裁切图；标题、标签、输入框、计数和双按钮均清晰可读，因此不需要额外更小的局部裁切。

## 必查保真面

- 字体与排版：系统中文无衬线字体、粗体标题、次级计数层级与参考一致；没有影响层级的偏差。
- 间距与布局：约 510×305 的横向弹窗、20px 圆角、单列字段和底部 1:1 双按钮已对齐；残余垂直差异小于约 10px，归为 P3。
- 颜色与令牌：白色面板、灰色描边和计数、橙色保存按钮、深色遮罩与参考一致。
- 图片与资产：参考弹窗无插画、Logo 或非标准图标，不存在资产替代问题。
- 文案与内容：标题、字段名、20字提示、取消/保存均对齐；输入内容使用当前真实章节名而非参考图示例，属于预期数据差异。
- 可访问性：输入框有标签；取消、保存均为真实按钮；空标题不可保存。

## 比较历史

1. 第一轮：视觉结构已对齐，但自动化粘贴可绕过原生 `maxLength`，计数曾显示 33/20，判为 [P2] 约束状态不可靠。
2. 修复：输入状态层强制截断到 20 字。
3. 第二轮：粘贴 33 字后实际值为 20 字，计数稳定为 20/20；取消不保存，保存可持久化，刷新回显正常。

## 交互与运行检查

- 已验证：打开、输入、实时计数、20字截断、取消、保存、刷新持久化、恢复原名。
- 当前章节最终恢复为“退回的旧木盒”，正文与章节序号未改变。
- 控制台：无 error、warn。
- TypeScript、6 项单元测试、Vite 生产构建、容器健康检查均通过。

## Follow-up Polish

- [P3] 参考图与实现的标题到字段区域垂直节奏仍有约 6–9px 差异，可在后续全站弹窗令牌统一时再微调。

final result: passed
