# 文档与视觉资产冗余审计（2026-08-31）

状态：**首轮只读扫描与高置信清理已完成；历史验收证据的批量去重尚未执行。**

范围：`docs/` 与 `audit/`。本轮不读取旧项目，不触碰数据库、小说正文、构建产物、当前未跟踪的审计目录或前后端施工文件。

## 扫描结果

- 扫描 299 个 Markdown 文件和 797 个图片／SVG 文件。
- Markdown 中直接引用 169 个图片文件；628 个图片没有 Markdown 直接引用。无直接引用不等于无效：部分证据依赖目录、JSON、验收脚本或文件命名约定，不能据此批量删除。
- 未发现失效的本地图片链接。
- 全仓通用链接复核另发现 4 个既有非图片断链，均位于悬疑刑侦 A/B 证据中的 v2／v3 `baseline/prose-writing.SKILL.md`，指向未随快照保存的 `references/narrative-craft.md` 与 `references/genre-promises.md`。它们不是本轮删除造成；在决定补齐完整 Skill 快照还是改为外部哈希引用前，本轮不改写历史实验输入。
- 发现 73 组字节完全相同的图片，共 75 个重复副本。完全相同也不自动等于冗余，例如“来源失效前／恢复后”截图相同，可能正是恢复成功的证据。
- `docs/开发文档` 共 257 个 Markdown 文件、约 3.9 万行。非证据文档中体量最大的五份为计划 18（2672 行）、计划 20（1180 行）、计划 27（1119 行）、计划 33（869 行）和计划 09（831 行）。

## 本轮已处理

1. 把 `docs/README.md` 收敛为高层导航，不再复制完整开发计划列表和验收长摘要。
2. 把 `docs/开发文档/README.md` 改为“当前基线／现行实现／待裁决／研究／历史”的状态分流，保留全部主要计划和 ADR 的入口。
3. TTS 页面设计 V1 已由其 README 和 V2 明确判定为过期布局：它未保留 QwenPaw 原生助手，并使用会冲突的右侧全高复核抽屉。本轮删除 12 张 V1 概念图（约 14 MB），保留文字历史摘要、替代原因和 V2 链接。删除文件均已受 Git 跟踪，可从 Git 历史恢复。

## 下一批高置信候选

### A. 妙笔一比一基线中的伪双视口副本

`证据/妙笔一比一基线-2026-08-23/` 有 321 张图片，其中 63 个重复组、64 个重复副本，可回收约 6.14 MB。多数成对文件名分别标为 `1920x1080` 与 `2560x1440`，但文件字节完全相同；抽样像素尺寸也同为 `2008×1240`，因此不能证明两个独立视口。

建议后续先为该目录建立清单，再为每组保留一个不虚构视口的 canonical 文件；涉及生成中／确认态等不同语义却图片相同的组，应标记为无效捕获，而不是冒充两个状态均已验证。

### B. 明确的调试／中间态副本

以下文件组字节相同、没有 Markdown 直接引用，且名称包含 `debug`、`repeat`、`fixed` 或“非 final → final”演进，可进入第二轮逐目录核验：

- `助手计划V2验证-2026-08-25/21-*`、`22-*`、`25-*-repeat` 三份相同截图；
- `章节标题编辑对标-2026-08-24/implementation-*` 与对应 `implementation-final-*`；
- `关系网P0优化-2026-08-25/04-zoom-in.png` 与 `zoom-current-debug.png`；
- `章节名称编辑-2026-08-24/02-fixed-modal.png` 与已被引用的 `03-final.png`。

### C. 不能仅按哈希删除的相同图片

- 关系来源“当前／恢复后”图片即使完全相同，也可能用来证明恢复后视觉与基线一致；必须保留语义索引或改为单图加双阶段哈希记录后才能删副本。
- 不同审计目录中的相同人物声音截图属于当前未跟踪工作，且可能由不同审查任务引用；本轮保持不动。

## 后续文档精简建议

- 计划 18 是最明显的正文收敛对象：文首叠加多轮状态覆写，正文同时保留已取消范围、旧工作包和长命令。后续应先冻结“当前规格”和“历史 T4 记录”的分界，再把历史运行细节留在既有 `证据/MOSS-TTS-Nano施工/`，不能直接截断导致门禁证据丢失。
- 计划 20、27、33 和 09 只应在明确现行替代关系和接口权威后拆分；未批准计划不能因精简被误写成已施工。
- 新增截图必须进入 README、证据清单或机器可读 manifest；临时 debug 图默认不进入正式证据目录。

## 删除门禁

后续删除图片前必须同时满足：无独立引用、无独立状态／视口／恢复语义、不是迁移或历史验收唯一原始证据、未覆盖用户未提交改动，并在删除后通过链接检查、`git diff --check` 和相关证据索引复核。
