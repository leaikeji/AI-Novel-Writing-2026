# 计划 46：人物卡写作视图与故事账本纠错闭环隔离验收证据

状态：**源码候选自动化与隔离合成数据验收通过，本专项已按用户后续授权独立提交并推送；长期 18088 和真实小说数据未操作。**

日期：2026-09-01（Asia/Shanghai）

## 1. 隔离边界

- QwenPaw：`anw-ccl-qwenpaw-20260901`，验收端口 `127.0.0.1:18190`。
- PostgreSQL：`anw-ccl-postgres-20260901`，独立网络 `anw-ccl-net-20260901`。
- 数据卷：`anw-ccl-qwenpaw-data-20260901`、`anw-ccl-qwenpaw-secrets-20260901`、`anw-ccl-qwenpaw-backups-20260901` 及隔离 PostgreSQL 卷；验收后只停容器，不删除卷。
- TTS 产品、运行时、参考音频克隆和验证开关全部关闭。
- 长期端口 `18088` 未安装候选包；《潮汐盲区》未发生保存、修正、同步、撤销或扩写。

Docker Desktop 在一次隔离容器重启后丢失了宿主端口发布状态。验收时先保留原容器为 `anw-ccl-qwenpaw-20260901-stale` 回退副本，再使用相同镜像、相同三个隔离 QwenPaw 数据卷和同一隔离数据库重建 `18190` 映射；新容器通过验收后删除无数据卷的旧容器外壳，没有删除数据卷。

## 2. 合成小说与覆盖数据

| 用途 | 小说 ID | 人物 ID | 覆盖范围 |
| --- | --- | --- | --- |
| 主验收样书 | `1a734808-914c-408b-93a8-82cbf5e2ca4f` | 林遥 `47e8cdb8-4899-450e-aee3-6fad12facf1d` | 7 人物、3 章节、500 条人物事实、关系、冲突、来源失效、替代链、已撤销批次 |
| 多时间线样书 | `badfb629-d0fe-46b5-af3b-68d97ddd1c84` | 乔岚 `cc8188b0-a053-4f09-9088-1387af615f7c` | 主线、分支线、2 个实例、显式时间线选择 |

主样书事实互斥计数：当前 8、历史 488、已替代 2、来源失效 1、已撤销同步 1，总计 500。风险摘要为冲突 2、不确定 0、来源失效 1。

写入验收包括：

- 人物根和实例 profile 在同一次保存中成功：人物根版本到 v3，实例版本到 v2；职业保存为“独立调查记者”。
- 身份事实修正为“独立调查记者（已公开身份）”，旧事实保留并由 supersedes 链连接。
- 批次 `df6553b8-72cd-4c7f-a905-ce02f8972be4` 经影响预览后撤销；批次事实保留为 `batch_reverted`，当前目标回落到先前有效值。
- 多时间线未显式选择时返回 `422 timeline_required`；选择“合成分支线”和对应实例后人物卡正常打开。

## 3. 自动化与构建

| 门禁 | 实际结果 |
| --- | --- |
| `.venv/bin/python -m pytest` | `3431 passed, 161 skipped, 3 warnings` |
| 暂存快照直接运行 Vitest | `122 passed` 测试文件，`1039 passed` 测试；不含未提交朗读改动 |
| `pnpm typecheck` | 通过 |
| `pnpm build` | 通过，157 modules transformed |
| `.venv/bin/python scripts/package_plugin.py` | 通过，输出 `build/ai-novel-world-2026` |
| `.venv/bin/python -m pytest tests/test_skill_contract.py` | `12 passed` |
| `git diff --check` | 通过 |

## 4. 500 条事实性能

同一隔离容器预热 5 次后，通过宿主 HTTP 串行采样：

| 接口 | 样本 | 响应大小 | P50 | P95 | 门禁 |
| --- | ---: | ---: | ---: | ---: | ---: |
| workspace V2 | 60 | 30,632 bytes | 59.84ms | 261.32ms | < 300ms |
| facts，20 条／页 | 100 | 29,661 bytes | 46.95ms | 228.62ms | < 250ms |

workspace 不再内联 491 条事件／历史事实，只保留 8 条当前生效事实；500 条完整历史仍通过分页接口可审计。该数据只代表本次隔离本机门禁，不是跨机器 SLA。

## 5. 浏览器证据

- [批次撤销后的状态与经历](./01-状态与经历-批次撤销后.png)
- [鼠标关闭后人物卡列表无装饰性蓝框](./02-人物卡列表-鼠标关闭后.png)
- [来源 revision 精确证据高亮](./03-来源证据-精确高亮.png)
- [事实修正抽屉](./04-事实修正抽屉.png)
- [冲突与来源失效风险态](./05-风险态-冲突与来源失效.png)
- [多时间线显式选择器](./06-多时间线选择器.png)

浏览器同时核对了 1240px 对话框、固定头部／栏目／底部、中间单滚动区、四栏目命名“基础资料／当前线设定／状态与经历／声音”、500 条历史惰性展开、鼠标无装饰性焦点和可操作控件的键盘语义。

在删除 `workbench-v2.ts` 返回后的旧角色／线索不可达渲染及全书 `story-facts` 请求后，重新构建并热安装最终候选包；角色页与线索页均完成无应用错误的浏览器冒烟检查。随后再次停止隔离容器。

## 6. 恢复与后续边界

- 合成写入只存在于隔离 PostgreSQL；恢复方式是停用隔离容器并保留卷，或在获得明确删除授权后精确删除这些 `anw-ccl-*` 资源。
- 验收结束后 `anw-ccl-qwenpaw-20260901` 与 `anw-ccl-postgres-20260901` 已停止；临时端口测试容器和旧容器外壳已删除，四个 `anw-ccl-*` 数据卷保留。
- 长期环境没有候选安装，因此不需要恢复 18088 或真实小说数据。
- 若未来申请长期安装，仍必须单独执行插件安装／卸载、旧包回退、原生聊天与设置非回归、备份和失败恢复门禁。
