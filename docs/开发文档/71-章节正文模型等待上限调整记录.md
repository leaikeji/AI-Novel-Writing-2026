# 章节正文模型等待上限调整记录

状态：`V1.1 / 正式 18088 已安装并验证生效；未执行真实模型调用`

日期：2026-09-12（Asia/Shanghai）

## 1. 用户决定与现场事实

用户根据《缺氧：末日地下世界》第1章“签下这栋破楼”的失败记录，明确要求把章节正文模型等待上限调整为12分钟。该次正式任务请求 `bigmodel / glm-5.3-flash`，在原300秒硬上限结束，没有收到可核验的实际模型身份、用量或正文候选，正式正文未修改。

本记录只落实时间上限调整，不把“模型仍在正常思考”写成已核实事实，也不改变模型、Provider、Skill选择或正式作品数据。

## 2. 实现范围与安全边界

- `CHAPTER_GENERATION_TIMEOUT_SECONDS` 从300秒调整为 `12 * 60` 秒；既有章节正文入口与受管正文入口共用该上限。
- 陈旧章节任务仍按“硬上限＋30秒宽限”回收，因此新回收门槛为750秒。
- 继续保留请求取消、超时取消子任务、迟到结果丢弃、失败不修改正式正文及模型身份核验。
- 计划70的语义辅助路由继续使用自身短超时；模板、取名、正文采用、同步和TTS超时不在本次范围。
- 不修改QwenPaw上游，不新增依赖、配置项、数据库字段或迁移。正式插件替换只在作者后续明确要求“修改生效”后执行。

2026-08-26的300秒记录是当时真实历史证据，保持原文；本记录只说明2026-09-12之后的仓库候选行为。

## 3. 子代理并行施工设计

本任务不并行：仅一个常量、其直接回归测试和状态文档，拆分会增加同一契约的协调成本。

- `S71-ONE`（SER/INT/GATE）：唯一目标是把章节正文硬等待改为720秒并验证超时安全语义；初始非目标是未经授权部署、真实模型重试和其他生成任务调参。作者后续单独授权正式生效后，打包、安装、重启恢复与运行复核仍由主代理串行完成。
- 写入所有权：`backend/generation_runtime.py`、`tests/test_generation_runtime.py`、`tests/test_domain_integration.py`、本文及两级文档索引；只读 `backend/app.py`、`backend/writing_skills/button.py`、`backend/services.py`。
- 禁止触碰：QwenPaw上游、数据库、正式小说、模型设置、计划66/69/70既有候选和其他用户改动。
- 冻结接口：异常类型、错误文案结构、30秒宽限、取消与迟到结果作废语义不变。
- 验收：项目解释器定向pytest、相关领域超时回收用例、`git diff --check`和本次diff复核；获部署授权后增加候选边界比较、公开插件安装、进程内常量、Agent／Skill／Tool／模型及容器健康复核；不调用付费模型。

## 4. 本地验证

- `.venv/bin/python -m pytest tests/test_generation_runtime.py tests/test_api_model_orchestration.py`：`9 passed`。覆盖默认720秒契约、750秒陈旧门槛、硬超时取消、请求取消、正常返回及现有模型编排负向路径。
- `.venv/bin/python -m pytest tests/test_domain_integration.py -k 'stale_chapter_generation' -rs`：`1 skipped`，原因是当前未配置隔离集成测试数据库；未把该项记为通过。测试数据年龄已从10分钟调整为15分钟，确保未来在隔离库中仍超过750秒新门槛。
- `git diff --check`：通过。

## 5. 正式部署与运行证据

作者随后明确要求修改生效，并明确授权在热加载卡住后强制重启 Docker Desktop。部署候选从提交 `ad4cba3` 的干净 Plan 67 正式基线构建，仅应用本记录的超时常量修改；与同源300秒基线逐文件比较，只有 `backend/generation_runtime.py` 不同。正式前端 bundle SHA-256 与候选均为 `e00348b6fa70a18285ad86e874a8ce4d42faeddb90c4f1dbb8d3b77e2ee4164c`，未夹带尚未发布的计划70界面或运行时改动。

- 候选 tree SHA-256：`ad839727af2e9f3218ebc0887b86867e49e4345eb93278772d1a3c08f6d40816`；迁移 head 保持 `20260911_0053`。
- 通过 QwenPaw 2.2.0 公开 `/api/plugins/install` force 热更新成功。热加载随后使 `18088` 无响应，普通 Docker Desktop restart 未能在30秒内结束；经作者明确授权，使用官方 `docker desktop stop --force --timeout 30` 后 `docker desktop start` 恢复。没有删除容器、卷、小说、revision、媒体或数据库。
- 重启后项目插件进程内只读导入结果为 `720` 秒，陈旧任务门槛为 `750` 秒；安装树除运行产生的 `__pycache__` 外与候选逐文件一致，目标源文件 SHA-256 均为 `9097ae5098fe1eeb71d55a991991fce70c2098503282f773978c88b1d12e7afc`。
- 干净基线的正式公开契约验证通过：PawApp、数据库和朗读均为 `ready`；`AI小说作家` 仍使用 `bigmodel / glm-5.3-flash`，11项小说Skill和5项小说工具全部启用，默认与QA Agent均无项目Skill／工具。
- 原失败任务仍保留 `300015 ms`、`failed`、零候选和“正式正文未修改”证据；历史失败消息不会被改写。新的正文生成任务才使用720秒上限。
- 未执行真实模型重试，因此本次只证明等待上限与运行契约生效，不证明该模型一定能在12分钟内成功返回。

仓库外恢复点：`/Users/liujia/Documents/AI小说世界2026-backups/plan71-timeout-20260912-020421`，目录权限0700、文件0600。720秒候选ZIP SHA-256为`9228169f62f25ea41844a81ad335b59d3ba0f0544f00370944d24c8d6b4f23f6`；300秒回退ZIP为`92c87c14529e70696a0bcb83465f17300e776f3a41ed07502ba083d1ac290941`，并保存安装前Skill状态。若需回退，应通过同一公开插件安装接口安装300秒包、重启QwenPaw并按快照恢复Skill；schema未变化，不需要倒库。
