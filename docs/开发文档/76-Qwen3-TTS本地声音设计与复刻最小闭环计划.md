# 开发计划 76：Qwen3-TTS 本地声音设计与复刻最小闭环

当前增量状态（2026-09-14）：**V0.7.1已完成代码校验、正式更新、页面描述核对及既有候选的桌面Edge完整播放；该候选早于V0.7.1创建，新候选生成、作者听感、锁定／绑定与章节应用尚未完成。V0.8人物声音页状态一致性与减重、V0.8.1步骤呈现与焦点收尾均已正式生效，并通过桌面只读验收。** 详见[第10节](#10-v07-人物音色生成优化计划2026-09-14)、[第11节](#11-v08人物声音页状态一致性与减重计划2026-09-14)、[V0.7.1记录](../../audit/qwen-tts/plan76/character-voice-v07/README.md)、[V0.8记录](../../audit/qwen-tts/plan76/character-voice-v08/README.md)与[V0.8.1记录](../../audit/qwen-tts/plan76/character-voice-v081/README.md)。V0.8／V0.8.1均未调用模型、播放音频或更改声音绑定，因此不扩张为听感或完整声音链验收。相关源码、计划、测试与证据已由提交`32bea56`纳入并推送，索引补记由`38da2e0`纳入并推送；各证据中“未提交／推送”只描述正式更新当时的历史时点。

计划级治理状态：**`CONDITIONAL`（等待作者试听决定，不属于当前施工）**。现有候选安全保留为`draft/preview_ready`，人物仍绑定原官方音色；只有作者明确回复采用或重新生成后，才恢复一个精确窄任务。采用时只锁定／绑定当前候选，重新生成时只创建一份新候选且不自动采用。

2026-09-13 Git 交付补记：作者新增明确提交与推送授权，本计划已发布源码作为计划 77 页面依赖一并纳入精确提交。此前“未提交／推送”为当时事实；范围与本次独立回归见 [Git 交付复核](../../audit/reading-ui-20260912/git-delivery-20260913.md)。

状态：**V0.6 已发布到正式 `18088`；W0—W3 的自动化、隔离数据库、真实 MLX 两模型、正式数据库迁移、运行态与桌面浏览器门禁均已通过。作者听感确认仍由作者实际使用后判定。**

2026-09-13 年龄优先补充（已正式发布）：人物卡进入文字设计音色时，从人物档案的“开篇年龄说明”读取明确年龄，缺失时才从公共小传提取明确的“X岁”；生成的可编辑音色描述把年龄放在第一句和第一优先级，性别、性格与声线气质随后排列，且明确禁止“沉稳／坚韧／威严”等词覆盖年龄感。年龄没有明确证据时不根据姓名、职业或身份猜测，也不自动启动 VoiceDesign。该补充只生成可编辑描述，作者仍需显式点击创建、试听、确认和绑定。

本次正式更新只替换 PawApp 前端 `frontend/dist/index.js`，未重启 Docker，未调用语音模型，也未新建、锁定或绑定音色。安装后公开验证脚本通过，正式 `18088` 的本地 TTS、产品后端、worker 与 Base 参考复刻均为 `ready`。桌面浏览器以人物“陈屿”复核：其公共小传中的明确年龄“26岁”已成为音色描述首句，页面同时显示“年龄感优先于沉稳、坚韧等气质”；没有点击“创建设计候选”。发布候选树为 `9861b8980fc32c585e08a8c06969e29a39460c11`，备份位于 `/private/tmp/plan76-age-backup.hgOLu4`。年龄优先功能源码已通过独立提交 `2df5f17` 纳入主分支并推送；本段发布记录自身仍为工作区文档改动，未单独提交。

2026-09-13 声音听感年龄补充（已正式发布）：实际年龄仍放在描述第一句；只有人物公共小传明确出现“声音／声线／嗓音听起来像、接近或呈现某个年龄／年龄段”时，才把“声音听感年龄”作为有意覆盖写入描述。普通的沉稳、坚韧、低沉或沙哑不构成年龄覆盖，否定句（例如“声音不像四十岁”）也不触发；沙哑、粗粝等音质与年龄感分开表达。这样既避免年轻人物被性格词意外中年化，也保留“实际十八岁、声音有意像四十岁”的作者设定。

该补充的正式候选为临时发布提交 `0cd2479`、tree `95a52c1f2b4199cd3dac8f1544ef1a73587f937e`，只替换 `frontend/dist/index.js`，SHA-256 为 `4b1cbe9881eff5b88dd40608573c64a8dade757325127a5820f14799d4b17083`。首次热装后发现 PostgreSQL 既有连接已达上限，导致朗读生命周期停在 `starting`；本地 TTS 进程独立健康。为释放连接只重启了 QwenPaw 容器，未重启 PostgreSQL、未删除卷或恢复数据库。重启后精确包、逐 Agent 选择、schema `20260913_0056`、健康、worker、Base 参考复刻及完整公开验证均通过；桌面页面确认现有人物年龄优先描述正常。没有为特殊样例修改正式人物资料，也未调用模型或改动正文、音频、音色版本和绑定。备份位于 `/private/tmp/plan76-perceived-age-backup.tHlHIz`；本次补充源码与本段记录尚未提交或推送 Git。

日期：2026-09-12

## 0. 施工记录（2026-09-12）

已完成并验证：

- 本地运行时正式接通 CustomVoice、Base-8bit 与 VoiceDesign-BF16 三种角色；声音设计只产生耐久普通话参考 WAV，最终试听和章节生产统一由 Base 参考复刻执行；
- 新增 Provider 中立的私人音色应用服务，接通设计候选、上传候选、后台试听任务、媒体发布、模型身份记录、质量确认和锁定；
- 前端开放文字设计与上传参考录音两条最小路径，所有官方／设计／上传／范围覆盖入口统一为普通话，不再提供语言或方言选择器；后端同时拒绝非 `zh-CN` 和明确方言设计指令；
- 修复失败投影在后台任务终态后的权威刷新；播放器只跳过 `failed/cancelled` 终态缺口并继续下一段，`pending/queued/rendering` 仍等待，不会把生成中的句段误跳过；
- 短中文质量失败补充实际时长、允许上限、字符数和策略版本，不保存失败音频或正文；
- 新迁移 `20260912_0055` 先在一次性 PostgreSQL 18 隔离数据库从空库升级到 head，并通过“设计→耐久参考资产→Base 试听→锁定→双模型运行记录”真实事务闭包测试；随后在备份、停机和迁移前权限门禁通过后，正式数据库已从 `20260912_0054` 精确升级到 `20260912_0055`；
- 临时回环运行时真实完成 VoiceDesign-BF16→Base-8bit 串行切换：设计锚点为 24 kHz 单声道、5.36 秒、257324 bytes，Base 对不同普通话文本的复刻试听为 24 kHz 单声道、3.04 秒、145966 bytes；返回的模型 ID、revision、制品树指纹与冻结值一致，参考 SHA-256 校验一致，服务随后正常关闭；样音未落盘、未入库、未进入 Git，也没有云端调用；
- 完整后端回归：`2919 passed, 274 skipped`；前端：154 个测试文件、1340 个测试全部通过；`pnpm typecheck`、`pnpm build`、插件打包和 `git diff --check` 通过；
- 正式 `18088` 已安装隔离构建且树指纹锁定为 `92fbbc2678b496dbdb5a80956b53f20abda5f51844053e6651bbcad37134fd65` 的候选包，QwenPaw 2.2.0 容器和 PostgreSQL 均为 `healthy`；运行态报告本地 TTS、产品后端、worker 和 Base 参考复刻均为 `ready`；
- 桌面浏览器已只读验收朗读设置、普通话固定规则、文字设计／上传参考录音入口和既有章节播放器；播放器实际从 `1:11` 播放到 `1:14`，3 个历史音频质量失败句段均显示终态说明与禁用重试按钮，不再错误提供“恢复 2 组”。

仍需作者实际使用后确认：

- 正式发布没有替作者新建、锁定或绑定私人音色，避免污染作品数据；文字设计与上传参考录音的真实产品写入路径仍需作者按正常页面流程创建候选并确认听感；
- 本轮测量时本机已有约 20 GiB 系统 swap 占用，缺少启动前独立基线，不能把本轮写成“0 swap 已通过”；模型管理器的单驻留／先卸载后切换已由自动化和最终 Base 身份核实，但低内存门禁仍需在干净基线复测；
- 本轮未提交或推送 Git；正式发布备份和恢复信息见 `audit/qwen-tts/plan76/formal-release-20260912.md`。

一次性 PostgreSQL 验证容器已停止。安全规则阻止在没有单独删除授权时移除其匿名测试卷，因此测试数据暂时保留；正式数据卷没有删除或清空。

## 1. 结论

本计划只补齐两个已经存在但尚未发布到产品层的本地能力：

1. `Qwen3-TTS-12Hz-1.7B-VoiceDesign`：根据文字描述生成一段合成音色锚点；
2. `Qwen3-TTS-12Hz-1.7B-Base`：根据合成音色锚点或作者有权使用的参考录音，生成可长期复用的人物声音。

正式章节不直接依赖 VoiceDesign 的临时 `provider_voice_id`。VoiceDesign 只负责第一次生成参考音频；作者确认前，再由 Base 使用该参考音频合成另一段试听。最终锁定的声音始终以 Base 参考复刻方式参与章节朗读，因此服务重启、模型卸载和后续章节生成都不依赖进程内临时状态。

本计划不恢复 MOSS、Nano、VoiceGenerator、旧 `voice_preparation_commands`、人物批量静默生成或 24 槽通用池，也不在每章开始时重新设计音色。人物卡可依据已保存人物事实预填一份可编辑的年龄优先描述，但预填不调用模型、不创建资产、不锁定或绑定声音。

新增统一语言规则：无论使用官方音色、文字设计音色还是上传录音复刻，产品内生成、试听和章节朗读都只使用普通话。页面不提供粤语、四川话、东北话等方言或地域口音选项，后端不接受方言指令，worker 固定使用 `zh-CN`／普通话执行。正文中原本存在的方言词句不由 TTS 改写，但只能按普通话朗读。

## 2. 当前事实与缺口

### 2.1 已有能力

- Mac 本地运行时已有 CustomVoice、Base、VoiceDesign 三种模型角色和串行加载锁；三者已分别通过真实中文生成与 0 swap 验证。
- 本地运行时已有 `/v1/voices/clone` 与 `/v1/voices/design`，PawApp Provider 也已有 `clone_voice`、`design_voice` 和模型身份校验。
- 数据库仍保留 Provider 中立的 `voice_profiles`、`voice_profile_versions`、`voice_previews`、`media_assets`、`background_jobs`、权利记录和人物声音绑定。
- `narration.voice_preview` 后台任务种类已由计划 62 改为 `qwen-tts` 资源类，没有随 MOSS 专用任务一起删除。

### 2.2 施工前缺口（已由 V0.6 闭合）

- 正式应用曾把 `reference_clone_released` 固定为 `false`，并给设置后端注入 `voice_product=None`；V0.6 已改由新的 Qwen 私人音色产品端口提供 Base 上传、真实试听与锁定。
- 前端私人音色页曾只允许选择 `uploaded`；V0.6 已增加 `generated` 文字设计表单。
- `source_type=generated` 曾缺少允许的权利来源；V0.6 已加入合成设计来源声明与校验。
- 正式 worker 曾只领取 `narration.segment_render`；V0.6 已同时处理 `narration.voice_preview`。
- VoiceDesign 的进程内临时设计 ID 不再作为耐久身份；V0.6 保存普通话参考 WAV，并统一由 Base 使用参考资产生成试听和章节音频。
- 计划 75 发布时保留了官方 speaker 的原始语言分组；V0.6 已把官方、设计和上传路径统一限制为普通话。

### 2.3 2026-09-12 当前章节失败复核

本次只读复核确认截图中的问题包含两个层面，不能只按“模型偶发失败”处理：

- 正式 Edition `52650b36-db47-496d-a4ef-ca120e85f1e5` 当前有第 70、114、199 句失败；三句均使用 `qwen.WarmFemale`／Serena、`zh-CN`、固定 seed `1234` 和本地 `CustomVoice-8bit`。
- 三句初次生成和一次作者手动重试都返回 `TTS_AUDIO_INVALID`，持久化原因均为 `SHORT_CHINESE_DURATION_IMPLAUSIBLE`；当前后端投影已把三句全部标为 `LATEST_MANUAL_ATTEMPT_NON_RETRYABLE`。
- 页面仍把第 114、199 句显示为可重试并保留“恢复 2 组”按钮，和同时刻后端权威投影不一致。根因是页面等待重试时只观察 Manifest：终态失败不会产生新的可播放 Manifest revision，最后一次失败投影可能在 worker 完成前被读取，此后没有触发再次刷新。
- 当前质量证据只保存原因码，没有保存“实际音频时长／允许上限”，暂时不能证明是模型失控还是阈值需要重新校准。因此不得直接放宽校验规则。

本次检查没有重试模型、修改正文、切换音色、写数据库或替换已有 Edition。

## 3. 产品流程

### 3.1 入口

复用朗读页现有“私人音色”区域，不新增独立模型管理页。作者先选择或创建一个作品内音色档案，再选择以下来源之一：

- **文字设计音色**：填写人物音色描述和试听文本；语言固定为普通话，不提供选择器；
- **上传参考录音**：上传 WAV／FLAC、填写参考文本和现有权利声明。

音色档案可在锁定后通过既有“人物配音”能力绑定给一个人物。人物入口可以预选人物，但不复制第二套音色工作台。

### 3.2 文字设计音色

1. 人物卡先读取明确的开篇年龄并把它作为第一约束，再补充性别、声线、语速和情绪，形成作者可编辑的自由描述；没有明确年龄时不猜测。作者也可手动修改描述，但不得填写方言、地域口音或非普通话指令，系统不强制复杂参数表。
2. VoiceDesign 按固定普通话锚点文本、描述和 seed 生成候选参考 WAV。
3. 系统把候选 WAV 保存为该候选版本的不可变 `reference_asset`；不把临时设计 ID当作耐久身份。
4. Base 使用该参考 WAV 和对应锚点文本，合成作者填写的另一段试听文本。
5. 作者播放 Base 试听并明确确认质量后，锁定声音版本；拒绝或重新生成只创建新候选，不覆盖旧版本。
6. 后续章节只调用 Base 参考复刻；VoiceDesign 不再参与该声音的日常合成。

### 3.3 上传参考录音

1. 沿用现有 WAV／FLAC、大小、哈希、转码和权利校验；只处理作者自有、获授权或可合法克隆的普通话声音。
2. 作者确认参考录音为普通话，系统保存规范化参考 WAV 与准确的普通话参考文本；明显为方言或非中文的样本不得进入锁定状态。
3. Base 生成试听；作者播放并确认后锁定。
4. 后续章节持续使用同一参考资产，不重复提取或上传。

### 3.4 章节行为

- “一键朗读本章／全书”继续只识别说话人并解析已锁定声音。
- 新人物没有声音时继续显示 `B_VOICE_MISSING`，并给出“前往人物配音”的明确入口；不自动调用 VoiceDesign。
- 新锁定或切换人物声音只影响随后显式新建／更新的 Edition；历史 Edition 与已有音频保持不变。
- 官方音色、设计音色和上传复刻音色的试听与章节请求都固定为 `zh-CN`；人物籍贯、性格、台词用词或模型建议不得自动切换方言。

### 3.5 失败句段的最小收口

- 保留现有“音频质量失败最多允许一次作者手动重试”边界；同音色、同参数再次失败后立即成为终态，不再显示可点击的重复重试入口。
- 重试完成状态同时以后台任务和失败句段投影为准，不再只等待 Manifest revision。受影响句段全部变为 `ready` 或明确终态后，页面一次性更新计数、按钮和解释。
- 连续播放遇到 `failed/cancelled` 终态句段时，播放器明确提示“第 N 句音频失败，已跳过”，随后继续播放下一个 `ready` 句段；仍在 `pending/queued/rendering` 的句段继续等待恢复，不把尚在生成的缺口误判为终态并跳过。
- 失败详情提供“更换普通话旁白音色并更新朗读”入口。更换声音必须由作者明确操作并创建／切换新 Edition，不在同一 Edition 内偷偷混入另一种声音。
- 页面重新获得焦点、重新打开失败详情或后台任务结束时，执行一次有界权威投影刷新；稳定终态后停止轮询，不恢复整页闪烁。

## 4. 最小技术设计

### 4.1 不新增业务表

复用现有结构：

- `voice_profiles`：作品内音色档案；
- `voice_profile_versions`：上传或设计产生的不可变候选／锁定版本；
- `voice_previews`：Base 试听状态和结果；
- `media_assets`：耐久参考 WAV 与有期限试听 WAV；
- `background_jobs`：`narration.voice_preview` 的排队、租约、重试和崩溃恢复；
- `voice_rights_records/events`：上传来源的授权证据，以及合成设计音色的来源声明；
- `character_voice_bindings`：作者确认后的既有人物绑定。

不得重新建立计划 62 已删除的 MOSS／VoiceGenerator 命令表。

### 4.2 唯一前向迁移

基于施工时唯一 Alembic head 新建一个 revision，不修改历史迁移。迁移只做：

1. 允许 `source_type=generated` 使用新的稳定来源 `qwen_synthetic_design`；
2. 约束生成音色在 `preview_ready/locked` 时必须具备耐久 `reference_asset_id`；排队中的 draft 可暂时为空；
3. 校验上传／生成音色最终都以 `voice_kind=reference_clone` 进入章节生产；
4. 更新声音版本、试听任务、媒体和人物绑定的数据库闭包；
5. 保持现有官方 9 音色、旁白设置、人物绑定、Edition、render 和媒体行完全不变。

迁移不新增表，不修改小说正文，不回填或自动生成任何声音。

### 4.3 Provider 中立应用服务

新增一个窄的 `QwenVoiceProductService` 实现现有 `VoiceProductPort`：

- HTTP 请求只负责校验、创建候选与排队，模型调用不占用数据库长事务；
- `narration.voice_preview` worker 对上传版本只执行 Base 试听；
- 对设计版本先执行 VoiceDesign、发布参考 WAV，再切换到 Base 生成最终试听；
- 每次外部调用前后都核对 novel/profile/version/job、内容哈希、模型身份和租约 fence；
- API 只接受普通话语言值，拒绝方言／地域口音字段和明确的方言设计指令；worker 再次断言 `zh-CN`，不能仅依赖前端隐藏选项；
- 失败只更新候选／试听状态，不改变当前已锁定声音和人物绑定；
- 重试复用已成功发布且哈希一致的参考资产，不重复收费、不产生重复媒体。

### 4.4 本地运行时

- 继续只允许一个模型角色常驻；设计任务按 `VoiceDesign → 卸载 → Base` 串行执行。
- 正式章节只使用 `Base` 或 `CustomVoice`，并统一传入 `zh-CN`／普通话；不使用进程内 `_voice_roles` 作为耐久声音事实。
- 模型路径、revision、制品 SHA、实际模型身份继续按现有 Provider 契约校验。
- 取消、超时或进程重启后由后台任务租约恢复，不把半成品标记为可用。

### 4.5 音频质量证据

- `audio_validation_json` 继续使用现有 JSON 字段，不新增表或列；对 `SHORT_CHINESE_DURATION_IMPLAUSIBLE` 增加不含正文的有界证据：实际时长、允许上限、参与计算的字符数和策略版本。
- 质量门禁仍在转码和媒体发布前执行；失败 WAV 不保存、不发布，已有音频和媒体摘要不变。
- 先用真实普通话短句复核 Serena 和至少两个其他官方音色，再决定是否需要校准时长策略；没有客观时长证据不得放宽全局阈值。
- 同参数第二次失败是停止信号，不再用无限重试掩盖确定性模型问题。恢复方式优先为作者选择另一款已通过普通话门禁的音色并更新 Edition。

### 4.6 API 与页面契约

在现有命名空间内增加最少接口：

- `POST /voice-profiles/{profile_id}/versions/designed`：创建 `generated → reference_clone` 设计候选；请求固定包含 `expected_profile_version`、有界 `description`、`language=zh-CN` 和可选 seed，返回 `VoiceProfileVersionResource`。随后复用现有 preview 接口提交另一段试听文本并排队；
- 现有 `POST /voice-profiles/{profile_id}/versions/uploaded`：从 HOLD 开放为真实 Base 复刻候选；
- 现有 `POST /voice-profiles/{profile_id}/previews`、`GET /voice-previews/{id}`、锁定接口继续复用；
- `voice_design`、`reference_clone` 只有在 schema、VoiceProductPort、worker、媒体与对应模型身份全部就绪时才 actionable。

页面只增加“文字设计音色”来源卡及一个简短描述框、普通话固定标识、试听文本、生成／取消按钮，不提供语言或方言选择器。官方音色页统一显示“普通话”，并只提供普通话试听。每个候选只局部更新，不刷新整页，不影响章节播放器。

失败收口不增加新接口：继续复用失败句段 GET／重试 API、现有 Manifest 和后台任务状态；前端只修正终态判定、权威刷新与播放跳过行为。

W0 冻结裁决：产品唯一 TTS 语言值为 `zh-CN`；显式方言、地域口音或外语设计词返回 `VOICE_LANGUAGE_UNSUPPORTED`。设计描述只在候选尚未产出参考资产时暂存于现有 `parameters_json`，VoiceDesign 成功发布耐久参考 WAV 后立即移除明文，只保留摘要、策略版本、参考资产和模型身份。Preview 任务只保存既有有界试听文本，终态后继续按当前规则清除。

## 5. 明确非目标

- 不做每章自动设计、每个人物静默生成或批量一键造声；
- 不恢复 24 槽池、自动选角计划、旧 MOSS 数据表或 VoiceGenerator 名称；
- 不新增第二套任务队列、数据库、音色市场、波形编辑器或复杂参数面板；
- 本计划不接入阿里云声音设计，也不执行云端计费调用；本地闭环稳定后，云端设计音频可在后续小计划中作为同一 Base 参考资产来源；
- 不修改 QwenPaw 上游核心，不让模型输出直接修改人物卡或正文；
- 不自动删除候选参考音频、历史声音版本或已有朗读。
- 不提供外语、方言、地域口音或按人物籍贯自动切换发音；以后若要开放，必须另立计划并由作者重新批准。
- 不静默给单句切换音色、不把终态失败伪装为成功、不发布未通过质量校验的 WAV，也不为了消除红色提示直接放宽全局时长阈值。

## 6. 子代理并行施工设计

规划阶段不启动子代理。作者批准施工后，先由主代理冻结 wire、数据库字段语义和任务状态，再按以下波次执行。工作包 ID 仅属于计划 76。

| 波次 | 工作包 | 类型 | 目标与精确文件所有权 | 禁止触碰 | 验收证据 |
| --- | --- | --- | --- | --- | --- |
| W0 | Q76-C | SER/GATE | 主代理冻结请求／响应、`generated → reference_clone`、普通话唯一语言值、方言拒绝错误码、任务状态和资产生命周期；修改本计划、`backend/narration/contracts.py`、`schemas.py`、前端 fixture | 运行环境、数据库、模型文件 | 契约正负测试与评审记录 |
| W1 | Q76-R | PAR | 加固本地 VoiceDesign→Base 串行与重启无临时 ID 依赖；仅 `tts_runtime/service.py`、`server.py` 及对应测试 | PawApp、数据库、前端 | fake＋真实模型定向测试、模型卸载证据 |
| W1 | Q76-B | PAR | 新建 `backend/narration/qwen_voice_product.py` 及独立单元测试，实现候选、任务和短事务应用服务 | `app.py`、`production_runtime.py`、迁移、前端 | 幂等、CAS、失败零污染、恢复测试 |
| W1 | Q76-F | PAR | 扩展 `voice-source-panel.ts`、`voice-source-workspace.ts`、`official-voice-library.ts`、`api.ts` 及对应测试，增加文字设计来源、普通话固定标识和局部状态 | 后端、迁移、共享工作台入口 | Vitest、键盘、取消、方言选项不存在、错误与 Blob 释放 |
| W1 | Q76-Q | PAR | 为短中文时长失败补充有界诊断；仅 `backend/narration/audio_pipeline.py`、`worker.py` 及对应测试 | 不改质量阈值、数据库结构、Provider、前端 | 原因码＋实际／上限时长证据、失败零媒体 |
| W1 | Q76-P | PAR | 修正失败投影终态刷新和连续播放跳过；仅 `failed-segment-retry-state.ts`、`segment-playback-queue.ts`、`narration-player.ts` 及对应测试 | 不改后端、共享 `workbench-v2.ts`、声音绑定 | worker 慢完成、终态失败、pending 不跳过、播放续接测试 |
| W2 | Q76-D | SER/MUTEX/GATE | 主代理修改 `backend/models.py`，创建唯一新 Alembic revision并更新迁移／角色／删除闭包测试 | 历史 revision、正式数据库 | 空库升级、0054→新 head、约束正负、恢复说明 |
| W2 | Q76-I | SER/MUTEX/INT | 主代理接线 `backend/app.py`、`production_runtime.py`、`privacy.py`、`voices.py`、`settings_api.py`、worker／scheduler、`frontend/src/workbench-v2.ts` 和前端导出 | QwenPaw 上游、云端渠道 | API→任务→媒体→锁定→人物绑定、失败终态刷新与续播集成测试 |
| W3 | Q76-V | GATE/MUTEX | 主代理全量回归、隔离升级、本机真实两条闭环、服务重启与桌面浏览器验收；正式发布另等授权 | 正式数据写入、Git 提交／推送，除非作者另行授权 | 自动化、音频身份、重启续用、页面截图与发布裁决 |

冻结接口后 Q76-R、Q76-B、Q76-F、Q76-Q、Q76-P 可并行，各包不得修改同一文件。迁移、`backend/app.py`、`production_runtime.py`、`frontend/src/workbench-v2.ts`、worker 调度、唯一 Qwen 运行时、正式数据库、部署、暂存、提交与推送全部为 MUTEX，由主代理单一负责。子代理不得读取真实密钥、操作正式环境、提交 Git 或改动当前工作区内计划 27／66／68／69／70／74／75 等无关候选。

汇合顺序：`Q76-C → Q76-R/B/F/Q/P → Q76-D → Q76-I → Q76-V`。唯一集成责任人为主代理。

## 7. 验收门禁

### 7.1 自动化

- 本地运行时：VoiceDesign、Base、模型切换、取消、错误、身份漂移和进程重启；
- 后端：上传／设计候选、幂等重试、CAS、权利、媒体发布、worker 租约、失败零污染、锁定和人物绑定；
- 后端语言规则：官方／设计／上传三条路径只接受 `zh-CN`，非普通话语言值、方言字段及明确方言设计指令都返回稳定业务错误；
- 质量证据：短中文时长失败记录实际时长、允许上限、字符数和策略版本，不保存正文或失败 WAV；
- 前端：两种私人来源、官方音色、字段校验、普通话固定标识、无方言入口、局部进度、取消／重试、试听播放、质量确认和冲突恢复；
- 失败收口：worker 在 30 秒边界前后结束都能刷新为真实终态；成功项消失、失败项禁用，按钮计数与后端一致；播放只跳过终态失败，不跳过生成中缺口；
- 数据库：现有 head 升级、新约束、四角色、整书删除与声音媒体闭包；
- 全量：`.venv/bin/python -m pytest`、`pnpm typecheck`、`pnpm test`、`pnpm build`、插件打包和 `git diff --check`。

### 7.2 真实本地模型

1. 用 VoiceDesign 生成一个测试人物候选，确认实际模型精确为冻结的 1.7B VoiceDesign；
2. 同一任务卸载 VoiceDesign 并由 Base 合成不同文本试听，核对 WAV、非静音、模型 identity 和参考哈希；
3. 作者确认后绑定测试人物，生成至少两个句段；重启本地 TTS 服务后再次生成不同句段，音色仍可用；
4. 上传一段获授权测试录音，完成 Base 试听、确认、绑定和章节生成；
5. 取消、断连、磁盘不足、模型身份不符和 worker 崩溃均不得创建锁定版本或改变既有绑定；
6. 全程确认三个模型不同时常驻，16 GiB 机器不发生 swap；真实音频和参考录音不进入 Git。
7. 对全部可选官方音色逐一生成普通话固定样句；不能稳定输出普通话的音色不得进入可选目录。设计音色和上传复刻音色也必须通过普通话试听后才能锁定。
8. 用截图中的三类短句形状复测 Serena 和至少两个其他普通话官方音色，记录实际时长与门限；若仍失败，页面在终态后不再显示可重试，并能越过失败句继续播放后续 ready 音频。

### 7.3 桌面与发布

- 仅验收桌面 Web、桌面缩放和宿主侧栏挤压；不新增手机端门禁；
- 私人音色页可完成两条闭环，状态只局部刷新，章节已生成音频可继续播放；
- 失败详情的数量、可重试按钮和后端权威投影一致；当前三句终态不能继续提交同参数重试；
- 现有官方 9 音色、旁白选择、人物绑定、历史 Edition 和媒体摘要发布前后不变；
- “现有官方 9 音色保持”只适用于数据不删除；可选目录以普通话真实试听为门禁，未通过者保留历史数据但不允许新选用；
- 正式 `18088` 已在作者授权后完成数据库 dump、已安装 PawApp 树和 Skill 状态备份，并按公开插件升级路径安装；
- 未完成真实重启后章节生成，不得宣称 Base／VoiceDesign 产品闭环已可用。

## 8. 风险与恢复

- **音色漂移**：VoiceDesign 结果只作参考锚点；正式输出固定走 Base，锁定后不重设计。
- **冷加载较慢**：页面明确显示“正在加载声音设计模型／正在切换复刻模型”，不让多个模型常驻掩盖等待。
- **任务中断**：数据库任务和媒体哈希作为恢复事实；重试复用已发布参考资产，半成品保持不可用。
- **声音权利**：上传路径继续要求明确授权；合成设计路径要求确认“未刻意模仿可识别真人”，两类来源不得混用权利证据。
- **数据回退**：发布前保存数据库 dump 和旧插件树。若新生成音色已被绑定，不执行破坏性 downgrade；恢复使用同批 dump＋旧安装树，不删除模型、正文或媒体卷。

## 9. 完成标准

同时满足以下条件才算完成：

1. 私人音色页真实开放“文字设计”和“上传参考录音”；
2. 两条路径都经过 Base 的真实试听和作者确认后才能锁定；
3. 设计音色在本地服务重启后仍能用于新章节，不依赖临时设计 ID；
4. 人物绑定、章节更新和历史 Edition 保护通过；
5. 没有恢复任何 MOSS／Nano／VoiceGenerator／24 槽残留；
6. 官方、设计、上传复刻、试听和章节生成全部固定普通话，页面无方言入口，后端能拒绝绕过请求；
7. 失败重试完成后页面不会停留在旧投影；连续播放能明确跳过终态失败并继续下一段，生成中缺口仍等待；
8. 短中文音频失败具备足够但不泄露正文的诊断证据，质量门禁没有被无证据放宽；
9. 自动化、迁移、隔离、真实模型和桌面浏览器门禁全部通过；
10. 正式部署、Git 提交和 push 均只在作者分别明确授权后执行。

## 10. V0.7 人物音色生成优化计划（2026-09-14）

### 10.1 目标、状态与事实基线

状态：2026-09-14作者同意开发并要求页面生效后，V0.7.1已完成代码校验、G2备份／公开更新／本地TTS重载和页面核对；后续确认早于V0.7.1创建的既有候选可在桌面Edge完整播放，内置浏览器播放仍会崩溃。G3的新候选生成、作者听感、锁定／绑定与章节应用尚未闭合。先前必须先提交Git的说法属于旧发布脚本约束，并非公开API要求；本次以HEAD归档加明确增量及哈希固定来源完成发布，源码随后已由`32bea56`纳入并推送。发布范围只有前端产物和普通话校验模块，详见本轮记录。

W0冻结记录：保留既有未提交年龄补充与无关66／79／80改动。建议函数调用者为`narration/index.ts`和单元测试，结果消费者为`voice-source-workspace.ts`；新增可选`warning?: string | null`，null／缺省均表示没有待核对提示，及输入`characterName?: string | null`只用于核对明确主体、不推断年龄。现有四个结果字段不变。人物工作台新增可选`characterId/characterName/targetProfileId`，目标只能来自权威绑定或作者明确选择。公共API、版本DTO和schema不变。12项共享反例已冻结于`tests/fixtures/character_voice_design_cases.json`，A/B只读；新seed随一次候选请求快照保存，失败不得另造seed。

复查结论：保留原轻量范围，补齐设计音色可绑定、本地运行时同版校验、描述原文不可恢复及未知请求恢复边界；收紧资料提取为保守的直接证据规则，不建设通用语义解析器。P0/P1先完成，P2在同一候选中随后完成，不先行发布有已知绑定缺口的半条链。

目标：依据人物资料给出可信、简短、可编辑的普通话音色描述；能够成功生成，候选归入正确档案，作者能听清区别并明确采用。继续使用 VoiceDesign 生成耐久参考、Base 复刻试听与章节的现有流程。

已核实事实（2026-09-14，工作区代码复查及纯函数复现）：

| 编号 | 问题 | 复现或代码证据 | 优先级 |
| --- | --- | --- | --- |
| F01 | 自动描述被自身校验拒绝 | 描述包含“不使用方言”，`require_mandarin_voice_design` 只按子串拒绝；请求 schema 和应用服务均调用该函数 | P0 |
| F02 | 年龄主体与听感混淆 | “父亲四十岁”被提取为本人实际年龄；“声音清亮，像溪水一样。父亲四十岁”触发听感覆盖 | P1 |
| F03 | 年龄表达失真 | “18至22岁”变成22岁，“不到十八岁”变成十八岁；18岁／十八岁被判为不同听感 | P1 |
| F04 | 音质误取与长期化 | 眼睛明亮、皮肤干涩、不要过于沙哑、哭过后暂时沙哑均可被写入固定音色 | P1 |
| F05 | 档案目标不够明确 | 人物入口列出作品全部音色，未命中选择时默认首项；切换档案重填当前人物描述 | P1 |
| F06 | 提示与实际输入可能脱节 | 建议由 props 计算，输入保存在组件状态；未编辑／已编辑、人物资料变化及入口差异缺少统一刷新语义 | P1，具体交互待复验 |
| F07 | 试听与变体不便判断 | 默认试听只有简短介绍；创建候选提交 `seed=null`，后端默认1234，未区分主动换声和失败重试 | P2 |
| F08 | 设计完成后可能无法在人物面板选用 | `narration/index.ts`给绑定面板传入`allowedSourceTypes: ["uploaded"]`；`character-voice-panel.ts`还按来源映射能力 | P1，来源过滤已确认，完整绑定链待复验 |

本轮新增事实：`tts_runtime/service.py`在Mac本地进程内再次调用同一普通话校验函数；只更新PawApp不能证明该常驻进程已加载新规则。`qwen_voice_product.py`在参考资产发布后删除`design_description`明文，前端版本DTO也没有原描述或seed；不能据`description_available`恢复原文，也不能要求前端从该DTO读取seed。

现有相关 Vitest 18项通过，但F01—F04反例仍可复现，证明覆盖不足。本轮没有执行真实声音生成；不能把上述代码复现写成新的正式音质验收。源代码重点：`character-voice-design-suggestion.ts`、`voice-source-workspace.ts`、`narration/index.ts`、后端`contracts.py`／`schemas.py`／`qwen_voice_product.py`。

### 10.2 最小范围与行为决策

#### A. 先打通普通话描述提交

- 自动模板采用正向表达“标准普通话”。后端保留唯一语言值`zh-CN`，允许明确禁止方言／口音的描述。
- 按局部短句识别肯定、否定及转折，不因一句“不使用方言”放行后面的“但要四川口音”；未知或冲突表达明确提示改写，不能静默去词后继续提交。
- 规则只针对有界、明确的语言要求，允许“不要播音腔”“声音不像中年人”等合法声音描述；不泛化为禁止所有否定词。复杂语言要求无法确定时给出具体冲突短句和修改建议，不用继续叠加无限关键词来假装理解语义。
- 不新增通用自然语言分类服务、不调用聊天模型做语言校验。复用后端唯一校验函数，核对 schema、服务和 worker 的一致性；前端只提供及时提示，不另建一份业务裁决。
- 增加真正的跨层代码校验：将前端函数实际产出的描述送入真实 Pydantic 请求模型及后端校验函数；禁止仅使用手写替代描述或 mock API 成功返回。

#### B. 人物资料转声音描述

- 数据优先级：统一读取已保存人物年龄、性别、小传与性格；小传仅补充明确属于本人的年龄或声音事实。人物卡未保存编辑不自动用于设计，复用既有保存操作并提示“保存人物资料后可更新声音描述”，不增加“使用未保存事实”的第二套流程。声音描述本身仍可自由编辑后提交。
- 实际年龄、声音听感年龄分开。默认由实际年龄决定听感；只有明确属于本人、肯定且长期的声音年龄设定才覆盖。明确声音性别与人物性别有差异时也保留作者的声音设定；无法判断归属或有冲突时简短提示作者核对。
- 识别边界以分句、主体和直接声音关系为准，不再用跨句的固定字符窗口把“像”和另一个年龄拼起来。复杂叙述无法确定时不自动采用该条，可手工编辑；不承诺理解所有小说语义。
- 小传只识别本人开篇年龄的直接陈述、连续分句中无主语切换的明确声音描述；新主语、回忆、假设、转述或矛盾出现时终止自动继承。不能靠排除“父亲”等少数亲属词代替主体归属。仅用开篇资料生成固定声音，不按后续章节年龄自动换声。
- 统一中文数字／阿拉伯数字的比较，保留“18至22岁”“不到十八岁”“二十多岁”等原有范围／限定。“不详，待补充”仍属于未知；幻想种族的长寿设定不截取尾数或自动转换成人类老年声。
- 未知实际年龄时仍可使用作者明确写出的声音年龄／年龄段，标注实际年龄未明；两者都不明确则不猜年龄，保留已确认的声音特征，允许作者编辑后生成。
- 固定音质只取明确修饰声音的长期描述；外貌、他人声音、否定项、临时感冒／哭泣／疲惫不固化。性格可轻量影响表达方式，但不能擅自推出年龄、音高或方言。
- 不从角色名、职业或“沉稳、威严”推断年龄；同一年龄、同一性格不人为保证声音必然不同。无区别证据时由作者试听后选择另一版。
- 最终描述按“实际年龄背景 → 目标声音听感 → 稳定音质 → 简短表达方式 → 标准普通话”组织，通常80—180字，硬上限沿用500字。只输出已知项；年龄优先通过明确目标表达，不堆叠相互竞争的“第一优先级”。
- “通常80—180字”是简洁目标，不是最低字数要求。资料充分性不足时允许更短。不要整段拼入性格原文；只选少量与表达直接相关的已知词。未采用的矛盾／不明确声音事实用一条简短提示呈现，不能默默变成确定设定。

冻结示例：

> 人物实际18岁；声音听感明确接近40岁的成年男声，沙哑、粗粝，这是人物固有的声音反差。标准普通话，自然对白，吐字清楚。

普通18岁且仅“声音沙哑”的人物应保留青年听感。既有私人音色页自由编辑入口保留，不强迫所有自定义声音都绑定人物后才能创建。

#### C. 档案对应与描述更新

- 人物入口优先读取现有权威绑定对应的`profile_id`；没有私人绑定时显示“为某人物新建声音”，不默认把作品首个音色作为写入目标，不按姓名相似度推断关联。
- 显示一行“当前人物／目标音色档案”。已有档案可明确选择复用；在其他档案创建候选前能看清影响对象。继续按既有版本和CAS校验提交。
- 不新增人物草稿关联表：未锁定候选通过明确档案选择及现有档案列表找回，不能声称刷新后会自动认领无绑定候选。
- 按人物和档案隔离输入及异步结果，切换人物不串入另一人的描述。建议更新仅自动填充未编辑内容；已有手工修改时显示“按最新人物资料更新描述”，由作者点击替换，不用重渲染覆盖输入。提示从当前应用的建议派生，不把已修改内容继续标成已遵循原规则。
- 人物卡、朗读人物配置共用同一建议函数和同一工作台；核对年龄、性别、性格、小传的实际字段映射。通用私人音色入口无人物上下文时不宣称自动按人物生成。
- 锁定与绑定继续是清楚的作者动作，复用现有按钮与流程；不增加新的设置页或多层确认弹窗。
- 补齐`generated`来源的绑定入口：设计中要求VoiceDesign能力，已有耐久参考的锁定声音按Base参考复刻能力判断可用性，不因当前未加载VoiceDesign而从人物列表消失。上传／设计均继续通过授权、参考资产、质量确认、版本状态及作品范围检查，不能仅扩大来源白名单就认为闭环完成。
- 一个音色档案可能被多个人物明确复用：新建／锁定候选不得批量改绑这些人物，复用现有影响范围提示；仅作者当前绑定操作生效，历史版本保持。声音源于上传录音的档案也不能因打开某个人物而自动转换来源。

#### D. 试听和“另一版”

- 默认试听改为一段自然普通话对白，包含陈述和疑问，约50—80字；例如“门外的脚步停了下来。你听见了吗？先别开门，让我看看走廊。等灯重新亮起，我们再一起往楼下走。”作者可编辑，已生成音频重复试听直接播放。
- “重试”继续使用原候选、seed和已发布参考资产；“设计另一版”明确创建新候选，使用新的seed，保留旧版本。seed不暴露成普通作者的技术参数。
- 复用现有请求的可选seed字段及版本持久化，保持`null`的既有调用兼容。仅主动创建新候选时生成一次非负31位整数seed，与描述、目标档案、CAS基线和幂等键组成同一请求快照；点击瞬间即阻止重入，不能等下一次渲染才防双击。同页断网重发复用快照，重试已存在候选由服务端读取版本参数，前端无需新增seed查询字段。
- 页面刷新后先读取档案与版本状态，找到已创建候选则继续该候选；不能确认上次是否成功时保持“结果待核实”，允许刷新状态但不自动重发创建请求或宣称失败。无需为此新增持久任务表或在浏览器保存完整人物描述；避免承诺对未知请求自动恢复到原始表单。
- 为让刷新后仍知道存在未决请求，仅在当前标签页会话存储保存作品／人物／档案ID、提交前版本号和请求标识，发送前记录，确认结果后清除；不保存描述、正文或录音。存储不可用时明确降级为恢复服务端候选列表，不保证恢复请求关联。多个新增候选无法唯一归属时由作者选中已有候选或明确创建新候选，不按名称／时间猜测，不无限轮询；复用现有列表与创建按钮。
- 设计参考完成后服务端按既有策略清除描述明文。本页仍有作者输入时可复用来设计另一版；刷新后原描述不可读时，明确提示按最新人物资料重新整理或手工填写，经作者核对再生成，不伪称还原旧参数／同描述。已生成的参考、试听及版本仍可恢复。
- 每次只生成一个候选，不批量造声。作者听到的是Base最终复刻试听；修改描述后必须创建新候选，不能把旧试听当成修改后的声音。
- 试听文本改变只生成同一候选的Base试听，不重跑VoiceDesign；现有试听资产仍可用时直接播放，失效或删除时显示真实状态并明确触发重新生成，不能承诺预览永久有效。新seed仅提供另一种生成结果，不保证听起来一定更好或明显不同。

### 10.3 范围约束

不增加模型、付费语义调用、运行时依赖、数据库表、schema迁移、任务队列或通用声音池；不改QwenPaw核心、官方音色资产、云端渠道配置或章节生成调度。复用现有范围权限、声音版本、媒体、幂等和Base参考复刻契约。

本次“自动”指自动整理可编辑的声音描述，不扩大为每章自动设计、批量人物造声或自动锁定绑定。临时情绪识别与章节表演调度后置；本轮仅防止它们污染固定音色。旧历史说明／迁移／证据保留，确认无独立覆盖的冗余实现和测试才在所属文件内合并或删除。

### 10.4 子代理并行施工设计

本轮规划不启动子代理。进入已批准施工后，先冻结接口，再按下表派发；工作包仅属于本节。主代理是唯一集成责任人。文档、共享fixture、入口、后端服务以及唯一正式环境均单一所有者，避免多个包修改同一文件。

共同只读范围：本仓库当前人物DTO、声音API／worker／媒体、对应测试和本计划。所有包禁止触碰QwenPaw上游、旧项目Data、密钥、无关脏文件、迁移历史、正式数据库；子代理不得部署、调用真实模型、暂存、提交或推送。

| 波次／ready set | ID／标记 | 唯一目标与非目标 | 精确写入所有权 | 依赖、验证与返回证据 |
| --- | --- | --- | --- | --- |
| W0 | Q76V7-C／SER／GATE | 冻结建议结果、人物来源、目标档案选择和seed语义；不实现模型新能力 | 本计划、两级文档索引；新建`tests/fixtures/character_voice_design_cases.json` | 主代理记录基线diff、反例、现有绑定字段和冻结契约；未冻结不派发W1 |
| W1 ready={A,B} | Q76V7-A／PAR | 修复本人年龄／听感／稳定音质提取；不改页面和API | `frontend/src/narration/character-voice-design-suggestion.ts`及其`.test.ts` | C完成；读取冻结fixture；相关Vitest；返回输入输出、遗漏边界、diff与命令结果 |
| W1 ready={A,B} | Q76V7-B／PAR | 修复普通话否定／转折校验；不放宽语言范围或改请求结构 | `backend/narration/contracts.py`、`tests/narration/test_qwen_tts_provider_contract.py`；新增`tests/narration/test_voice_design_language.py` | C完成；相关pytest及真实schema校验；返回接受／拒绝矩阵、兼容影响与diff |
| W1主代理并行、W2汇合 | Q76V7-U／PAR-C／SER／MUTEX | 完成档案目标、已保存资料接线、设计音色绑定、试听与另一版；不改A/B文件 | `frontend/src/narration/voice-source-workspace.ts`、`voice-source-panel.ts`、`character-voice-panel.ts`、`index.ts`及各自`.test.ts`；`reading-page.integration.test.ts`；`frontend/src/characters/character-workspace.ts`及其`.test.ts`；`frontend/src/workbench-studio.ts` | C完成后主代理只在冻结接口上施工；最终接线等A/B；前端定向回归、跨人物及刷新恢复证据 |
| W2 | Q76V7-I／INT／SER | 跨层描述提交与版本参数复核；不重复领域逻辑 | 新增`tests/narration/test_character_voice_design_handoff.py`；必要时`backend/narration/qwen_voice_product.py`及`tests/narration/test_qwen_voice_product.py`；上述fixture仅主代理更新 | A/B/U完成；真实执行前端建议函数→Pydantic请求→后端校验，不连生产库；全量回归、类型、构建及打包 |
| W3 | Q76V7-R／GATE／MUTEX | 备份、公开更新、真实桌面及声音链验证 | `audit/qwen-tts/plan76/character-voice-v07/`及本计划／索引 | I通过；正式环境无冲突操作；记录精确包、恢复点、实际音频和听感结论；Git依作者明确要求 |

冻结接口：建议内部结果保留`description`、`ageEvidence`、`perceivedAgeEvidence`、`ageSource`；允许`ageSource=missing`且`description`非空，表示缺少实际年龄但存在可用声音证据。C须在派发前列出全部调用者并冻结所需可选提示字段及其null语义，不能派发后让A自行加字段。公共HTTP请求仍为现有`description/language/seed/expected_profile_version`，不增新API；版本DTO不增加描述明文或seed读回字段。工作台可加可选人物范围／目标档案props，但以实际绑定ID为准，既有通用入口保持兼容。fixture由C/I单一所有者维护，A/B只能读取并回报新增案例需求。

资源锁：`narration/index.ts`、工作台、人物入口、共享fixture、Git集成归主代理；正式18088、TTS模型切换、数据库备份与更新为独占MUTEX。汇合顺序`C → (A∥B∥U) → I → R`；冲突文件转主代理串行处理，不让子代理扩大写入范围。若可用并发不足，沿同依赖顺序串行，验收标准不变。

### 10.5 验收与退出门禁

G1：代码矩阵必须覆盖以下输入，断言目标行为而非仅“函数成功”。

| 类别 | 必需案例与预期 |
| --- | --- |
| 普通话提交 | 自动模板可过真实schema和服务校验；“不使用方言”允许；“不要普通话”“不用方言但要四川口音”拒绝；语言非zh-CN仍拒绝 |
| 年龄事实 | 精确年龄、中阿数字同义、区间／上限、未知、超过120岁的虚构人物保留语义；父亲年龄／回忆中的年龄不冒充本人开篇年龄 |
| 声音反差 | 18岁沙哑仍年轻；明确像40岁保留反差；否定／跨句他人／同龄同义不误触发；实际年龄未知但明确青年听感可生成建议 |
| 稳定音质 | 眼睛／皮肤形容不入音质；临时哭泣／感冒不固化；否定及“不是A而是B”正确处理；明确长期粗粝保留 |
| 页面与档案 | 无私人绑定不选首项；甲乙切换不串描述／异步结果；手改不被刷新覆盖；候选保存前明确目标；通用入口不冒充人物入口 |
| 绑定与共享 | generated锁定后在人物入口可见；VoiceDesign未加载但Base可用时仍能选用；未锁定／缺参考／授权失效不可绑定；共享档案新候选不批量改绑 |
| 候选与恢复 | 原候选重试不换seed；另一版创建新版本；同步双击／断网不重复造声；刷新恢复已存在候选；未知结果不自动重发；旧描述清除后不伪装还原；新描述不冒用旧试听 |

代码命令：使用已解析的Node24及项目pnpm执行`pnpm exec vitest run frontend/src/narration/character-voice-design-suggestion.test.ts frontend/src/narration/voice-source-workspace.test.ts frontend/src/narration/voice-source-panel.test.ts frontend/src/narration/index.test.ts frontend/src/narration/reading-page.integration.test.ts frontend/src/characters/character-workspace.test.ts`；后端执行`.venv/bin/python -m pytest tests/narration/test_voice_design_language.py tests/narration/test_character_voice_design_handoff.py tests/narration/test_qwen_tts_provider_contract.py tests/narration/test_qwen_voice_product.py`。跨层测试须解析真实Node可执行路径，缺失时记为未验证而非假通过。集成后按跨层变更运行`.venv/bin/python -m pytest`、`pnpm typecheck`、`pnpm test`、`pnpm build`及`.venv/bin/python scripts/package_plugin.py`，最后`git diff --check`。

V0.7.1增加`pnpm exec vitest run frontend/src/narration/character-voice-panel.test.ts`，覆盖设计来源选择与Base能力判断；跨层校验除源码函数和schema外，还核对实际提交请求使用同一描述。新建测试文件命令是施工后的验收入口，本次文档复查不声称文件已存在或测试已执行。

G2：候选通过代码校验后，按项目公开更新路径集中安装正式18088。事前核实实际宿主／schema／TTS健康、活动生成任务、精确源差异，备份数据库、安装包及必要配置，记录包哈希与回退命令。保护当前工作区已有年龄补充与其他任务改动，发布包只含明确纳入本节的增量和既有正式基线。不因便于检查而重启；公开更新失败、数据库连接不足或scope检查异常立即中止并诊断，不能只凭健康恢复判为功能通过。

Mac本地TTS与容器PawApp共用源码规则但进程独立，G2必须核对本地进程实际代码路径、加载版本及启动方式。若常驻进程仍使用旧校验，等待无活动生成任务后，通过已有受管服务更新方式进行一次必要的TTS进程重载；事前说明对象、触发原因、旧代码／启动配置恢复点及中止条件，不顺带重启Docker、换模型或改变保活配置。回退同时核对本地校验版本，不能只恢复容器包。若启动方式无法加载精确候选，先记录部署差异并在本节复核，禁止直接覆盖运行副本凑结果。

G3：从《缺氧：末日地下世界》的陈屿正常创作需要出发，先复核其最新已保存年龄，不把本次检查时的26岁写死为未来事实。实际从人物入口生成匹配年龄的声音描述，未经手工删改模板即可提交，完成VoiceDesign参考→Base试听→浏览器播放；核对请求／实际模型、参考哈希、候选目标及刷新后可播。原则上只生成这一份正常创作所需候选；年龄反差语义用作者给定案例做完整代码校验，实际音质只有作者要为该设定创建声音时再验证，不为覆盖案例给陈屿新建不符合人物设定的声音。甲乙档案切换只读复查可用既有人物，不额外造声。

仅在作者确认听感并采用后验证锁定、明确人物绑定以及新朗读版本中的对白；已有采用授权继续有效，作者未确认听感时如实标注待确认，不代替作者判断。新版本至少验证两段不同文本，既有音频继续可播。作者觉得“仍像中年”“粗糙过重”等时修改描述后显式生成另一版，每次一份，验证新seed与旧候选保留。断网／崩溃的确定性错误矩阵在代码层验证，正式环境只走正常刷新与恢复，不制造生产故障。

桌面检查按实际视口、缩放、宿主侧栏挤压和键盘完成，重点检查输入不丢、人物目标清楚、进度局部刷新、试听不重复启动模型。证据保存至本节指定audit目录，音频仍在现有媒体存储，不入Git。

退出结论分别记录“代码通过／正式提交生成播放通过／人物绑定和章节应用通过／作者听感已确认”，不能以测试数量代替音质结论。提示词改善不能保证模型精确呈现某个年龄，必须给作者保留试听和另一版选择。

不得因尚未试听的另一种人物设定而无限增加模型调用；年龄反差真实听感和主动另一版效果若未实测，单列未验证，不将其写为通过。作者已确认的声音可继续走绑定和章节应用，避免把所有可继续的工作都停在听感确认上。

### 10.6 恢复与交付

本节无schema变更。发布失败优先恢复旧PawApp包并核对配置，保留期间创建的候选、参考资产与审计记录；不自动用旧dump覆盖更新后的正式数据。音色效果不满意时继续使用原锁定版本，作者通过正常界面恢复原人物绑定；历史Edition保持可播。

交付包含本节问题关闭表、变更文件、反例与跨层校验结果、正式发布包／备份、真实试听和采用状态。P0/P1全部关闭且G1—G3对应环节有证据后再宣称本次优化完成；未做的作者听感判断或章节采用明确列出。提交／推送不作为默认施工步骤。

### 10.7 本轮施工补记（2026-09-14）

C→A/B/U→I已完成代码汇合。F01—F08均有代码修复与针对性覆盖，G2已完成，G3仍待验证：人物资料读取已保存值；无私人绑定不默认首档；手改描述按档案保留；设计来源进入人物绑定且已有参考走Base；未知创建结果不自动重放；当前标签页仅保存范围／请求／版本标识，收到确切候选ID后可恢复，无法关联时作者核对。另一版换seed、原请求重试复用快照，锁定不批量改绑。已存在的试听可播放后直接确认锁定，不必重新生成。

细化恢复边界：不凭新增候选数量或时间猜测请求归属；有响应返回的versionId才自动对应恢复，否则保留“结果待核实”。刷新不恢复被服务端清除的描述明文，页面如实提示使用最新人物资料或手工填写。可选列表默认展示档案最新候选，不修改`current_version_id`、任何人物绑定或历史Edition。

代码校验、正式生效及后续既有试听播放结果见[证据记录](../../audit/qwen-tts/plan76/character-voice-v07/README.md)。正式人物卡按保存资料生成“实际26岁；保持青年年龄感，男声”的普通话描述；原档案、绑定和正文未改。未调用模型、新建／锁定／绑定声音或生成章节。既有候选在桌面Edge完成6秒播放，Codex内置浏览器仍会在播放时崩溃；该候选早于V0.7.1创建且作者尚未确认听感，因此不宣称新声音链、音质或人物采用已经通过。

## 11. V0.8人物声音页状态一致性与减重计划（2026-09-14）

状态：**已获作者批准并完成代码校验、公开热更新和正式桌面只读验收。** 本节只优化人物卡“声音”页的前端表达与交互，不重做朗读设置，不改变现有TTS、音色、绑定、Edition或正文。实施及恢复证据见[V0.8记录](../../audit/qwen-tts/plan76/character-voice-v08/README.md)。

### 11.1 已核实问题与设计判断

1. `dedicated`在现有契约中表示“人物明确绑定某个声音”，可指向官方预设、上传复刻或文字设计音色；当前页面却固定翻译为“使用专属声音”。因此陈屿实际使用官方`Serena`时，下方仍选中“使用专属声音”，这是**前端文案和展示模型错误**，不能据此认定数据库绑定错误。
2. 私人区把“创建／维护音色档案”和“把已锁定音色应用给人物”同时展开。未选私人档案时仍展示声音策略、禁用的锁定版本和影响预览，导致用户在条件尚未满足前就看到后续表单。
3. 已有“陈屿｜冷静坚韧男声 · 待完成”藏在下拉框里，而新建名称和“创建音色档案”直接可见，容易重复建档。
4. 官方音色采用“选中即保存”，私人绑定采用“最后保存”；两者技术上成立，但页面没有把“实际已生效”和“未保存草稿”清楚分开。
5. “私人音色”“我的声音”“当前作品专属”“声音策略”“锁定音色版本”等标题和卡片层级重复；底部“声音设置由共用声音组件独立保存”是工程说明，不帮助作者决策。

### 11.2 目标页面与最短任务路径

页面只保留一个始终可信的顶部摘要：`当前实际声音：名称｜来源｜普通话｜状态`。该摘要只读服务端已保存绑定，不被未保存选择提前改写。发生草稿修改时，在操作区显示“尚未保存”，而不是改变顶部“正在使用”。

官方与私人声音继续使用现有两个折叠区，不增加新页面、向导或弹窗：

- **浏览官方音色**：保留现有目录和试听，标题明确“选中后立即生效”；试听仍不换绑定，选择成功后刷新顶部实际声音。
- **定制私人音色**：标题改为“定制私人音色”，副说明为“设计、试听、确认后再应用”；去掉“高级调音”这一过宽说法。
- 私人区保持四个线性步骤：`选择档案 → 设计或上传 → 试听并锁定 → 应用到当前人物`。未到达的步骤不展示无效表单，只显示一行条件说明。
- 存在未完成档案时，直接显示一行“继续完成 陈屿｜冷静坚韧男声”，由作者显式点击选择；不自动选作品第一份档案、不按姓名猜关联。只有“新建另一个音色”展开后才显示新建名称输入框。
- “当前音色”字段改名为“正在编辑的私人音色档案”，避免与顶部实际声音混淆。
- 只有选中了**已锁定且可绑定**的私人版本，才显示“应用到陈屿”和影响范围；保存按钮紧邻影响说明。创建档案、生成候选、试听、锁定均不得自动绑定人物。
- “跟随作品规则／暂不单独配置”保留为私人区底部的“其他使用方式”，避免丢功能；当前为官方声音时，不再把它显示成“使用专属声音”已选中。
- 删除禁用状态下显示官方音色的“锁定音色版本”死表单；官方声音只能从官方区更换，私人区只列可用私人版本。
- 删除人物卡底部工程文案；改为面向作者的保护说明“已有朗读不会改变，主动更新朗读后新声音才用于受影响句段”，仅在准备保存绑定时显示。

### 11.3 状态与保存规则

新增的只是前端展示状态，不改变公共DTO：

| 页面状态 | 顶部实际声音 | 私人区主要动作 | 保存语义 |
| --- | --- | --- | --- |
| 官方声音已绑定 | 显示官方名称与“正在使用” | 继续已有档案／新建设计 | 官方选择立即生效；私人流程不改绑定 |
| 私人档案待完成 | 实际声音保持原值 | 继续设计或上传 | 不显示人物绑定保存 |
| 私人版本待确认 | 实际声音保持原值 | 试听、确认锁定 | 锁定只改变音色档案，不改人物 |
| 私人版本已锁定 | 实际声音保持原值 | 选择“应用到当前人物” | 显示影响并显式保存 |
| 绑定草稿未保存 | 实际声音保持原值 | 保存／撤销草稿 | CAS成功后才更新顶部摘要 |
| 跟随／暂不配置 | 显示服务端实际状态 | 明确选择另一使用方式 | 显式保存，不删除音色资产或历史Edition |
| 不可用／冲突 | 显示真实错误或恢复状态 | 刷新当前绑定 | 保留草稿，不猜测或静默覆盖 |

冻结约束：`binding_policy`、`profile_id`、`version_id`、CAS版本、幂等语义、官方音色立即选择接口全部保持；不新增HTTP字段、数据库字段、迁移或第二套状态。不得自动选择第一份私人档案、自动锁定、自动绑定、批量重生成或修改历史Edition。

### 11.4 精确施工范围与子代理设计

本任务**不并行、不启动子代理**。原因是改动小且状态、渲染、样式和测试围绕同一组共享组件，拆分会让多个所有者同时改相同文件，增加状态漂移和冲突风险。主代理是唯一实现、集成和正式更新责任人。

共同只读范围：声音DTO、现有API适配、计划44／76／77与相关测试。禁止触碰QwenPaw上游、后端契约、数据库、迁移、模型、云端渠道、音频资产、正文、无关脏文件和旧项目`Data`。

| 波次 | ID／标记 | 目标与非目标 | 精确文件所有权 | 门禁与证据 |
| --- | --- | --- | --- | --- |
| W0 | Q76V8-C／SER／GATE | 冻结实际状态、草稿状态和文案映射；不写功能 | 本节及两级文档索引 | 列出全部入口、当前绑定组合和不可变API；未冻结不进入W1 |
| W1 | Q76V8-U／SER／MUTEX | 状态一致性、四步私人流程、去重文案和布局；不改后端 | `frontend/src/narration/character-voice-configurator.ts`、`character-voice-panel.ts`、`voice-source-workspace.ts`、`index.ts`、`styles/t2-c.ts`、`styles/t2-d.ts`；必要时`frontend/src/characters/character-workspace.ts` | 只复用现有组件和Token；逐文件diff复核 |
| W2 | Q76V8-T／SER／INT | 覆盖状态矩阵、键盘焦点和三个挂载入口；不造声音 | 上述模块对应`.test.ts`、`reading-page.integration.test.ts`、`reading-accessibility.test.ts`及必要的人物工作台测试 | 定向Vitest→全量前端→类型→构建→打包→`git diff --check` |
| W3 | Q76V8-R／SER／GATE／MUTEX | 备份、公开热更新和正式桌面复核；不调用模型或改绑定 | 新建`audit/qwen-tts/plan76/character-voice-v08/`，本节与索引 | 精确包、回退点、正式页面截图、健康与配置保态；Git仍需作者另行要求 |

### 11.5 验收标准

自动化至少覆盖：官方／私人／继承／未配置／不可用五种实际状态；零份、一份和多份私人档案；未完成、待确认、已锁定版本；选择官方立即成功／失败；私人草稿保存／撤销／CAS冲突；人物切换不串状态；折叠区首次挂载和再次展开保留输入；同一操作不重复提交。

正式桌面只做不改变创作数据的页面核对：人物卡声音入口和朗读人物配置入口显示一致；当前官方声音时不再出现“使用专属声音已选中＋尚无私人音色”的矛盾；已有未完成档案可一眼找到并继续；影响说明只在准备应用时出现；关闭、Escape、Tab顺序、焦点返回、页面滚动、常用桌面窗口缩放及宿主侧栏挤压正常。浅灰说明文字需达到可读对比，图标关闭按钮须有可读名称和足够点击区域。

退出条件：P0状态矛盾关闭，私人音色主路径无需理解`dedicated`或`Edition`等内部概念，所有现有功能仍可达；正式包与代码候选一致，Agent模型／Skills／工具、TTS健康和既有绑定保持。由于本节不调用模型，不能把页面通过写成试听、音质或声音设计全链通过。

### 11.6 发布与恢复

本节预期只有前端产物变化，不需要数据库迁移、本机TTS重载、Docker重启或新模型下载。施工获批后先冻结当前候选和无关脏文件，代码校验通过再备份正式插件并使用公开更新路径热装；出现包差异超范围、健康下降、配置漂移或活动生成冲突立即中止。回退只恢复本节更新前插件包，不覆盖数据库、不删除期间产生的声音或朗读数据。

实施结果：最终候选与正式插件相比恰好只有`frontend/dist/index.js`变化，SHA-256为`2fe4278a442e36974a8d1330ac4732b3b6a8d0bb7a77896549eac4f32655298c`。最终更新前即时恢复点为`/private/tmp/plan76-voice-v08-final-psfGKU`；完整回到V0.8施工前基线可使用首轮备份`/private/tmp/plan76-voice-v08-cG9sPV`。两次公开热更新均未重启Docker或本机TTS；首轮安装后首次立即查询尚未达到完整ready，随后只读健康查询恢复`ready`并通过精确验证，最终候选安装后首次精确验证即通过，未触发回退。

正式人物卡“陈屿→声音”只读核对通过：顶部继续显示服务端实际绑定`Serena｜温暖女声 · 官方音色 · 普通话`；官方区明确“选中后立即生效”；私人区显示四步流程及“继续完成 陈屿｜冷静坚韧男声”；未锁定档案不会出现可保存的人物绑定；旧“使用专属声音”与工程说明已移除；弹窗位于桌面视口内且无横向溢出。只选择未完成档案以检查本地表单后关闭人物卡，没有生成、试听、锁定、保存或改动正式数据。

### 11.7 V0.8.1交互收尾（2026-09-14）

作者同意V0.8复查确认的两个小缺口后，本轮继续沿用本节“不并行、单一所有者”的范围完成收尾，不新增计划、DTO或状态机：

1. 未明确选择可应用私人音色前，不再提前显示编号第4步。零可用版本显示完成条件；已有锁定版本但尚未选择时先显示无编号选择提示，选中后才进入`4. 应用到人物`、影响核对和保存。
2. `继续完成`会在状态提交后把焦点移入实时状态区，避免原按钮禁用后焦点落到页面根节点，使紧接着按`Esc`仍能由人物卡弹窗处理。

定向32项、全量前端170文件／1618项、类型、构建、打包及差异检查通过。最终包哈希为`4a8b436025732bd961670b3bc6e02405f36de250d071f54c0265d35fad91a930`，正式更新即时恢复点为`/private/tmp/plan76-voice-v08-1-final-QkmIyD`。正式桌面复验确认陈屿未选私人音色时无第4步、继续后焦点位于弹窗状态区、紧接着`Esc`可关闭、横向溢出为0，实际绑定仍是Serena。未试听、生成、锁定、保存或改绑定。完整证据见[V0.8.1记录](../../audit/qwen-tts/plan76/character-voice-v081/README.md)。
