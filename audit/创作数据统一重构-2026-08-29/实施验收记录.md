# 创作数据统一重构实施验收记录

日期：2026-08-29（Asia/Shanghai）

状态：**源码、实验数据库重建、长期 PawApp 升级与真实三视口验收已完成；按用户要求未调用真实百炼接口。**

## 1. 本次完成范围

- 新增正式大纲、故事设定、人物根与人物实例的不可变 revision 和 CAS 服务。
- 新增单／多时间线、显式链接、分支、循环、汇合、正文 revision 时间线映射及确定性故事状态投影。
- 将人物、关系、故事线、伏笔、故事时间、知识和世界状态统一到 `StoryFact v2`，保留候选确认和来源有效性边界。
- 新增私有素材不可变 version、小说固定版本绑定及生成输入快照。
- 新增 `NovelContextEnvelopeV3` 只读组装，按正式规划、时间线／人物实例状态、关系／线路／伏笔／知识、固定私有素材和语义证据排序。
- 新增阿里云百炼 DashScope Native adapter、write-only SecretStore、candidate generation、小说级 consent、V1/V2 分语料 renderer、混合检索和泄漏过滤。
- 新增 PawApp 全局“向量模型接入”页、小说内“语义索引”卡片，以及仅在第二条活动时间线出现后显示的时间线／人物实例工作区。
- 删除生产样书关键词推断、GET 写故事线／伏笔、非空正文强制至少一项情报、按姓名猜测结构化实体等权威硬编码。

## 2. Schema 与隔离数据库证据

迁移链保持唯一线性 head：

```text
20260828_0024
  -> 20260829_0025
  -> 20260829_0026
  -> 20260829_0027
  -> 20260829_0028
```

在一次性隔离 PostgreSQL 数据库中实际验证：

- `base -> 20260829_0028`：通过；
- `20260829_0028 -> 20260829_0024`：通过；
- `20260829_0024 -> 20260829_0028`：通过；
- 最终 `alembic current`：`20260829_0028`；
- 验证后删除该一次性隔离数据库。

没有修改历史 migration。长期数据库在仓库外完成全量备份后已重建并升级到 `20260829_0028`；没有继续降到 `0024` 以下，因为历史 `0009` 已明确为单向迁移，本次不改写历史恢复边界。

## 3. 自动化验证

已通过：

- 隔离 PostgreSQL 领域集成：`tests/test_domain_integration.py`，25 项通过；
- 向量专项：`tests/embedding`，59 项通过；
- 创作权威、时间线、私有库、V3 上下文、schema、只读安全、QwenPaw 契约及受影响后端专项回归通过；
- 前端 Vitest：92 个测试文件、829 项通过；
- `pnpm typecheck`：通过；
- `pnpm build`：通过；
- `docker compose config --quiet`：通过；
- `.venv/bin/python scripts/package_plugin.py`：通过；
- `git diff --check`：列入最终交付前复核。

全量 `.venv/bin/python -m pytest -q` 最终结果：`2973 passed, 127 skipped, 2 warnings`。施工中发现并修复了两类跨专项真实契约漂移：TTS 崩溃恢复测试改为显式注册测试专用 job kind；写作评测的冻结 B 契约改为校验 manifest 中的冻结 hash，不再错误要求持续演化的 live Skill 永久等于旧基线。

## 4. 浏览器与运行环境

已把新包安装到长期 QwenPaw 2.1.0 并重启为 `runtime=true / product=true / validation=false / reference=false`。安装后健康检查为 `ready`，PostgreSQL 18.6 已连接，narration production runtime、sidecar 与 worker 均 ready；TTS 处于计划 30 允许的按需卸载态（`model_loaded=false`、`worker_generation=null`、正数 lease generation），未为了验收强制加载模型。

真实安装包完成以下只读浏览器检查，检查后恢复视口并关闭临时标签页：

- 1920×1080：向量接入页、小说语义索引卡片、多时间线工作区均可见，`body` 无横向溢出；
- 2560×1440：同上，无横向溢出；
- 390×844：宿主进入窄屏导航，三个新页面均保持关键控件可见，无横向溢出；
- 单线作品显示“单时间线默认”，普通写作无需额外选择；
- 多线作品显示“多时间线模式”、主时间线、第二次回环和本线人物实例；
- 小说语义索引卡片保持未授权，复选框未勾选，授权按钮 disabled；
- 全局页正确显示 `qwen3.7-text-embedding`、1024 维、DashScope Native 和 write-only API Key；没有点击“测试连接”；
- 浏览器控制台没有 PawApp error；仅观察到宿主既有 `[moduleRegistry] Module not found: AppCenter` warning。

## 5. 云端与数据边界

- 只读核验了阿里云官方模型、Native endpoint、1024 维 Dense、document/query 和公开限流资料；按用户明确要求没有发送任何真实模型或连接测试请求。
- 没有读取真实 API Key，没有将密钥写入仓库、日志、前端存储或数据库明文字段。
- 没有处理真实小说正文，没有授权任何小说云端索引，没有产生云端费用。
- 长期库重建后 `novel_embedding_consents=0`，因此未授权小说云端请求保持为 0；项目没有实现计费、扣费或费用统计功能。

## 6. 恢复说明与剩余门禁

重建前长期库清单为 10 本小说、25 个文档、1 个私有素材，Alembic 为 `20260828_0024`。仓库外备份：

- 目录：`/Users/liujia/Documents/AI小说世界2026-backups/20260829-cdv-rebuild.7xaR1O`；
- dump：`ai_novel_world_2026-before-rebuild.dump`，8,111,927 bytes；
- dump SHA-256：`e71dcde70d5c9dff4803749abe9f95d88c2c50bfc1910f12499bb7b1c1d2bafb`；
- manifest SHA-256：`3bfda49dc47f37b5aac281c06ad618e73a43a6fa91e86a148e3c91d10515d1e2`；
- `pg_restore --list` 已验证可读。

长期库随后按精确目标重建，以 `scripts/seed_experimental_database.py --confirm SEED-CREATIVE-DATA-V2` 播种两本完全合成的实验小说。当前清单：2 本小说、2 个文档、3 条时间线、4 个人物实例、1 个私有素材、0 个向量授权；Alembic head 为 `20260829_0028`。没有删除 PostgreSQL/QwenPaw 数据卷、媒体卷或 QwenPaw 上游数据。

如需恢复旧实验库，先停止 PawApp，重建同名空数据库，再对上述 dump 执行 `pg_restore --clean --if-exists --no-owner --no-privileges --dbname=<精确数据库 URL> <dump>`，随后安装与该备份 schema 相匹配的旧 PawApp 包。恢复会覆盖当前两本合成实验小说，执行前仍需再次确认精确目标。

若后续发布候选失败：

- PawApp 回退到旧包；
- 数据库按已验证顺序从 `0028` 降回 `0024`，不得删除数据卷；
- semantic source、chunk 和 embedding 按 generation 精确清理并可重建；
- API Key 清除、小说 consent 撤销与本地向量删除仍是三个独立动作。

真实百炼哨兵、实际 revision／维度验证及真实小说索引未执行，这是用户明确要求的发布边界，不是本地页面和数据权威能力的失败。向量 candidate 在真实连接验证前继续保持 `unverified`，不能激活；本地正文写作、词面搜索和 QwenPaw 原生能力不受影响。
