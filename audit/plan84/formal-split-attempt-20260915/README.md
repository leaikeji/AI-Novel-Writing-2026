# 计划84拆分候选首次正式验收与回退

执行时间：2026-09-15 00:31—00:38 CST

状态：`ROLLED_BACK_FORMAL_GATE`（F84-02、F84-03与1920×1080创作中心布局取得局部正式证据；建书草稿绑定前置不成立后按冻结门禁停止，其余矩阵未执行）

## 发布对象与恢复点

- 正式容器：`ai-novel-2026-qwenpaw-lab`；QwenPaw 2.2.1；PawApp `ai-novel-world-2026` 0.4.0。
- 拆分候选源码、打包树、bundle与manifest身份分别为`abc3bf0d…`、`6ee292e7…`、`f993d703…`、`79fae92a…`，发布前复算均与冻结记录一致。
- 本次独立恢复点：`/private/tmp/plan84-split-release-WWWpeG`；上一安装树哈希`4e253b97…`，归档哈希`7018671f…`，回退命令见该目录`ROLLBACK.md`。
- 发布前正式验证器通过；容器healthy且启动时间为`2026-09-13T15:52:14.055019458Z`，模型为`bigmodel/glm-5.3-flash`，12项Skill、8项小说工具、4份prompt及正式TTS状态无漂移。
- 发布前数据库计数为作品／草稿／私有资产／内容版本`6/6/16/41`；冻结草稿`c150ebf2-a5a9-400a-ae33-9b96c6f59e21`仍为`draft/version 3/step 1`。

## 正式安装

- 项目完整安装器要求先把TTS四开关全部关闭并重建QwenPaw，不符合本计划“不重启”的冻结边界，因此没有使用该模式。
- 通过QwenPaw公开`qwenpaw plugin install --force`原位热更新拆分候选；安装成功并热加载，未重启、未卸载、不动数据卷。
- 随后通过公开配置接口恢复Skill，正式验证器完整通过；PawApp、数据库、Agent、模型、Skill、工具、prompt和TTS均正常。

## 本轮正式Edge事实

本轮使用正式Edge、1920×1080视口；截图在执行会话中逐步捕获和人工检查，但浏览器控制工具没有返回可落仓的本地文件路径，因此它们不作为仓库内持久截图证据。以下结论同时有当次可访问树、DOM只读测量和正式API／数据库读回支持：

1. 创作中心在助手展开与折叠两态均无顶层横向溢出；两态分别只有一个可聚焦的折叠／展开按钮，折叠后焦点留在展开按钮。
2. 私有库《灾后设施抢修词》默认详情没有原始Markdown、`\\n`转义串或UUID路径；列表显示v4，历史显示`v4 当前`。
3. 非词汇资产《逆潮系统私密规则·20260829-140023》完整显示既有正文，列表与历史均为`v2 当前`，没有内容丢失。
4. 点击“新建”后，向导正常打开；Escape触发保存忙碌态，期间关闭入口被撤下，完成后弹窗关闭且焦点回到“新建”。

## 中止原因

- 计划冻结草稿没有与本次正式Edge的浏览器草稿键建立可证明绑定。点击“新建”没有复用`c150ebf2…`，而是创建新草稿`7b520cff-0a54-423e-9a3a-c0e1cc52aa34`，草稿键`novel-c81e39ab-74e0-4c4f-867d-ac2dd26148b6`。
- Escape完成后该草稿为`draft/version 2/step 0`、`completed_novel_id=null`；冻结草稿仍是version 3。创建时间与更新时间只出现一次创建和一次保存，结果与同步关闭互斥合同一致，没有重复PATCH迹象。
- 数据库计数变为`6/7/16/41`。作品、私有资产与内容版本均未变化，但“不得新增第二份计划84草稿”的冻结门禁已经触发，所以立即停止设定、搜索、2560×1440、1366×768、无障碍和其他非回归检查。
- 本轮未调用模型、TTS或embedding，未保存作品、正文或私有库，未执行恢复或删除。

## 回退结果

- 00:37通过同一公开CLI回装上一安装树并恢复Skill；正式验证器通过，容器仍healthy且启动时间未变。
- 回退后已安装树哈希重新成为`4e253b9712356138a8f615cc7b36c3c828e1706e2dbd919cce9e1249d84d88ec`；bundle哈希为`6bce11c0b3c5010d229f7bd9ec58a6c6c71991a9c39f3ca91c10ce48172128bc`，与本次恢复点逐字节一致。
- 新草稿属于正式产品链已产生的用户数据；按项目安全边界保留，不擅自删除。回退后计数仍为`6/7/16/41`。

## 再进入条件

1. 先由作者决定是否把新草稿`7b520cff…`作为后续正式验收的唯一保留草稿；未获决定前不再打开建书向导。
2. 若采用，下一轮必须沿用能够复用该浏览器草稿键的正式Edge状态，并在打开前后读回同一ID；若再次生成新ID立即停止。
3. 只有重新安装同一冻结候选并完成剩余正式桌面矩阵，F84-02—F84-07才可取得产品状态；本轮局部证据不能外推为整体PASS。

## 后续

作者随后明确采用本轮产生的草稿作为唯一正式验收草稿；同一冻结拆分候选已完成剩余矩阵，结果见[正式完成记录](../formal-split-completion-20260915/README.md)。本页仍保留首次停止与回退的原始历史状态。
