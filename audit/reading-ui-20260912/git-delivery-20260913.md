# 朗读改动 Git 交付复核

日期：2026-09-13。作者明确要求“提交 git，然后 push”，本次授权独立于此前发布授权。以下为提交前验收事实；最终提交 ID 与推送结果以 Git 记录及本次对话回执为准。

## 精确范围

- 计划 77 的朗读六分区、人物抽屉、普通话音色名称、输入反馈和桌面布局优化。
- 页面依赖且此前已正式发布、尚未提交的计划 76：本地设计锚点、Base 复刻、普通话约束、失败状态刷新、终态缺口续播及新迁移 `20260912_0055`。
- 对应测试、0055 维护门禁、两份计划、两级索引的相关条目及现有审查／发布截图。
- `backend/app.py` 仅暂存五个 TTS 接线差异块；文档索引仅暂存计划 76／77 条目。不纳入其他写作、Skill、私有库、研究任务的候选或安装器改动。
- 未纳入模型权重、音频、数据库备份、密钥、依赖目录或构建产物；其他工作区改动保留。

## 实际待提交树验证

将 Git 暂存区完整导出至 `/private/tmp/reading-git-check.9UYpt5`，复用固定依赖，在该目录运行，避免工作区其他未提交代码掩盖缺失依赖：

- `pnpm typecheck`：通过。
- `pnpm test`：156 文件、1355 项通过。
- 项目解释器 `-m pytest -q`：退出 0，全量通过；逐项进度为 2791 passed、266 skipped。需专门数据库／外部环境的跳过项不作本轮新增通过结论。
- `pnpm build`：通过。
- 项目解释器 `scripts/package_plugin.py`：通过。
- `pytest tests/test_qwenpaw_integration_contract.py -k packag`：19 passed、65 deselected。
- 暂存 diff 空白检查通过；常见凭据模式及禁止制品扫描无发现，最大的单文件不足 200 KB。

此次仅验证和交付 Git，不再部署、重启、迁移或调用模型。已发布 UI 的正式验证继续引用 [V1.4 发布记录](./simple-ui-release.md)；不将 Git 回归解释为云端真凭据、私人音色真实写入全链或完整听感验收。
