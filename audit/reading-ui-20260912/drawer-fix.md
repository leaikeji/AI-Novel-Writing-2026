# 人物音色抽屉布局修复验收

2026-09-12，V1.1 已通过公开热更新生效到正式 18088，未提交或推送 Git。

## 事实与修复

界面审查从作者标注及正式截图复现：1916px 桌面视口内的 760px 抽屉仍使用双列音色卡片，固定最小列宽使详情按钮溢出。此前六分区验收遗漏了展开抽屉后的卡片布局场景。

- [修复前](./17-drawer-before.png)
- [本地展开验证](./18-drawer-after.png)、[详情展开](./19-drawer-details.png)
- [正式修复后](./20-formal-drawer-fixed.png)

仅改变 `frontend/src/narration/styles/voice-library.ts`：命名容器查询按音色库实际宽度响应；小于等于 860px 使用单列，标题列允许收缩，小于等于 560px 把动作排入独立行。不改组件绑定、试听请求或数据接口。新增同目录布局契约测试两项。

## 实际验证

- 工作区：类型检查、155 文件／1346 项前端测试、构建通过。
- 正式基线隔离候选：类型检查、155 文件／1338 项前端测试、构建、插件打包通过；19 项打包契约通过。数量差异来自工作区其他候选，未打包这些无关改动。
- 本地浏览器：抽屉九卡无按钮越界；详情展开抽屉宽和 scrollWidth 均为 760；搜索 Serena 显示一项，清空恢复，关闭后焦点返回人物按钮。旁白宽容器双列正常，模拟桌面侧栏挤压后音色库 837px 自动单列且无横向溢出。
- 正式浏览器：1916px 视口、760px 抽屉，九张卡片均约 679.15px；逐卡检查可见试听／详情控件均在卡片边界内，抽屉 scrollWidth=clientWidth=760。人物仍为 6 位、5 位待配置，陈屿仍绑定 Serena。
- 日志未见 error；存在更新前已有的宿主 `Module not found: Chat` warning。刷新时 URL 改为宿主会话路径，但人物配音页保留且可正常操作，未修改上游路由。

## 正式部署与恢复

独立候选和恢复材料：`/private/tmp/reading77-drawer-release.qM2JoJ`。基于上一轮已部署计划 77 源码，只叠加本次 CSS 与测试；与更新前安装树逐文件 SHA-256 比较，唯一改变为 `frontend/dist/index.js`。公开热安装成功，Skill 开关恢复一致；公开 overview 的 settings／authorization 更新前后一致。未操作正文、音频、数据库、声音绑定或模型。

正式与候选 bundle SHA-256 均为 `7f69a43bae5d70db22ce6fca75e9a8260c9849a70b48b0f027e175dc3f8c466d`。容器 ID 与启动时间未改变，healthy；只读运行验收确认 backend／worker／reference clone ready，9 个官方音色，11 个小说 Skills 与 5 个工具作用域保持。

需要回退时，用项目 `.venv/bin/python` 执行上述目录 `deploy.py rollback`，经公开热安装恢复 `installed-before` 和 Skill 快照。未触发回退，不需要恢复数据库。

本轮不调用模型生成试听，不新增音质或声音生成全链通过结论。容器小于 560px 的独立按钮行有样式契约覆盖，但未追加极窄窗口真实截图；本轮正式复验针对作者标注的桌面抽屉。
