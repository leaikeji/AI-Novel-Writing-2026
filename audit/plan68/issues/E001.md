# E001 唯一长期 QwenPaw 环境被并行计划占用

- 类型：测试环境共享锁，不是产品缺陷。
- 发现：B003/B004候选准备部署时，长期 QwenPaw 容器已被另一活跃计划67维护任务停止；同时存在计划67备份/角色引导容器。普通启动未生效，Compose重建进入旧容器退出、新候选 Created 的中间态。
- 处置：立即停止进一步容器清理、安装、迁移和浏览器写操作；只读确认并行任务仍 inProgress。未删除容器、卷、数据库或小说数据。
- 后续事实一：作者明确同意重启 Docker Desktop 后，引擎恢复；只读检查发现长期数据库已经由计划67升到 `20260911_0053`，而原隔离候选只到 `0051`，其朗读门禁正确返回 `TTS_DATABASE_SCHEMA_OUTDATED`。没有降级或改写数据库。
- 后续事实二：保留计划67全部工作区改动并合入计划68修复的主工作区候选通过前端全量、后端全量和打包门禁，候选 head=`20260911_0053`、tree SHA-256=`85ecda1549a597e1d43741daf2c979678477b6cb44ad3a9672d13adc26669fd9`。经公开离线安装器安装后，PawApp、数据库、narration 和 worker 一度均为 `ready`，只读 verify 通过。
- 最终状态：计划67释放互斥锁后，本任务先从真实UI复核唯一作品仍为3卷9章、24,092字，再通过公开QwenPaw安装链部署兼容0053的候选并继续S0070–S0096；未并发覆盖、未删除容器/卷/数据库。
- 状态：`RESOLVED`；本问题是共享环境协调，不是产品缺陷。
