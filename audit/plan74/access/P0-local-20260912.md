# P0私有库助手接入本地验证

日期：2026-09-12。结论：`LOCAL_PASS`；G1A真实宿主验证为`NOT_RUN`，不是PASS。

## 范围与基线

作者要求“优化方案，然后开始施工”。主代理按计划74先完成指令边界、AI效果验收、失败处置和当前授权状态整理，再实现L74-ACCESS的最小候选。

工作区存在计划70、75及研究等在途改动。未覆盖它们，未暂存、提交、推送或清理。注册表文件在本轮修改前无Git差异；已有共享`backend/app.py`、生成服务、安装器、写作Skill及相关测试未修改。当前原生宿主为QwenPaw 2.2.0的项目记录基线；没有重新验证正式模型、Agent或数据库状态。

## 已核实事实

- 原注册表只接受小说或建书草稿绑定，不能直接提供无小说的私有库范围。
- 新候选新增互斥的库级信封，复用同一注册表的随机ref、限流、过期、同session租约及owner／tab规则，没有另建票据存储。
- 新适配器默认关闭且未注册，使用现有公开`on_reply`形状和项目当前消息提取器；区分页面范围、作者消息及其hash。页面范围不等于写授权，候选`write_authorized`固定为false。
- 没有数据库写入、模型调用、HTTP维护入口或产品工具注册；不声称已经能收藏或维护资料。
- Docker首次只读列表在沙箱中被拒，经只读提权后成功。正式`18088`健康；计划70的QwenPaw`28175`和PostgreSQL`55472`正运行，未借用、重启或修改。没有为本计划创建或安装独立宿主。

## 实际命令与结果

使用项目`.venv/bin/python`，实际Python 3.12.14。

```bash
.venv/bin/python -m pytest tests/private_library/test_access_probe.py tests/test_assistant_context_transport.py tests/test_assistant_context_retention.py
# 93 passed in 0.40s
.venv/bin/python scripts/verify_plan74_library_access.py
# local_status=LOCAL_PASS; g1a_real_host_status=NOT_RUN
.venv/bin/python -m pytest
# 2890 passed, 273 skipped, 3 warnings in 19.64s
```

全量结果覆盖当时工作区，不代表所有其他计划候选可发布。273项跳过不算通过，不以此宣称数据库、真实模型或宿主集成已验收。3项警告为既有Starlette TestClient／HTTP状态常量弃用提示，未为本任务修改依赖。

最终复核仅移除了已由函数入口保证的重复类型判断，没有改变接受范围；随后将原HTTP入口测试加入定向回归，结果104 passed、1 warning（0.66s）。全量2890项结果对应此前等价实现，不冒称做了第二次全量。`git diff --check`通过，计划与新证据9个本地链接有效。

最终代码SHA-256（用于复验，不含模型配置或真实资料）：

```text
e5c92554636659ea4813f9e004ed0564769cbbf094363b33e4d276b28989c066  backend/assistant_context_registry.py
e5ac9853c70635ce8ad64c2235d09a846a1c91e30e80352b892eb539c1aab12b  backend/private_library/access_context.py
889a331e605baeb902871db40c61f383665b260c992a7d8f1305f6b5058054d7  scripts/verify_plan74_library_access.py
2938330dc8547548102f77bfb30506f52948cb41c5cf75db9c51b9e15e68d18f  tests/private_library/test_access_probe.py
```

探针实际输出9项为true：默认关闭、库级范围建立、事件透传、未伪造小说、当前作者消息来源、页面票据不授写权、流结束清除范围、跨session拒绝、重启后票据失效。

定向测试另覆盖绑定混用、字段伪造、嵌入资料命令、时间窗、上下文身份、模型自填小说ID无效、本书范围恢复、首次session绑定、owner／tab、异常／关闭／空消息的流式清理。所有资料为合成内存数据，不连接真实小说库。

## 仍待验证与后续顺序

1. 在独立隔离环境验证公开HTTP／浏览器传输，确保原生当轮输入的形状与本地替身一致；补Python 3.11宿主验证。
2. 完成维护Skill与工具的最小隔离样例，确认能从原指令得到实际受限操作与回执；当前候选只证明消息来源，不判断语义授权。
3. 验证无小说维护、本书维护、咨询不写、异常回执及作用域隔离；真实模型评测先冻结有效模型、样本、调用上限与停止条件。
4. G1A通过后再冻结业务DTO、存储与迁移；当前不得派发后续W2或以探针成功代替首发。

本地代码中的函数和schema仍属P0候选；真实链路发现契约不匹配时在本计划适配器修正，不改QwenPaw上游。若只能降低“一句话直接维护”的要求，按计划停止并请作者裁决。

## 恢复

候选未接线，正式运行不受影响；无需重装、重启或数据恢复。后续撤回本候选应只撤回计划74新增接入文件及注册表的对应增量，保留其他任务修改与本证据；不要通过整文件回滚处理共享工作区。
