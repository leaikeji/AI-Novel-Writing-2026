# 计划74 P1代码与环境门禁记录

日期：2026-09-13。结论：`CODE_PASS / ENV_HOLD_RESOLVED / RELEASED`。

## 已完成的候选

- 新增`lexicon-pack/1`、`library-change/1`和`library-check/1`合同，加法迁移`20260913_0056`保持唯一线性head。
- 复用不可变私有资产版本与本书固定绑定，增加通用／本书范围、收集包、本书副本、CAS、幂等、变更提案／执行／撤销和服务端回执。
- 本地确定性扫描支持中文精确短语、ASCII完整词、显式变体、Markdown可见文本、UTF-16定位、慎用窗口及结果完整性标记。
- 桌面私有库工作台支持四类资料、搜索、范围／启用筛选、分批加载、词项编辑、本书启停、归档、不可变历史恢复；旧组合只读展示并按明确选择应用固定版本，不再暴露第二套组合管理。选区工具条提供一个“加入私有库”入口，保存冻结文本及来源hash，不改源文。
- 原生助手增加库级／所选本书可信范围，注册查询、准备变更、执行／撤销三个默认关闭工具，并新增`private-library-maintenance` Skill。
- 章节正文生成冻结完整用词策略；推荐、慎用、禁用分区渲染，完整禁用清单不能静默裁剪；生成候选在同一事务写入检查报告，采用时校验报告、豁免、完整性和当前规则变化。章节工作副本可手动检查并按UTF-16定位；选区全量或部分采用由服务端按冻结Diff和当前工作副本重建最终整章，检查失败、基线变化或未明确保留禁用项时不写回。
- 永久删除作品若会销毁作品级私有库资料或检查证据，会在任何媒体与数据库删除前明确阻断；回收站与恢复不受影响。

## 实际代码验证

以下命令均从仓库根执行，退出码为0：

```text
.venv/bin/python -m pytest -q
.venv/bin/python -m pytest tests/private_library -q
pnpm test
pnpm typecheck
pnpm build
.venv/bin/python scripts/package_plugin.py
.venv/bin/python -m compileall -q backend
git diff --check
```

结果：Python全量回归通过（跳过项仍为跳过，未冒充正式集成）；私有库定向136项通过；前端159个测试文件、1380项测试通过；TypeScript类型检查通过，Vite生产构建转换198个模块，插件可重建打包及Skill／助手安装／宿主集成／生命周期合同回归通过。全量Python首次运行发现9个旧迁移head断言及1个旧采用参数断言，更新到新合同后定向和全量重跑通过。

## 确定性性能记录

输入为项目合成语料，不含用户或外部正文。机器`arm64`、macOS 26.5.2、Python 3.12.14；1000条规则、10000个可见字符，3次预热后测量30次：

```json
{"min_ms":50.776,"median_ms":51.901,"p95_ms":53.586,"max_ms":54.089,"peak_bytes":2405075,"status":"complete","scanned_rules":1000,"omitted_rules":0,"retained_hits":5}
```

本地p95低于计划建议的500ms目标。该结果只证明固定代码与合成输入的本地性能，不代表正式服务器网络、数据库或浏览器性能。

## 正式环境门禁（历史故障与解除）

只读检查确认正式QwenPaw为2.2.0、宿主Python为3.11.2、当前PawApp仍为0.4.0。当前`/health`因既有`TTS_FEATURE_CRASHED`为`degraded`；正式PostgreSQL新连接返回`base/5`不是有效数据目录、`PG_VERSION`不含有效数据，`/novels`返回HTTP 500；Docker存储还出现I/O／只读错误并阻止创建一次性PostgreSQL。

该故障记录是施工中止点，不是最终状态。环境恢复后先冻结数据库、插件树、Skill状态和生效绑定清单，再执行正式`0056`迁移、公开原位热更新、真实Skill／工具调用、真实小说写作链与桌面验收。完成验收修复后，最终私有库定向测试为147项，全量Python、前端159个文件／1380项、类型检查、构建和打包均再次通过。

最终又补充执行完整生命周期：正式卸载后PawApp、12项小说Skill与8项小说工具归零，插件健康路由返回404，QwenPaw与PostgreSQL仍健康且schema保持`20260913_0056`；随后用同一候选经公开热装恢复，Skill状态无差异、正式健康为`ready`。详见[正式发布证据](../formal/formal-release-20260913.md)和[恢复说明](../recovery/RECOVERY.md)。
