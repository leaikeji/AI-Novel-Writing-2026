# 计划 81：计划 76-V0.8／V0.8.1 并发工作区漂移记录

状态：**RESOLVED_TO_CLOSED_PASS / V0.8.1-FOLLOWUP-CLOSED / GIT-DELIVERED / WORKTREE-PRESERVED**

日期：2026-09-14（Asia/Shanghai）

## 1. 观察事实

本轮开始的 `git status --short` 未列出 `frontend/src/narration/character-voice-configurator.ts`；完成索引漂移复核后再次检查时，该文件已经变为 tracked modified。计划 81 本轮没有编辑产品代码，因此这是与本次文档治理并发出现的工作区变化。

首次只读 diff 显示该文件当时只有三处文案变化：

- “当前声音”改为“当前实际声音”；
- “浏览全部官方音色／选中即保存……”改为“浏览官方音色／选中后立即生效……”；
- “私人音色与高级调音／仅在需要时展开”改为“定制私人音色／设计、试听并确认后，再应用给人物”。

三处文字与计划 76 第 11 节 V0.8 的目标一致，但不足以实现其四步私人流程、实际状态／草稿隔离、条件渲染、状态矩阵、键盘焦点、样式和正式发布门禁。

随后再次检查时，`character-voice-configurator.test.ts`、`styles/t2-c.ts`、`styles/t2-d.ts` 也新进入 modified 状态，证明工作区仍在变化，不能把首次三处文案快照当作稳定候选全集。

Codex 任务只读清单同时显示，同一 `cwd=/Users/liujia/Documents/AI小说世界2026` 存在另一个 `active` 任务，界面标题为“设计 Qwen-TTS 云本地混合方案”；其最新 turn 为 `inProgress`。使用当前 cursor 等待 30 秒后状态未变，没有可读 assistant message 或 tool marker。该事实证明存在同目录并发任务，但不能单凭标题证明它就是上述 diff 的所有者。

## 2. 中间裁决

以下内容记录共享任务尚未完成时的中间快照，已经被第4节最终汇合取代，不作为当前状态：

计划 76 第 11 节和文档顶部仍明确写明 V0.8“等待作者批准施工、尚未实施”；当前没有发现作者针对 V0.8 的单独批准记录，也没有 V0.8 专属测试、构建、打包、正式热更新或桌面证据。

因此当前准确状态为：

`PROPOSED_WITH_PARTIAL_WORKTREE_CANDIDATE`

即方案仍未获施工批准，但工作区已出现一个未验证、未发布的局部候选。它不能算作已实施、已批准或在途正式候选，也不能继续继承 V0.7.1 的授权。

## 3. 保护措施

- 不修改、覆盖、暂存、提交、发布或回退已观察到的全部并发变化。
- 不把该文件加入计划 81 的文档收敛写入范围。
- 后续任何 V0.8 行动前重新读取 `git status` 和逐文件 diff；若差异继续变化，按最新实际状态重新冻结。
- 在同目录活跃任务结束、需要处理或能够给出稳定文件清单前，不对 V0.8 做集成、测试归属裁决或发布；不以一次超时观察判定其已经停止。
- 作者批准 V0.8 后，可把届时经确认的稳定 diff 作为候选输入，但仍须完成计划 76 第 11 节 W0—W3，不能跳过状态实现和验证。
- 作者不采纳 V0.8 时，也不能自动丢弃该 diff；只有确认其归属与精确恢复目标后，才能执行任何撤销。

## 4. 最终汇合与总台账影响

同目录任务最终 turn 已转为 `completed`、`error=null`，任务状态为idle。其完成前已将计划76顶部和第11节更新为“V0.8已获作者批准并完成”，并形成[V0.8正式记录](../../audit/qwen-tts/plan76/character-voice-v08/README.md)。

最终证据为：定向4文件47项、全量前端170文件1617项、typecheck、build、插件打包和`git diff --check`通过；正式候选与安装包`frontend/dist/index.js`哈希统一为`2fe4278a442e36974a8d1330ac4732b3b6a8d0bb7a77896549eac4f32655298c`；公开热更新、正式桌面只读路径、健康和Agent配置保态通过；生成的`__pycache__`已清理。未调用模型、播放音频、创建／锁定声音或修改人物绑定／正文。

据此，V0.8子范围最终由`PROPOSED_WITH_PARTIAL_WORKTREE_CANDIDATE`转为`CLOSED_PASS`。计划级主状态仍为`ACTIVE_CLOSEOUT`，但只因为V0.7.1 G3真实声音链和作者听感尚未关闭；V0.8不再属于计划81 G4待裁决项。当时源码和证据仍未提交／推送，Git交付状态单列；该时点事实已由下节后续状态取代。

## 5. 二次复查后的后续状态

V0.8正式只读复验又发现两个同范围交互缺口：没有明确选择可应用私人音色时提前显示第4步，以及点击“继续完成”后焦点落到页面根节点。原任务随后按作者同意完成V0.8.1，定向2文件32项、全量前端170文件1618项、类型、构建、打包和差异检查通过；正式桌面只读复验确认第4步条件呈现、焦点、紧接着Escape关闭及横向溢出通过，实际绑定仍为Serena。最终正式包SHA-256为`4a8b436025732bd961670b3bc6e02405f36de250d071f54c0265d35fad91a930`，见[V0.8.1记录](../../audit/qwen-tts/plan76/character-voice-v081/README.md)。未试听、生成、锁定、保存或改绑定，因此V0.8.1为`CLOSED_PASS`，不关闭V0.7.1 G3。

随后提交`32bea56`已把V0.7.1、V0.8、V0.8.1的源码、计划、测试与证据纳入版本控制，提交`38da2e0`补齐两级索引；两项均已推送到`origin/main`，二次复查时分支差异为`0/0`。因此第4节“Git交付仍OPEN”只保留为当时快照，当前计划76增量的Git交付已闭合。计划79—82仍另行等待Git归宿，不受本结论替代。
