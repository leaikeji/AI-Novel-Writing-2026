# 计划81：G1文档收敛执行记录

状态：**PASS / G0-APPROVED / G1-COMPLETE / G5-SUBSEQUENTLY-COMPLETE**

日期：2026-09-14（Asia/Shanghai）

## 1. 授权与范围

作者在收到“本轮只修正文档状态、索引和审计证据，不启动产品施工，也不碰正式环境”的白话说明后明确回复“同意”，据此通过计划81 G0。

本轮仅执行文档治理：没有修改产品代码、数据库、迁移、Agent、Skill或运行配置；没有调用写作／语音模型，没有生成、播放、锁定或绑定声音，没有安装插件、重启服务、操作正式环境，也没有执行`git add`、提交或推送。

## 2. 已完成动作

- 按[冻结横幅记录](./header-banners.md)给计划16、17、18、22、27、28、29、38、44、45、46、47、48、58、61、67、70、74共18份历史计划添加当前终态；每份只在标题后新增一段，旧状态、失败、HOLD、PARTIAL和证据原样保留。
- 窄修[计划76](../../docs/开发文档/76-Qwen3-TTS本地声音设计与复刻最小闭环计划.md)顶部当前事实：G1时点补入V0.8.1完成状态及提交`32bea56`、`38da2e0`已经推送的事实；后续G5在既有候选Edge播放通过且无运行中动作后，将计划级状态转为`CONDITIONAL`。
- 重建根README、文档导航和开发文档索引的状态分流：当前主动产品收尾只剩计划76-V0.7.1 G3；计划80／82只列为`PROPOSED`；其余计划进入完成、带边界结项、替代、未采纳、参考或条件触发。
- 更新[机器主状态清单](./status-manifest.tsv)的G0动作字段：18份横幅为`APPLIED_TERMINAL_BANNER`，计划76为`CURRENT_FACT_CORRECTED`。
- 计划70继续遵守既有local-only决定，没有加入Git跟踪。

## 3. 机械复核

- 18份冻结横幅逐字比对：`terminal_banners=18 mismatches=0`；每段只出现一次并位于对应文档第3行。
- 机器清单（G1时点）：`manifest_plan_rows=82 subscope_rows=2`；计划级状态计数为`REFERENCE=11`、`CLOSED_PASS=35`、`CLOSED_WITH_LIMITS=13`、`SUPERSEDED=16`、`NOT_ADOPTED=3`、`ACTIVE_CLOSEOUT=1`、`ACTIVE_GOVERNANCE=1`、`PROPOSED=2`。G5最终计数见[完成性审计](./completion-audit.md)。
- 本地Markdown链接检查：85份文件共627个本地相对链接，`broken=0`。检查集合为根README、两级导航和00—82共82份实际计划文档；明确纳入被忽略的计划70。
- `git diff --check`通过；本轮18份受跟踪横幅文件均只有2行新增、0行删除。计划70另行检查，无尾随空白且仍命中`.gitignore`。
- 三级导航不存在旧的“当前施工”或“待裁决或尚未施工”章节；主动产品队列只指向计划76，计划80／82仍明确为未获施工批准。

这些是文档一致性检查，不是对历史产品测试或正式环境验收的重新执行；各计划原有PASS／HOLD／PARTIAL仍以对应原始证据为准。

## 4. G1后的真实剩余项

1. 计划76-V0.7.1 G3：早于V0.7.1创建的既有候选已在桌面Edge完成播放；仍需作者确认听感、锁定／绑定，并决定接受既有候选或生成一份V0.7.1新候选。候选已安全保存，G5将其改为条件门禁，不再列作主动施工。
2. 计划80：原生助手智能Skill路由与升级兼容方案，仍为`PROPOSED`，需要作者单独决定批准、不采纳或延后。
3. 计划82：现行界面、章节保存和朗读Edition状态缺口方案，仍为`PROPOSED`，需要作者单独决定批准、不采纳或延后。
4. Git归宿：计划70保持local-only；计划79—82仍需作者分别决定提交／推送、继续local-only或如何保留。文档收敛不构成Git授权。

以上结论描述G1完成时点。后续既有候选桌面播放、运行态保全和全量状态复核已经完成；计划76转为`CONDITIONAL`，计划81转为`CLOSED_PASS`。最终结果与复现命令见[G5完成性审计](./completion-audit.md)。
