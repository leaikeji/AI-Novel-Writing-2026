# 计划 81：00—82 号开发计划完整性与终态复核

状态：**PASS / SOURCE-REVERIFIED / ALL-82-CLASSIFIED / ZERO-ACTIVE**

日期：2026-09-14（Asia/Shanghai）

## 1. 本次核实回答什么

本记录用于证明三件事：

1. 00—82 号范围内没有漏查、重复归类或同号多文件；39 号从未创建，不是待补计划。
2. 每份计划都已获得一个且仅一个主状态；计划 76 内新增的 V0.8、V0.8.1 子范围另列为已完成增量，不重复计作新计划。
3. 哪些旧文档顶部会误导“当前任务队列”已经被精确识别，并在作者批准计划81 G0后按冻结文本完成收敛。

本轮只读取计划、当前源码和已有证据，没有修改产品代码、正式环境、数据库、小说、音频或历史验收原件。

## 2. 编号与文件完整性

机械复核口径：枚举 `docs/开发文档/00-*.md` 至 `80-*.md`，明确排除不存在的 39，再与计划 81 第 4 节分组逐项比对。

结果：

- 预期编号：80 个（00—80 共 81 个编号，排除 39）。
- 已归类编号：80 个。
- 唯一编号：80 个。
- 重复编号：0。
- 遗漏编号：0。
- 同号多份计划文档：0。
- 计划 76-V0.8：同一计划内的新增子范围，不计作第二份计划76；复核中经历并发候选、批准、施工与正式发布，最终归入 `CLOSED_PASS`，详见[`worktree-drift.md`](./worktree-drift.md)与[V0.8正式记录](../../audit/qwen-tts/plan76/character-voice-v08/README.md)。
- 计划 76-V0.8.1：V0.8正式只读复验后的同范围交互收尾，不计作新计划；步骤条件、焦点与Escape已修复并正式生效，归入`CLOSED_PASS`，详见[V0.8.1正式记录](../../audit/qwen-tts/plan76/character-voice-v081/README.md)。
- 新增计划 82：1 份，单独归入 `PROPOSED`；不改变上述 00—80 的 80 份计数。

复核命令只使用文件名和计划 81 中的编号分组；输出为 `expected=80 assigned=80 unique=80`，随后逐号检查均为一个 `NN-*.md` 文件。

扩展到当前00—82后，`rg --files --no-ignore`得到82份实际计划文档；[机器主状态清单](./status-manifest.tsv)也有82条计划级记录，编号和路径集合完全相等。另有计划76-V0.8、V0.8.1两条子范围记录，不计作第二份文档。G1最终复查时，根README、两级索引和全部82份计划共85份文件、627个本地相对Markdown链接均能解析到现有文件，`broken=0`。

## 3. 唯一主状态

以下分组覆盖 00—82 的全部82份现存计划；括号内为数量。

| 建议终态 | 计划编号 | 数量 | 当前动作 |
| --- | --- | ---: | --- |
| `REFERENCE` | 00、01、02、04、05、06、07、09、10、11、12 | 11 | 作为规格／架构／环境基线保留，不加入施工队列 |
| `CLOSED_PASS` | 08、13、14、15、19、20、21、25、26、30、31、32、36、37、42、43、49、50、51、52、53、54、55、62、63、64、65、69、71、72、73、75、77、78、79、81 | 36 | 保留原验收边界；计划81完成治理审计，不重复施工 |
| `SUPERSEDED` | 03、16、17、18、29、33、35、40、44、45、46、47、48、56、58、60 | 16 | 指向后续架构／计划／停止决定，不重开旧工作包 |
| `NOT_ADOPTED` | 27、28、38 | 3 | 记录未按旧方案实施；未来有需要重新立项 |
| `CLOSED_WITH_LIMITS` | 22、23、24、34、41、57、59、61、66、67、68、70、74 | 13 | 如实保留 PARTIAL／HOLD／未证明项，但结束原计划无限续跑 |
| `CONDITIONAL` | 76 | 1 | 既有候选安全保存；作者明确采用或重新生成后才恢复窄任务 |
| `PROPOSED` | 80、82 | 2 | 等待作者分别批准，不自动施工 |

数量合计：`11 + 36 + 16 + 3 + 13 + 1 + 2 = 82`。

计划81已完成本次治理并归入`CLOSED_PASS`；计划82是从计划70当前源码残留中拆出的`PROPOSED`窄方案。计划76-V0.8及V0.8.1位于计划76第11节，是同号文档中已经`CLOSED_PASS`的独立增量；计划级主状态为`CONDITIONAL`，因为V0.7.1现有候选没有运行中动作，只等待作者试听触发。

### 3.1 边界型计划复核

- **计划 22**：Skill 候选、安装合同和自动化完成，但多轮真实样本未形成可比 A/B 提升证据；应为 `CLOSED_WITH_LIMITS`，不能把“批准 S0—S3”继续显示成当前施工。
- **计划 34**：V1 语义索引与写作链主要范围完成，但原文明确记录真实正文长度门禁仅部分通过；计划 37 后续修复长度闭环，不应倒改计划 34 当时的结果，因此从初稿的 `CLOSED_PASS` 调整为 `CLOSED_WITH_LIMITS`。
- **计划 40**：当时机器与产品闭环完成、作者听感 HOLD；MOSS／Nano 现行运行架构后来被计划 59 移除，当前终态用 `SUPERSEDED`，同时保留当时通过与未听检事实。
- **计划 43**：当时只安装到隔离环境，但功能随后进入长期安装树；原计划功能与回退证据完整，归入 `CLOSED_PASS`，不再补跑旧隔离矩阵。
- **计划 59／61**：本地链和配置页面已交付；真实云端凭据调用是条件触发项，历史表物理删除也不是当前产品交付阻断，因此以限制结项。
- **计划 67**：可恢复删除链已部署；最终不可逆删除必须发生在作者真实动作并逐次确认时，不为结项制造一本要销毁的作品。
- **计划 70**：限定章节正文 Skill 派发得到正式证据，质量结论为 HOLD；仍值得修复的现行 UI／保存／Edition 状态已转入计划 82，原计划以限制结项。
- **计划 74**：核心分类、写作接入、维护和恢复链已交付；101+ 真实资料规模与文学 A/B 尚未自然满足，作为条件观测保留而不让原计划持续“施工中”。

### 3.2 计划内部追加范围扫描

除逐份读取顶部状态外，另扫描全部 `NN-*.md` 中的“增量”“优化计划”“等待批准施工”“尚未实施／施工”等追加段落，防止新范围藏在旧计划尾部。结果：

- 计划 76 第 11 节 V0.8 曾是唯一藏在旧计划尾部的 `PROPOSED` 子范围；本轮已取得作者批准并完成代码、公开热更新和正式桌面只读验收，最终 `CLOSED_PASS`。随后同节追加的V0.8.1也已完成步骤条件与焦点收尾并正式生效，归入`CLOSED_PASS`。两者均未调用模型或改变绑定，不能用来关闭 V0.7.1 的声音生成与听感门禁。
- 计划 26 所写“基线性格分析调整尚未施工”已经转入计划 27；计划 27 又被后续人物 revision、StoryFact 与故事账本架构部分吸收，不能恢复为计划 26 的新尾项。
- 计划 22 提到的下一版三层评测合同从未形成独立批准范围，且多轮真实证据只能支持写作增益未证明；它作为限制保留，不自动生成新的评测任务。
- 计划 61 所写旧音色约束兼容迁移已经由计划 62 实施并正式验证，不是仍开放的子计划。
- 计划 70 的多轮 V0.x 追加施工记录已由当前源码残留复核重新裁决；确认仍存在的产品问题只进入计划 82，不沿用旧工作包编号继续执行。

因此，当前待裁决产品方案只剩计划 80、计划 82；没有发现第三个藏在旧文档尾部且仍应主动施工的方案。

## 4. G0 通过后的精确顶部状态动作

以下文档顶部原先可能让读者误判为当前施工、旧方案待批准或旧门禁尚需继续。G0批准后已只追加一段“当前终态说明”，未删除、重排或改写历史过程。

| 计划 | 当前顶部的误导风险 | 建议顶部动作 |
| --- | --- | --- |
| 16 | 仍写“执行中” | 标 `SUPERSEDED`，说明后续权威、写作链与产品形态已分散由现行计划接管 |
| 17 | 仍写“本地阶段 F 执行中” | 标 `SUPERSEDED`，保留冻结矩阵作为历史参考 |
| 18 | 顶部以多轮候选／旧长期环境叙述为主 | 增加 `SUPERSEDED` 总横幅，指向 Qwen TTS 现行架构 |
| 22 | 只写批准 S0—S3，缺最终真实评测结论 | 标 `CLOSED_WITH_LIMITS`，保留“写作提升未证明” |
| 27 | 仍写待批准施工 | 标 `NOT_ADOPTED`，说明底层能力被后续计划吸收、旧专项 UI 未实施 |
| 28 | 仍写等待裁决 | 标 `NOT_ADOPTED`，说明页面与依赖基线已变化 |
| 29 | 仍写等待批准、尚未实施 | 标 `SUPERSEDED`，指向计划 31 与当前正式大纲／角色 revision 权威实现 |
| 38 | 仍写等待批准 | 标 `NOT_ADOPTED`，未来只有授权语料与现行私有库条件满足后重新立项 |
| 44 | 仍写长期部署未获授权 | 标 `SUPERSEDED`，保留当时未跑完整矩阵事实，并说明代码后续已进入长期树 |
| 45 | 仍写旧长期候选与作者听感 HOLD | 标 `SUPERSEDED`，指向计划 59 后的新 TTS 架构 |
| 46 | 顶部主要解释当时未做长期发布 | 标 `SUPERSEDED`，说明能力已由后续人物卡／账本计划接管，不倒写旧发布事实 |
| 47 | 仍写候选未部署及两项 HOLD | 标 `SUPERSEDED`，保留旧验收事实并指向 Qwen TTS 后继 |
| 48 | 顶部主要解释当时未长期安装 | 标 `SUPERSEDED`，说明宿主主题兼容已被后续长期树吸收 |
| 58 | 仍写目标模式启动和多门阻断 | 标 `SUPERSEDED`，限定结果由计划 70 承接，新原生助手方案由计划 80 重新裁决 |
| 61 | 仍写 W3 生命周期未关闭及新建书 500 | 标 `CLOSED_WITH_LIMITS`，注明 500 已由计划 62 修复；云端 smoke 改为条件触发 |
| 67 | 仍写最终不可逆点击待确认 | 标 `CLOSED_WITH_LIMITS`，明确该点击是逐动作安全门而非计划尾项 |
| 70 | 仍显示 `UI-FOLLOWUPS-OPEN` | 标 `CLOSED_WITH_LIMITS`，保留 G3 HOLD；现行残留转计划 82 |
| 74 | 最新状态仍写“不宣称整体完成” | 标 `CLOSED_WITH_LIMITS`，把真实大库／文学效果改列条件观测，不改写历史门禁 |

下列文档的顶部已经足以表达终态，原则上只更新索引，不做无价值改写：03、33、35、40、56、59、60、66、68，以及已有明确 PASS 的计划。计划 34 当前顶部已经如实写明“主要验收完成、长度部分通过”，只需在索引归为 `CLOSED_WITH_LIMITS`，不必覆盖原句。

计划76、80、81、82的当前分层为：计划76的V0.7.1为`CONDITIONAL`，其V0.8／V0.8.1子范围已经`CLOSED_PASS`；计划80／82为`PROPOSED`，计划81为`CLOSED_PASS`。不能因页面优化和既有候选播放通过而伪造作者听感或新生成链结论；这些主观／新生成动作只在作者触发后恢复窄任务。计划76顶部已补入V0.8.1、最新Git事实和条件门禁。

## 5. 当前允许继续与不允许继续

当前没有主动产品施工。计划76的既有候选安全保存，只有作者明确采用或重新生成时才恢复一个窄任务；其余旧计划不继续施工。

计划80与计划82仍只有方案，不因计划81的分类而获批。计划76-V0.8／V0.8.1已由其原任务完成，不重复施工；计划81的G1与G5均已完成。Git提交／推送、正式模型调用、不可逆删除和新的产品施工均不包含在本记录中。

## 6. 复核边界

本记录证明的是当前文件、源码与证据之间的状态一致性，不重新执行所有历史自动化或正式环境验收。历史 PASS／HOLD／PARTIAL 以其原证据为准；当前仍存在的计划 70 残留源码问题另见 [`residual-audit.md`](./residual-audit.md)。

G0应用时已逐文件查看最新diff，未覆盖用户在本轮之外的新改动；完成后的链接、状态集合、`git diff --check`与工作区检查见[G1文档收敛记录](./g1-document-convergence.md)。
