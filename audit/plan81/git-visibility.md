# 计划 81：开发计划 Git 可见性复核

状态：**VISIBILITY-REVERIFIED / PLAN76-DELIVERED / PLAN79-82-DECISION-OPEN / NO-GIT-MUTATION-BY-PLAN81**

日期：2026-09-14（Asia/Shanghai）

## 1. 完整计数

使用 `rg --files --no-ignore` 枚举 `docs/开发文档/NN-*.md`，00—82 排除不存在的 39 后共有 82 份计划文档。Git 可见性分为：

| 类型 | 数量 | 计划 |
| --- | ---: | --- |
| 已跟踪 | 77 | 00—78，排除 39 和 70 |
| 明确忽略 | 1 | 70 |
| 未跟踪 | 4 | 79、80、81、82 |

`77 + 1 + 4 = 82`。没有发现第二份被忽略的编号计划，也没有同号多文件。

机器主状态清单见[`status-manifest.tsv`](./status-manifest.tsv)；其中包含82份计划级记录，以及计划76-V0.8、V0.8.1两条子范围记录。

## 2. 计划 70 是有意 local-only

`.gitignore` 第 23—26 行明确写明“Local-only Plan 70 planning, review, and acceptance evidence (author decision 2026-09-12)”，并精确忽略：

- `audit/plan68/审查整改报告.md`
- `audit/plan70/`
- `docs/开发文档/70-章节正文专属Skill智能调用最小可靠闭环计划.md`
- `audit/plan68/recovery/index.before-B002.js`

该规则来自已跟踪提交 `5692dbc`（2026-09-13 01:32:27 +08:00，`feat: harden managed writing skill workflow`），`git blame` 也把这四行归属到同一提交。因此计划70未出现在普通 `rg --files`、`git ls-files` 或 `git status` 中是已批准的本地保存策略，不是意外漏交付。

计划81不得自行删除忽略规则、强制添加计划70或提交其本地证据。若作者以后改变local-only决定，应单独裁决计划70正文、审查台账和证据是否整体或选择性进入版本控制，不能只强制加入一个文件造成证据链断裂。

## 3. 计划 79—82 尚未跟踪

- **计划79**：产品与正式环境闭环已经PASS，但计划文档及对应 `audit/qwen-tts/plan79/` 当前未跟踪。文档没有提供本轮可用的提交／推送授权；其工作包反而明确禁止自行提交。产品终态仍为 `CLOSED_PASS`，Git交付状态单列为 OPEN。
- **计划80**：仅方案，文档明确本轮不提交或推送；保持未跟踪不等于获批施工。
- **计划81**：本次治理计划，G0尚未通过，当前不应提前暂存或提交。
- **计划82**：仅方案，文档明确不提交／推送；保持未跟踪不等于获批施工。

二次复查时，计划76的Git状态已经变化：提交`32bea56`纳入V0.7.1、V0.8、V0.8.1的源码、计划、测试和证据，提交`38da2e0`补齐两级文档索引；两项提交均已位于`origin/main`，当前`main...origin/main`为`0/0`。因此计划76的上述增量已经完成Git交付，不再与计划79—82共用OPEN裁决。计划76证据中“本轮未提交或推送”只描述各自正式更新当时的时点，G0后需在计划76顶部补一条最新Git事实，避免把历史时点误读为当前状态。

计划79的“产品已通过”与“文档未纳入Git”必须同时陈述；不能用未跟踪状态否定正式验收，也不能用正式验收自动推导提交授权。计划80—82也仍保持未跟踪；本次计划81复查没有执行任何Git写操作。

## 4. 收敛门禁

计划81最终关闭前，应让79—82获得明确Git归宿之一：

1. 作者授权后，按精确文件清单审查、提交；push仍按当次授权；
2. 作者决定继续local-only，并像计划70一样记录清楚范围、原因和恢复方式；
3. 对未采纳方案，保留或移除文件仍需作者决定，不能由计划终态自动删除。

在获得决定前，计划81只记录可见性，不执行 `git add`、commit、push、强制添加被忽略文件、修改 `.gitignore` 或清理未跟踪文件。
