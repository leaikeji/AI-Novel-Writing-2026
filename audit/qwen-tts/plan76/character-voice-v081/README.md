# 计划76 V0.8.1 人物声音页交互收尾记录

日期：2026-09-14（Asia/Shanghai）。范围：[计划76第11.7节](../../../../docs/开发文档/76-Qwen3-TTS本地声音设计与复刻最小闭环计划.md#117-v081交互收尾2026-09-14)。

状态：**已正式生效，代码校验与正式桌面只读交互复验通过。** 本轮只修复人物私人音色流程的步骤呈现和键盘焦点；未改后端、数据库、模型、音色资产、人物绑定、正文或历史朗读，未调用TTS，未提交或推送Git。

## 修复结果

- 尚未明确选择可应用的私人音色时，不再提前显示`4. 应用到人物`。没有可用版本时显示完成条件；已有可用版本时先提供无编号选择提示，选中后才显示第4步、影响范围与保存动作。
- 点击`继续完成`后，焦点移到弹窗内可聚焦的实时状态区；按钮禁用后焦点不再掉到页面根节点，紧接着按`Esc`可以关闭人物卡。
- 单元测试覆盖零可用版本、已有锁定版本未选／选中切换及继续档案后的焦点目标。

## 代码校验

- 定向Vitest：2个文件、32项通过。
- 全量前端：170个文件、1618项通过。
- `pnpm typecheck`、`pnpm build`、`.venv/bin/python scripts/package_plugin.py`、`git diff --check`通过。
- 最终候选`frontend/dist/index.js` SHA-256：`4a8b436025732bd961670b3bc6e02405f36de250d071f54c0265d35fad91a930`。

## 正式更新与恢复

- 最终更新前即时恢复点：`/private/tmp/plan76-voice-v08-1-final-QkmIyD`。
- 恢复命令：`.venv/bin/python audit/qwen-tts/plan76/character-voice-v081/release.py rollback --backup /private/tmp/plan76-voice-v08-1-final-QkmIyD`。执行前仍须重新核对活动任务与正式包身份。
- 最终候选与正式包相比恰好只有`frontend/dist/index.js`变化；公开热更新后，精确包、Agent／Skill配置、产品健康、朗读生命周期和容器未重启均通过。
- 首个候选`6cd87e84c841a077c4ee208c7fef995ffdeedce5ef6795b3e7419968f3b28f92`在正式只读复验中暴露“存在其他锁定私人音色时仍提前显示第4步”的边界，随即按精确恢复点退回V0.8；未改变数据。修正后的最终候选再从V0.8基线备份并发布。

## 正式桌面复验

在正式`18088`通过《缺氧：末日地下世界》→角色→陈屿→声音完成只读交互核对：

- 当前实际声音仍为`Serena｜温暖女声 · 官方音色 · 普通话`；
- 展开私人音色时，页面有`继续完成 陈屿｜冷静坚韧男声`和无编号完成提示，不存在提前出现的`4. 应用到陈屿`；
- 点击继续后，活动焦点为弹窗内`role=status`，内容明确“已切换音色档案……尚未改变任何人物绑定”；
- 紧接着按`Esc`，人物卡正常关闭并返回正式人物列表；页面横向溢出为0；
- 浏览器错误日志无新增错误。

本次没有试听、生成、锁定、保存或人物绑定动作，因此不据此扩张为声音听感或TTS生成全链通过。
