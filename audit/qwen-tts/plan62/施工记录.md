# Plan 62 施工记录

状态：**PASS；生产 `0050`、角色维护、长期安装、浏览器播放和最终媒体销毁均已完成。**记录时间 2026-09-09（Asia/Shanghai）。

## W0 恢复点与冻结清单

- 施工前数据库 head：`20260909_0048`。
- 数据库 dump：`/app/working.backups/plan62-20260909/database.dump`。
- dump 大小：28,922,201 bytes；SHA-256：`a97615af9b900d87d96f7b5e5be11740764c589df921fd3ba322de8a1ea69048`。
- `pg_restore --list` 已成功读取，共 1,565 个 TOC entry。
- 施工前已安装 PawApp 快照：`/app/working.backups/plan62-20260909/installed-tree`。
- 旧 TTS 对象 ID 清单：`/app/working.backups/plan62-20260909/retired-object-ids.csv`，6,897 行（含表头），SHA-256 `34faf4cd65f9320e66aaec775ce18c8634647001e6a6b9384a5f74e1ade6c1ff`。
- 旧 TTS 媒体清单：`/app/working.backups/plan62-20260909/retired-media.csv`，1,678 行（含表头），SHA-256 `97c0b95c036e54f00b741ac74da61897ad1415f453b43121f7a591ea74df554e`；共 1,677 个本地文件、293,194,837 bytes。
- 媒体 dry-run 已逐个验证受控相对路径、文件大小和内容 SHA-256；无缺失、无共享引用、无活动任务引用。生产清理后 1,677 个文件先全部移动至 `/app/working.backups/plan62-20260909/retired-media-isolation` 并再次逐项验证；在 `0050`、角色、健康、浏览器和 Range 门禁通过后，按作者明确授权从隔离区销毁，销毁后文件计数为 0。活动媒体根始终不存在这些旧文件。

冻结对象计数：

| 对象 | 数量 |
| --- | ---: |
| 旧声音档案／版本／权利 | 62 / 66 / 66 |
| 旧 Edition／Edition 句段 | 19 / 1,880 |
| 旧 render／background job／model run | 825 / 898 / 931 |
| 旧媒体 | 1,677 |
| 旧人物绑定／旧旁白设置／旧匿名说话人 | 24 / 5 / 3 |
| `voice_design_drafts`／`voice_generator_commands`／run evidence | 33 / 34 / 23 |
| preparation commands／items | 5 / 30 |
| generic design／generation／pack／pack slots／pool／slots | 24 / 29 / 6 / 144 / 2 / 48 |
| character cast plan commands／items | 3 / 21 |

结构性断言：旧档案和旧 Edition 都没有与 Qwen 数据混合；候选媒体没有被当前 Qwen 版本、保留试听、保留 render/export、小说封面或未释放任务引用。6 个文档的当前朗读指针仍指向旧 Edition；清理时仅解除朗读指针，正文、revision 和故事账本不变。

## W1 失败恢复

施工前失败投影：107 个句段、104 个唯一任务组；其中 100 个唯一临时失败组覆盖 103 个句段，4 个为 `TTS_AUDIO_INVALID`。

- 通过公开 `retry-failed-segments` API、最新 CAS 和独立幂等键提交 100 个临时失败代表句段。
- 结果：103 个临时失败句段全部转为 succeeded，没有新增临时失败。
- 第 4、13、24、71 句分别执行一次独立本地 Qwen 重试，均再次返回 `TTS_AUDIO_INVALID`，底层证据仍为 `SHORT_CHINESE_DURATION_IMPLAUSIBLE`。
- 最终失败投影：request version 36、manifest revision 109、4 个失败，全部 `retryable=false`；Edition 为 `partial_ready`。
- 未调用云端 Provider，未修改正文或 revision。

这 4 个剩余项不是 MOSS 迁移故障。当前系统没有对已发布 Edition 做安全句段合并的公开操作；其中第 71 句还是人物说话，左右邻段均为旁白，不能无授权跨说话人合并。施工继续进入旧链解耦，最终页面会把它们显示为“音频质量／暂不可重试”，不再诱导重复生成。

## W2–W4 解耦、迁移与页面

- 活跃后端、前端、Compose 和运行配置已移除 MOSS/Nano/VoiceGenerator/24 槽通用池模块、接口、UI 分支和资源；历史迁移、ADR、计划及负向生命周期验证保留历史名称。
- 失败页面按“可恢复／音频质量／暂不可重试”分组；只在存在可重试项时显示“恢复全部可重试句段”，每批最多 100 个并在批间刷新 CAS。当前 4 项均不可重试，因此不显示会诱导重复生成的批量按钮。
- 生产 `0049` 已删除 14 张旧表及 pool/slot 列，旧声音版本、旧权利与旧表计数均为 0；保留 2 个 Qwen 声音版本。迁移首次因长期连接死锁完整回滚，停止 QwenPaw 后重试成功。
- 正式媒体清理结果：1,677 个文件、293,194,837 bytes；销毁前全部通过冻结 manifest SHA-256 `97c0b95c036e54f00b741ac74da61897ad1415f453b43121f7a591ea74df554e` 的路径、大小和内容哈希复核，销毁后隔离文件计数为 0。
- 无容器引用、仅保存退役 token 的 `ai-novel-2026-moss-tts-secrets` Docker 卷已精确删除；现行 QwenPaw、Qwen 本地运行时与小说媒体卷未删除。

## 0050 触发器修复与生命周期证据

隔离安装首次真实创建小说时暴露 `narration_validate_scope()` 仍静态引用已由 `0049` 删除的 `generic_voice_pools`。由于 `0049` 已在生产执行，没有改写迁移历史，而是新增前向迁移 `20260909_0050`，精确删除五段 pool/slot 分支并在函数形状漂移或仍有旧表引用时 fail closed。

- 演练库：`0049 → 0050` 成功，函数中 `generic_voice_pools`、`generic_voice_slots`、`target_pool_id`、`target_slot_id` 位置全部为 0。
- 完整隔离生命周期：[最终证据](./plugin-lifecycle-real-final3.json)；candidate tree SHA-256 `81a8bc7decf0414d1827c51cbe1d2ab5e48d805becbcd929282b1b4ab8a7d95f`；新建小说 201，安装、强制重装、卸载零残留、重装和哨兵保留全部通过，临时容器／卷／网络精确清理。
- 首次失败证据保留在 [原始记录](./plugin-lifecycle-real.json)，用于证明发现链路；重复诊断记录已删除，避免堆叠无独立价值的证据。

生产发布后首次角色 validator 发现 `writing_skill_dispatches` 与 `tts_cloud_profiles` 两张后续新增表仍由管理员角色持有。没有绕过门禁；仅把既有 bootstrap 和 schema-owner migrator 的精确步骤扩展到 `0049`／`0050`，通过 16 个角色契约测试后运行幂等 `bootstrap-20260909_0050`。最终[数据库角色证据](./database-role-validation-0050.json)为 PASS：59 张保护表，全部 application relation／routine／type 和数据库／schema 归属 `ai_novel_schema_owner`，API／worker 无 raw DML，运行连接切换继续 `HOLD`。

## 当前自动化与真实运行证据

- 后端全量：`2665 passed, 253 skipped`；跳过项依赖未提供的可选外部环境，3 条现有弃用告警无新增失败。
- 前端：151 个文件、1,286 个测试通过；TypeScript typecheck 与 Vite production build 通过。
- 打包、Compose 解析、`git diff --check` 通过。
- 活跃源码／配置扫描排除历史迁移、历史文档、负向测试和生命周期白名单后，MOSS/Nano/VoiceGenerator/通用池命中为 0；`git ls-tree -rl HEAD` 中不存在 50 MiB 以上的已跟踪 blob，模型权重未进入 Git。
- 五份隔离生命周期候选副本和一份辅助脚本均已从 QwenPaw 容器的精确 `/tmp/plan62-*` 路径删除，共释放约 54.4 MiB；长期安装树、恢复备份、演练库和隔离媒体未删除。
- 生产 `0050`、角色维护和最终媒体销毁后再次执行长期环境验证：PawApp `0.4.0`、宿主 health、朗读 lifecycle、production backend 和 worker 均为 `ready`，公开 Qwen 预设目录为 2 项。
- 真实章节 manifest 为 110 段，其中 106 ready、4 个明确质量失败；首段媒体 Range 请求返回 HTTP 206、`audio/mp4`、正确 ETag 与 4,096 字节范围，证明播放器能读取现行 Qwen 音频。
- 真实浏览器在最终销毁后刷新页面，仍显示播放器、110 段进度、4 个失败汇总及逐段朗读入口；从第 21 段启动后状态变为“正在播放”，随后已暂停。正文、revision、故事账本和非 TTS 媒体均未被本计划写入。
- 阿里云 Provider 未调用，真实云端 smoke 继续等待作者配置后另行授权。

## 最终发布与清理

- 长期生产数据库已由 `20260909_0049` 事务迁移到 `20260909_0050`，共享函数中 `generic_voice_pools`、`generic_voice_slots`、`target_pool_id`、`target_slot_id` 的位置均为 0；14 张退役表和 4 个退役列计数为 0。
- 长期安装树与已验证候选 SHA-256 一致；安装后应用健康、真实页面和音频 Range 均通过。
- 隔离的 1,677 个旧音频已精确销毁，释放 293,194,837 bytes。数据库 dump、对象／媒体 manifest 和旧安装快照继续保留；临时管理员 pgpass、维护根卷、容器脚本、演练数据库和临时 dump 已精确删除。

## 恢复方式

当前 Qwen 运行链可由保留的数据库 dump、对象／媒体 manifest 和 `installed-tree` 复核施工前状态；生产 `0050` 本身为前向迁移，失败恢复应恢复数据库 dump 与旧安装树，不能只回退镜像。作者已经明确不会回退 MOSS，并授权最终销毁旧音频，因此被销毁的 1,677 个退役音频不再能从本项目隔离区恢复；若未来强行恢复施工前 MOSS 朗读，只能依赖项目之外另有的外部备份。本计划保留的恢复资料足以审计删除范围，但不再包含被明确销毁的旧音频内容。
