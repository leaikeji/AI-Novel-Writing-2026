# MOSS 模型与可重建制品清理记录

状态：**2026-09-10 整机补充清理已执行并复核**
日期：2026-09-08；补充复核：2026-09-10（Asia/Shanghai）

## 授权与边界

作者在听检 Qwen3-TTS CustomVoice、Base 克隆和 VoiceDesign 样音通过后，明确要求先删除 MOSS 模型及清理文件释放空间。

首次清理只删除 MOSS 运行制品和可重建依赖；作者随后明确不再回退 MOSS，因此又删除了当前 Compose 拓扑与 sidecar 镜像源码。全程不删除小说正文、revision、故事账本、PostgreSQL/QwenPaw 数据卷、小说媒体、Qwen 模型或历史验收证据。

## 已删除

- Docker 容器：
  - `ai-novel-2026-moss-tts-sidecar`
  - `ai-novel-tts56-sidecar`
  - `ai-novel-2026-moss-tts-model-installer`
  - `ai-novel-2026-moss-tts-runtime-init`
- Docker 卷：`ai-novel-2026-moss-models`，删除前占用约 763.4 MiB。
- Docker 镜像标签：
  - `ai-novel-world/moss-tts-sidecar:t1-b-linux-arm64`
  - `ai-novel-world/moss-tts-sidecar:tts56-lease-guard`
  - `ai-novel-world/moss-tts-sidecar:tts56-reference-bound`
  - `ai-novel-world/moss-tts-sidecar:tts56-before-reference-bound`
- 可重建原型依赖：
  - `prototypes/moss-tts-nano/.venv`，约 589 MiB；
  - `prototypes/moss-tts-nano/node_modules`，约 316 MiB。

删除后 `prototypes/moss-tts-nano` 仅剩约 964 KiB 的源代码、锁文件与测试材料。磁盘可用空间由约 15 GiB 增至约 24 GiB；Docker Desktop 的稀疏磁盘回收会使该变化大于简单文件标称大小之和。

## 2026-09-10 整机补充清理

后续只读复核发现，早期清理只覆盖了 Docker 资源和仓库依赖，未覆盖 macOS 宿主上的两个旧模型目录，而且旧 VoiceGenerator LaunchAgent 仍在运行。因此早期“清理完成”只能解读为 Docker 范围完成，不能代表整机已清理。

在作者再次明确同意且无需回退 MOSS 后，已完成：

- 停止并卸载 `com.ai-novel-world-2026.voice-generator` LaunchAgent，确认原 PID 退出、目录无打开文件；
- 删除 `/Users/liujia/Library/Application Support/AI小说世界2026/moss-models`，删除前约 892 MiB；
- 删除 `/Users/liujia/Library/Application Support/AI小说世界2026/voice-generator-vg40`，删除前约 12 GiB，其中旧模型约 11 GiB；
- 删除对应 LaunchAgent plist，以及仓库删除目录下剩余的 `__pycache__`；
- 清理后重新查询宿主目录、进程、launchd、Docker 容器／镜像／卷，均未再发现 MOSS、Nano 或 VoiceGenerator 运行制品；
- QwenPaw `/health` 仍为 `ready`，本地 Qwen TTS Provider 和朗读 worker 仍健康；当时数据卷可用空间约 39 GiB。

二次复查又发现，仍在运行的本地 Qwen TTS 使用了约 14 MiB 的旧全量源码快照 `runtime-src-20260908-0043`。该快照夹带已退役源码和 MOSS/Nano/VoiceGenerator `.pyc`，虽然当前入口未导入这些模块，仍不满足“无可执行残留”的要求。作者授权后继续完成：

- 从当前未修改的 Qwen 源文件建立 132 KiB 的 `runtime-src-20260910-qwen-only`，只包含 `tts_runtime`、TTS 公共契约和 Qwen Provider 适配器；
- 新快照导入验证通过，且创建时未包含旧模块、旧名称或旧 `.pyc`；运行后生成的 `__pycache__` 仅对应现行 Qwen 模块；
- 正常停止旧进程并将本地 TTS 切换至新快照，确认新 PID 监听 `8766`，父进程 `PYTHONPATH` 精确指向新快照；
- 真实合成“这是清理后的本地语音验证。”成功，返回 RIFF/WAV、24 kHz、172,844 B，实际模型为 `mlx-community/Qwen3-TTS-12Hz-1.7B-CustomVoice-8bit`，运行时为 `mlx-audio`；
- 在确认旧快照无打开文件后删除 `runtime-src-20260908-0043`；
- 删除令牌配置脚本中仍可写入旧 VoiceGenerator 容器密钥路径的兼容入口。

同日继续按“零可执行残留”复查并完成：

- 删除仓库忽略目录中的 `scripts/tts/voice_generator/` 字节码目录，以及 backend、tests、scripts 中已删除旧模块留下的 MOSS/Nano/VoiceGenerator `.pyc`；
- 删除 QwenPaw 密钥卷中未被当前 Compose、容器环境或 PawApp 引用的旧 `voice-generator/` 令牌目录；
- 扫描已安装 PawApp：现行业务 Python、前端 bundle 和 Provider 代码无旧实现命中；只保留必须参与升级链的历史 Alembic migration，并删除其可重建 `__pycache__`；
- 生产数据库 schema 名称无 MOSS/Nano/VoiceGenerator 表或字段；旧 voice version、background job、model run、云端模型配置命中数均为 0；Alembic head 为 `20260909_0050`；
- 常见 Hugging Face／ModelScope 缓存、应用目录、LaunchAgent、Docker 容器／镜像／卷及 build cache 标签均无旧模型制品命中；Git 未跟踪模型权重。

当前唯一非项目自有命中来自 Qwen 本地运行所依赖的上游 `mlx-audio 0.5.2`：该通用发行包同时内置 MOSS 系列适配器，共 44 个文件、约 554 KiB，但不包含 MOSS 权重，当前 Qwen 配置和模型身份也不使用它们。不能通过手改 site-packages 将它们删除，否则会破坏第三方包完整性和可重建性；若“零残留”也包含第三方发行包内的未启用适配器，需要另行批准把本地运行时整体迁移到不依赖 `mlx-audio` 的官方 Qwen3-TTS 路径，再删除整个现有 venv。

复查后的 Qwen 可用性证据：

- 从 QwenPaw 容器经正式令牌挂载和宿主网络调用本地 runtime，Aiden 合成返回 HTTP 200、RIFF/WAV、24 kHz、哈希一致；
- 从已安装 PawApp 的 `LocalQwenTTSProvider` 严格契约调用 Serena，health 为 `healthy`，返回 RIFF/WAV、24 kHz、130,604 B、哈希一致；
- 两次实际模型身份均为 `mlx-community/Qwen3-TTS-12Hz-1.7B-CustomVoice-8bit`，运行时为 `mlx-audio`。

## 有意保留

- `ai-novel-tts56-qwenpaw-data` 与 `ai-novel-tts56-qwenpaw-secrets`：属于 QwenPaw 数据/密钥卷，不因“MOSS 模型清理”推断为可删除。
- 11.44 GiB Docker build cache：包含多个项目且无可可靠归属到 MOSS 的标签，不执行跨项目 `builder prune`。

## 后续部署收口

- `compose.yaml` 已删除三个 MOSS 服务、MOSS 模型/令牌卷声明、两个专用网络、QwenPaw 对旧 sidecar 的依赖及全部 `MOSS_TTS_*` 环境变量；以后执行 Compose 不会重新下载或启动 MOSS。
- 已删除无调用者的 `docker/tts-sidecar/**`，并从 `.dockerignore` 移除旧构建上下文放行项。
- 通用 FFmpeg 工具链继续保留，仅将构建身份变量从 `MOSS_FFMPEG_BUILD_ID` 改为 `NARRATION_FFMPEG_BUILD_ID`。
- 2026-09-10 复核时，旧 MOSS 令牌卷也已不存在；当前 QwenPaw 只保留自身数据／密钥卷和现行 Qwen TTS 连接。

## Git 核验

- `.git` 当前约 265 MiB。
- 全历史最大 blob 为 `frontend/assets/novel-cover-fengcunqu.png`，2,619,778 B。
- 未发现 Git 跟踪的 `.safetensors`、`.onnx`、`.pt`、`.pth` 或 `.gguf` 大模型权重；MOSS 大模型未进入 Git 历史。

## 恢复方式

本次制品未备份，当前工作树也不再保留旧 sidecar 的 lock、Dockerfile 和 Compose 定义；按作者“不再回退 MOSS”的决定，不提供运行时回退路径。Qwen 模型权重、令牌、数据库、小说正文和既有音频均未删除；Qwen-only 本地运行环境已通过健康检查与真实合成验证。
