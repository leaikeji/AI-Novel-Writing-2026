# 计划 61：长期部署与浏览器复核

日期：2026-09-08（Asia/Shanghai）

状态：**PASS；真实云端模型调用 HOLD。**

## 发布与恢复

- 发布前长期数据库 head：`20260908_0043`；发布后 head：`20260908_0044`。
- 发布前备份：`/app/working.backups/plan61-cloud-config-20260908-215616-before`。
- 宿主数据库副本：`/private/tmp/ai-novel-world-2026-plan61-20260908-215616/postgres-before.dump`。
- 备份包含旧安装树、冻结候选和 PostgreSQL custom dump；dump 已通过 `pg_restore --list`。
- dump 在 PostgreSQL 临时目录、QwenPaw 备份卷和宿主副本的 SHA-256 一致：`627aab6e839f368a2d82bc8f3de39693305968c99927f94abcb8d127e1e4bfdc`。
- 恢复时先停止 QwenPaw，通过公开插件机制安装 `installed-plugin-before`；只有精确确认需要回退 schema 时才从 `postgres-before.dump` 恢复，禁止删除 PostgreSQL、QwenPaw、密钥或媒体卷。

## 候选与安装身份

- 候选：`build/ai-novel-world-2026`。
- 候选 tree SHA-256：`9c34f3ca52dca344a5f97c3b182047953a490c97815cf487d4300b690751a1a5`。
- 候选 Alembic head：`20260908_0044`。
- 长期 QwenPaw container ID：`664c14858e68f020d6afa9d5edf520d6277e136b039e6d89f31d65aece0948ee`。
- QwenPaw image ID：`sha256:3a15aa8c762dfb1254332dafef931c7cf6dd377ede4d93380da39eb27db9b96e`。
- 安装过程先保持 PostgreSQL 运行并停止 QwenPaw，用备份卷中的冻结候选执行 additive `0043 -> 0044`，随后通过 `qwenpaw plugin install --force` 的公开机制安装候选并启动原容器；没有修改 QwenPaw 上游核心。
- 已安装树启动后包含运行时 `__pycache__`／`.pyc`；排除这些生成文件后，与冻结候选 `diff -qr` 无差异。

## 运行复核

- `ai-novel-2026-qwenpaw-lab`：`healthy`。
- `ai-novel-2026-postgres`：`healthy`。
- PawApp `/api/ai-novel-world-2026/health`：`status=ready`、数据库 connected。
- narration 与 narration production：`lifecycle_status=ready`、production backend installed、worker running。
- 数据库：`20260908_0044 (head)`。

## 真实浏览器复核

桌面 Edge：

- 刷新长期页面后，创作中心第三入口“语音模型接入”可见并可打开。
- 页面显示本地 Qwen3-TTS“运行正常”，云端渠道为空。
- “新增渠道”表单包含渠道名称、固定协议、Base URL、质量模型 ID、可选速度模型 ID、只写 API Key。
- 页面明确“保存配置不会联网测试或产生费用”，保存按钮显示“保存配置（不测试）”。
- 控制台 error/warn 均为空。

390×844 窄屏 Edge：

- 创作中心第三入口、配置页空态和新增渠道表单均可访问。
- 页面度量为 `clientWidth=386`、`scrollWidth=386`、`bodyScrollWidth=386`，无横向溢出。
- 控制台只有 QwenPaw 宿主资源的 `[moduleRegistry] Module not found: AppCenter` warning；配置页没有自身 error/warn。

## 未执行边界

- 没有保存任何虚构渠道或测试数据。
- 没有读取、填写或回显真实 API Key。
- 没有调用任何云端 TTS，没有发送小说文本或参考音频，也没有产生计费测试。
- 作者配置渠道并再次明确授权后，才执行质量／速度模型固定短句和本地／云端最小章播放验证。
