# 计划 61：Base URL 自动补全 HTTPS

- 日期：2026-09-08
- 状态：PASS；已热安装到长期 `18088`

## 改动

- 纯域名或路径前缀在输入框失焦时自动补充 `https://`。
- 保存时再次执行相同规范化，避免未触发失焦时漏补。
- 已包含任意显式协议的地址不重复拼接；`http://`、`wss://` 等不受支持协议继续交由既有 HTTPS 安全校验拒绝，不静默改写。

## 验证

- `pnpm test -- frontend/src/narration/cloud-config/cloud-config-page.test.ts`：实际执行 151 个测试文件、1281 项测试，全部通过。
- `pnpm typecheck`：通过。
- `pnpm build`：通过。
- `.venv/bin/python -m pytest tests/narration/test_tts_cloud_profiles_api.py -q`：10 项通过。
- `.venv/bin/python scripts/package_plugin.py`：通过。
- QwenPaw `plugin validate`：通过；随后经公开 `plugin install --force` 热安装成功，无需重启。
- 长期 QwenPaw 与 PostgreSQL 容器均为 `healthy`；PawApp `status=ready`，narration `ready`。
- Edge 独立标签页真实验证：输入 `workspace.cn-beijing.maas.aliyuncs.com` 并失焦后显示 `https://workspace.cn-beijing.maas.aliyuncs.com`；再次填写已有 HTTPS 地址后保持单一前缀。测试表单已取消，未保存渠道。
- 本地候选、打包候选与长期安装 bundle SHA-256 一致：`4222d7ef6ea83afb4b1b686a34dfef804ba52cadf537fb55808ece970f064b0c`。
- `git diff --check`：通过。

## 安全与恢复

- 未读取、填写或回显真实 API Key，未调用云端模型，未产生计费请求。
- 更新前安装树备份：`/app/working.backups/plan61-url-normalization-20260908-2303-before`。
- 如需恢复，使用 QwenPaw 公开插件安装机制回装上述备份；不删除数据库、小说、媒体、模型或任何持久卷。
