# Qwen TTS 长期部署与浏览器复核记录

日期：2026-09-08（Asia/Shanghai）

状态：**本地链 PASS；阿里云真凭据 smoke、窄屏复核与历史数据库物理清理待完成。**

## 部署与恢复

- 正式 QwenPaw：`2.1.0`，PawApp：`0.4.0`。
- 正式数据库迁移：`20260903_0040 -> 20260905_0041 -> 20260905_0042 -> 20260908_0043`。
- 发布前可恢复快照：`/app/working.backups/plan59-qwen-cutover-20260908-173327-before`。
- 快照内容：旧安装树、候选安装树、`postgres-before.dump`；dump 已通过 `pg_restore --list`，传输前后 SHA-256 均为 `8766e22aeaefb3d7365660c598b198fbdffa94de652c92bc3eed8fb1da0a5101`。
- 恢复原则：代码或插件故障使用快照中的旧安装树；数据库只在精确确认需要时从 dump 恢复。不得删除 PostgreSQL/QwenPaw 数据卷，也不恢复已明确弃用的 MOSS 模型链。

## 运行态证据

- `docker compose ps`：`ai-novel-2026-qwenpaw-lab` 与 `ai-novel-2026-postgres` 均为 `healthy`，只暴露既有回环端口 `18088`、`15432`。
- PawApp 公共健康端点：`status=ready`、`version=0.4.0`、数据库连接成功；朗读生命周期此前已复核为 `ready`，正式执行后端已安装且 worker 正在运行。
- 本地运行时由脱离终端的 `screen` 会话 `ai-novel-qwen-tts` 托管，监听 `0.0.0.0:8766`；正式 Provider 携带协议头和仓库外令牌复核结果为 `healthy`。
- 当前模型身份：`mlx-community/Qwen3-TTS-12Hz-1.7B-CustomVoice-8bit`，revision `41d3337e8b7f2843a75841595fc14e4b9a7a4b96`，制品树 SHA-256 `728e8b60b4cdb195a1379faf033b428bf93feaceccbdcf3c3a4bd3a6698b4fb6`，Provider `local_qwen3_tts`，量化 `8bit`。
- 本地模型与 Python 运行环境位于仓库外 `/Users/liujia/Library/Application Support/AI小说世界2026/qwen-tts`，未进入 Git。

注意：健康端点有协议头与令牌保护；不带正式 Provider 协议头的裸请求按设计返回 `409 TTS_MODEL_IDENTITY_MISMATCH`，不能据此判断运行时故障。

## 浏览器复核

在真实 QwenPaw 页面打开作品“雾宅来信”的“朗读”页后确认：

- 页面显示“朗读设置可用”；
- “本地 Qwen3-TTS”默认选中，文案明确本机处理、无云端调用费用；
- “阿里云 Qwen-Audio 3.0”作为独立显式选项显示，并说明北京地域、Plus/Flash 与按量计费；
- 运行状态显示“本地 TTS 技术就绪”，当前设置层无阻断项；
- 旧 MOSS 音色档案不再投影到当前 Qwen 音色选择器，页面不再出现 `INVALID_STATE`；
- 浏览器控制台 `error`、`warn` 均为空。

## 保留项与限制

- 未使用真实阿里云 TTS 凭据发送正文或参考音频，因此只可声明“云端 Provider 已接入”，不可声明“云端生产 smoke 已通过”。
- 历史 MOSS 数据库记录和历史迁移仍保留；现行选择器只做兼容过滤。精确物理清理需另过清理门禁。
- 本次只完成桌面真实浏览器复核；受影响窄屏路径仍待复核。
- LaunchAgent 因 macOS 启动上下文下 Python codec 初始化不稳定已撤回。当前 `screen` 方案简单且可用，但退出登录或重启后需要人工启动，不宣称开机自启。
