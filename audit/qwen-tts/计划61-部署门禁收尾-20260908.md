# 计划 61：部署门禁收尾

日期：2026-09-08（Asia/Shanghai）

状态：**类型与 Skill 注册门禁修复完成；完整部署验收未通过，发现 Qwen 音色数据库兼容阻断。** 用户要求“先收尾之前的任务”；不重启计划 58 全量评测或计划 60 页面重设计。

## 当前事实与边界

- 长期部署报告已经记录 `0043 → 0044`、备份、安装和浏览器复核。这些历史记录保留，不把后来修复写成部署前已通过。
- 当前另一个任务仍显示活动状态；长期运行环境和共享 `build/` 暂未接管。本轮候选使用独立临时目录，隔离安装不接触长期环境。
- 不修改 Skill 正文、发布批准记录、启用策略、迁移历史、QwenPaw 核心或用户小说；不调用真实云端模型，不提交或推送 Git。

## 子代理并行施工设计

本任务不并行：剩余内容是小范围类型修复、一个校验脚本和唯一运行环境收尾，主代理串行集成；独立测试进程可以并发，但不派发子代理。

| 波次 / 工作包 | 类型 | 目标、文件所有权 | 前置门禁 / 验收 |
| --- | --- | --- | --- |
| W0 / Q61-CLOSE-T | SER | 仅 `frontend/src/assistant-context-transport.ts` 及测试、`writing-skills/native.ts` 及测试、`assistant-route-wrap.ts` 的类型和范围保护 | 保留已有计划58改动；不放开功能；typecheck、相关 Vitest、全量 Vitest |
| W1 / Q61-CLOSE-S | SER | 仅 `scripts/tts/verify_qwenpaw_plugin_lifecycle.py` 与 `tests/narration/test_qwenpaw_plugin_lifecycle_runner.py` 的候选 Skill 清单门禁 | 冻结候选自身批准记录为输入；拒绝缺项、多项、未批准项、哈希漂移；定向 pytest |
| W2 / Q61-CLOSE-V | GATE | 主代理独占 `/private/tmp/ai-novel-q61-closeout-diAS3O/` 及本收尾报告、唯一前缀隔离资源 | 全量 pytest、构建与打包、完整生命周期门禁；只清理本次带所有权标签资源 |
| W3 / Q61-CLOSE-I | MUTEX/INT | 长期环境只在原部署任务释放锁后由唯一主代理接管；计划61状态同步 | 先备份，核验当前0044，不重复迁移；公开安装、浏览器检查；未释放锁不得同时部署 |

共享文件、数据库、容器、Agent/模型状态是互斥资源。除以上明确范围，其余改动只读并保留。冻结 API 路径、DTO 字段与发布批准内容不变；类型修复匹配已存在的创建草稿回执接口。汇合顺序 T → S → V → I，唯一集成责任人为本任务主代理。

## 已完成验证

- TypeScript `tsc --noEmit`：退出码 0。
- 定向 4 组 Vitest：41 项通过；全量 151 个文件、1281 项通过。
- 生命周期脚本定向 pytest：59 项通过（包括获批分类接受，缺项、多项、缺批准索引和资产哈希漂移拒绝，以及错误响应仅记录脱敏证据）。
- 全量 `.venv/bin/python -m pytest -q`：退出码 0；可选外部环境测试按既有条件跳过，存在已有 Starlette 废弃警告。
- `git diff --check`：通过。

## 修复内容

1. 建书草稿与小说上下文采用可区分联合类型；建书草稿回执使用已有的创建草稿端点，恢复时校验范围与 tab，不混用小说标识。空路由显式拒绝。
2. 安装门禁不再把注册集合写死为9项：从冻结候选读取发布批准记录，复用目录/资产校验，要求批准集合、实际候选文件、安装注册集合精确一致。旧的仅9项通用 Skill 候选保持兼容。不从宿主已注册结果倒推“批准”。
3. 发布清单记录到证据，候选读取后再次比对整树哈希；卸载零残留、工具集合、数据库哨兵、原生路由等原有保护继续保留。

## 隔离复现与新增阻断

- 自建候选构建、打包审计通过，tree SHA-256 为 `9ee29812136dc21377823a89490057493adc776b48776c663d89df454a3aeecf`；不覆盖共享 build。
- 另一个任务随后发布了 Base URL 补全，已记录 typecheck 通过及热安装。为避免回装较旧候选，本轮再复制其发布包至 `candidate-current`，整树 SHA-256 为 `29669d0df4b3231a0cb2177016c53620a50a2bea867d8346fdcf57eaa7ea43c5`；对应源构建、共享发布包与本轮复制件的 bundle SHA-256 均为 `4222d7ef6ea83afb4b1b686a34dfef804ba52cadf537fb55808ece970f064b0c`。正式安装同一 bundle 的证据来自原部署任务报告，本轮没有重复安装。
- 两轮隔离均已接受11项获批 Skill 精确注册，随后 `POST /api/ai-novel-world-2026/novels` 返回500；因此**完整安装/升级/卸载/重装链尚未通过**。
- 第二轮仅在独立数据库中调用本项目服务定位：`create_novel → initialize_new_novel_default_narrator → flush` 触发 `IntegrityError`，SQLSTATE `23514`，约束 `ck_voice_action_command_preset_key`。原始异常文本与凭据没有写入报告，只记录异常类型、约束名、代码栈位置。
- 已核实：`0031` 的该 CHECK 仅接受 `onnx.*`，而当前默认值是 `qwen.WarmFemale`；同一历史迁移的延迟闭合触发器还要求旧 MOSS 模型、旧版权来源及旧参数结构。`0043` 只迁移资源类、`0044` 只增加渠道表，没有更新这部分。不能只放宽 CHECK 或禁用触发器绕过。
- 尚未验证：没有在正式环境创建测试书籍；不能把隔离复现写成已对用户真实小说执行过测试。但同候选与相同0044迁移存在正式建书风险。
- 两次隔离运行均已按唯一名称和标签清理各自2个临时容器、5个临时卷和1个网络；只删除测试哨兵，可由脚本重建，没有删除正式数据或用户小说。

证据：[第一轮](./plan61-closeout-lifecycle-20260908.json)、[第二轮及脱敏定位](./plan61-closeout-lifecycle-20260908-r2.json)、[长期页面只读截图](./plan61-closeout-longterm-readonly-20260908.jpg)、[原任务热安装报告](./计划61-BaseURL自动补全-20260908.md)。

## 剩余工作与交接

1. 先确认唯一迁移／部署所有者。原 TTS 任务仍显示活动，尚未收到释放共享写入的确认；已将精确失败路径、约束和证据发给原任务。此时不新建并行迁移、不执行正式数据库操作。
2. 经交接后新增迁移（当时重新核实唯一 head，暂拟0045），为 Qwen 音色补齐 CHECK 与证据闭合，保留旧记录及原有不可变、scope、receipt、rights、投影保护；不得改写0031/0043/0044。
3. 补真实 PostgreSQL 的新建书成功、无效音色/跨范围/错误来源拒绝及旧证据保留测试，再跑完整隔离生命周期。
4. 隔离通过后再备份、迁移、公开安装和浏览器验收；计划61不能提前标记全部通过。云端真实计费测试仍为独立 HOLD。
